# QGNN＋LLM预测优势：优化修订稿

日期：2026-09-12。依据：同日《研究方案书.md》v1.0、《ROADMAP.md》v1.0及本任务讨论。

状态：完成纸面设计与条件分析的候选修订，尚无数值校核、训练或性能结果。本稿是可审阅的设计成果，未覆盖两份v1.0执行协议；不能与其混用后声称执行了同一版实验。当前目录未找到两份文件提及的protocol.yaml，正式实施时还须补齐一致配置。本轮不实施代码，不运行训练。

## 1. 目标：合理训练后的ADE、FDE，而非人为受限条件下的胜利

优化问题是：在同一三站观测、完整合法训练集、相同LLM规格与合理开发训练条件下，能否让QGNN＋LLM的ADE和FDE都低于GNN＋LLM。

“有限训练”指任何实际学习都只有有限数据和计算，不是“优先训练”，也不是要求故意少给GNN样本、时间或调参机会。它在理论上用于区分“某函数能被表示”和“真实训练能学到这个函数”。本研究不以小样本、少epoch或相同墙钟下的优势替代主预测优势。

主比较仍使用100%合法训练数据；50%曲线仅作可选解释。若任一模型在预定训练结束时仍有明显开发集改善趋势，则相应结论只能描述该日程下的性能，不能称其代表合理收敛后的能力。若资源允许延长，必须在测试解封前记录共同修订的停止规则，并允许两侧依同一规则获得延长；无法完成时报告未解决的训练充分性限制，不挑选较弱GNN检查点。

GNN不被宣称为某一永久限定类别；v1.0的具体实现只是可复现的实际对照。所有主要结果名称仍统一GNN，论文方法部分如实给出实际结构。一次具体比较不能覆盖所有GNN。

## 2. 修订裁决：一个候选，两处相互衔接的变化

根据用户后续明确决定，本稿选择“Q分支内部接近关系编码＋量子读出的信息保留式扩展”作为一个候选。经典GNN保持v1.0结构和正常训练，不追加特征或专门模块，不并行开启多个新电路族。

| 位置 | v1.0 | 本稿候选 | 理由 |
|---|---|---|---|
| Q边输入 | 四维相对位置、相对速度 | Q分支内部追加一维平滑有符号接近量，共五维 | 根据已有状态计算任务关系，无额外传感器或标签信息 |
| Q读出 | 21维 | 保留全部21维，追加3维接近量加权的浅层连接相关量，共24维 | 避免直接替换已有信息；为聚合提供不同权重 |
| Q核心参数 | 三层45个共享标量 | 三层48个 | 每层边函数多一个权重 |
| Q电路/比特 | 三块ZX、最多16比特 | 沿用 | 尚无证据表明必须增加深度或比特 |
| GNN | 常规非线性消息、128维、四维边 | 保持冻结实现及合理训练 | 不额外加强，也不削弱现有基线 |
| 时间模型 | GPT-2/LoRA，逐车时序建模 | 沿用 | 先判断图表示变化能否被现有接口利用 |
| 监督与输出 | 恒速外推＋修正，ADE＋0.5FDE | 沿用，不增加重复残差损失 | 现有目标已经监督运动修正 |

接近关系是Q分支内部的经典计算，不计作量子独有贡献。Q/G获取相同原始估计状态，但内部特征处理不同。新24维中的加权也是经典读出聚合。主比较可以检验完整QGNN混合方案相对冻结GNN的预测收益，不能仅凭这一比较把收益全归于量子门；本轮不要求额外研发增强GNN。

## 3. Q分支内部物理关系编码

对同一时刻合法估计状态，定义

\[
r_{ji}=\widehat p_j-\widehat p_i,\qquad
u_{ji}=\widehat v_j-\widehat v_i.
\]

原始距离变化率在非零距离为rᵀu/||r||。为避免位置几乎重合时除零，采用

\[
c_{ji}=-\frac{r_{ji}^{\mathsf T}u_{ji}}
 {\sqrt{\|r_{ji}\|^2+\varepsilon^2}},\qquad
a_{ji}=\tanh\left(\frac{c_{ji}}{s_v}\right),
\]

其中ε=1m、s_v=15m/s为本候选的预声明工程尺度，不是实测最优值，也不是文献常数。c>0表示接近，c<0表示远离。c是平滑的径向接近速度代理，不等同于TTC、碰撞概率、道路优先权或因果影响。

Q分支内部使用下式，GNN仍使用原四维边输入：

\[
e^+_{ji}=[r_{ji}/45\mathrm m,\ u_{ji}/15\mathrm{m/s},\ a_{ji}].
\]

沿用距离核κ与存在mask，量子边角度改为

\[
g_{j\to i}^{(\ell)}=
\frac{m_i m_j\Gamma_\ell\kappa_{ij}}7
\tanh\big((w_\ell^+)^{\mathsf T}e^+_{ji}+b_\ell\big).
\]

w⁺由4维增至5维；GNN消息MLP输入仍为2d+4，消息与更新输出宽度仍为128。Q侧a仅由两侧共有的合法估计状态计算，不用真值位置、真值速度或未来标签。明确披露Q内部包含这一物理特征处理。

该特征有三个可核查性质：

1. 对共同位置平移不变；对位置、速度共同正交变换不变。但整个QGNN仍含绝对坐标编码，不能据此声称整个模型平移或旋转等变。
2. 交换i、j后r和u同时变号，因此a_ji=a_ij。接近量自身是对称关系；有向边仍由原四维关系及不同P/V寄存器定义，不能将a称为方向因果信息。
3. ε>0保证无除零，且|a|<1。局部敏感性满足

\[
\|\nabla_r c\|\le\|u\|/\varepsilon,\qquad
\|\nabla_u c\|\le1;
\quad
\|\nabla_r a\|\le\|u\|/(s_v\varepsilon),\quad
\|\nabla_u a\|\le1/s_v.
\]

因此平滑只消除奇点，不保证近距离高相对速度时抗噪。位置重合时c=0仅是此代理的定义值，不能解释为无碰撞风险。新增a是旧输入的确定性函数，没有增加观测信息；可能的作用是改变模型更容易利用哪些关系。急转弯、并排行驶、信号灯和驾驶意图也不能由一个接近量完整解释。

## 4. 保留原读出，再补充有任务含义的聚合

沿用浅层态χ₁=Q₁E₁|0〉及连接相关量C(A,B)=〈AB〉−〈A〉〈B〉。原21维全部保留。

追加

\[
q^{app}_{i,1}=\frac17\sum_{j\ne i}m_im_ja_{ji}C(Z_{P_j},Y_{V_i}),
\]
\[
q^{app}_{i,2}=\frac17\sum_{j\ne i}m_im_ja_{ji}C(Z_{P_j},Z_{V_i}),
\]
\[
q^{app}_{i,3}=\frac17\sum_{j\ne i}m_im_ja_{ji}C(X_{P_j},Z_{V_i}),
\]

并定义q⁺_i=[q_i,q^app_i]∈R²⁴。没有邻车时新增三项为0；固定分母7与车辆重排规则沿用。各原始连接量涉及不同比特的Pauli观测，可由协方差Cauchy界得|C|≤1，故三项均有界于[-1,1]。

这些逐边连接量已用于原浅层三个聚合通道，因此不需要新增Pauli种类、量子门或整图态调用。若原实现流式聚合，可同步维护三个加权和；新增经典乘加、额外输入梯度与实际后端内存仍须计量，不能说成本为零。

为什么补充聚合可能有用：不加权求和只保留一类统计量，加权求和还保留相关量与接近关系之间的对应。仅作代数说明，两条边的相关值为z、−z时原和为0；若权重为a、−a，新和为2az。该例不是声称当前电路一定实现这组数值，也不是证明对应未来一定不同，更不是GNN无法区分的反例。

LLM图投影由W_g∈R^(768×21)变为W_g⁺∈R^(768×24)。本车四维状态与两个标记保留，所以Q侧总数值输入由27维变为30维；G侧仍为134维，不追加边特征。

参数账须包括经典接口：Q投影新增768×3=2304个权重，三层Q核心新增3个标量，合计新增2307个可训练参数；G不增加参数。若使用原来允许的两层备选，则Q核心30→32、整模型新增2306个；新增24维读出结构不随层数变化，深层读出取实际最终层。以上是设计算术，不是可训练参数实测。

### 4.1 能严格说明的改进：旧函数可以被完整保留

将每层w⁺第五分量设为0，并取W_g⁺=[W_g,0,0,0]，其余参数一致，候选与原Q模型输出完全相同。因此，在参数域其余约束不变时，旧Q函数类包含在新Q函数类中；GNN函数类保持原样。

这证明“存在一个不比原模型差的候选参数设置”，也证明针对任一固定损失的函数类最优值不会因扩展而变差。它不证明优化器会找到更好解、不证明有限样本泛化改善，也不证明Q相对G的差距会扩大；冻结GNN仍可能预测得更好。

还需区分：分别最小化ADE或FDE的最优值不增，与最小化组合J得到的同一个模型同时降低ADE、FDE，不是同一命题。函数类嵌套并不能排除J更低但其中一项更差。

新增Q边权与新增投影列可采用上述零值初始化以保留旧初始输出，但这只是明确起点，不是收敛保证。因为初始图交互为零，新连接读出也可能为零，不能声称所有新增路径在第一步都有非零梯度；必须核查图启动后各路径是否获得任务梯度。

## 5. 训练衔接：撤回重复辅助监督，使用真实增量诊断

上一条讨论提出研究额外残差监督。核对v1.0后发现，预测本来就是

\[
\widehat p_h=b_h+\widehat r_h,\quad
b_h=\widehat p_t+h\Delta t\widehat v_t.
\]

令r*_h=p_h−b_h，则

\[
\|\widehat p_h-p_h\|_2=\|\widehat r_h-r^*_h\|_2.
\]

因此对同一时间步、相同mask和权重，额外加入相同形式的“恒速残差ADE/FDE损失”只是重复原目标。换成MSE或任意额外头会改变训练问题，当前没有依据为其增加复杂度；本候选不加。

保留共有本车预热与联合训练。在开发诊断中，用已经生成的本车预热模型作为参照，报告加入图后ADE/FDE的变化。这是相对该实际本车模型的预测增量，不是精确条件互信息、Bayes误差，也不是邻车的因果效应；本车模型可能尚未充分训练。

恒速残差还包含位置误差、速度误差、自主转向及未观测意图，不能全部标记为“交互真值”。未来轨迹只用作监督和评价，不参与a、量子输入、mask或场景选择。逐车LLM只通过图表示获得跨车信息，仍不添加未说明的跨车注意力。

## 6. 从结构变化到ADE/FDE：需要成立的条件

本候选的项目假设是：真实数据中存在接近关系与未来运动修正的关联；加权量子读出比原聚合保留更多对预测有用的对应；训练和LLM能够利用它；修订Q在抵消噪声与拟合代价后，优于正常训练的冻结GNN，并优于旧Q。

以任一固定轨迹损失ℓ（ADE、FDE或J）定义表示最优风险B_M=inf_f Eℓ(f(Z_M),Y)，实际训练风险R_M=B_M+a_M，a_M≥0。Q/G比较为

\[
R_G-R_Q=(B_G-B_Q)+(a_G-a_Q).
\]

优化既可以争取更好的任务信息保留，也可以争取更容易训练利用的信息形式；没有必须依赖“小样本”的要求。上述等式只是拆解未知量，不是已经证明某项对Q有利。24维扩展证明的是模型类包含旧解，不是已经得到B_Q<B_G。

物理联系可用条件例说明：如果未来τ内近似恒加速度、起点与速度误差相同且其余误差可忽略，那么加速度估计误差引起δp(τ)=τ²δa/2。更准确地判断减速便可降低轨迹距离。真实数据中这些条件可能不满足，不能用该例推出全量优势。

必须保留的反例包括：接近量可能主要放大感知误差；冻结GNN可能从原始关系中已经学到相同规律；21维原读出可能已足够；新增三维可能冗余；不接近的邻车也可能影响变道；共有前端已丢失的信息无法由后端恢复；单纯MSE改善不保证ADE或FDE改善。

## 7. 后续验证与主协议合并位置

本轮只交付设计。后续实施须作为明确版本修订整体落地，不能局部混用v1.0的“45参数、21维、四维边”与本稿。

### 7.1 先验证结构与数据契约

- 检查接近/远离符号、同位置有限值、同速度零值、单位、源目标交换与车辆重排；改变未来轨迹不得改变截至t的a和输入。
- 用复用的原逐边Pauli矩计算新增聚合，与直接逐项计算交叉核对；检查相关量导数、a对状态的导数及mask。
- 对任意合法参考参数，把新增Q边权和投影列置零，核对Q与原版输出一致；这才是函数类嵌套在实现中的证据。另核对G保持原结构、输入与训练契约。
- 沿用F02原精度标准，单独核对新增路径；记录实际经典聚合和反向成本，不重做无关前端实验。

### 7.2 主比较与归因分开

主要比较为修订Q＋LLM对冻结GNN＋同规格LLM。a仅在Q内部计算；沿用原有有界开发配置和相同原始数据，不扩成电路、特征、读出、损失的笛卡尔搜索，不在看到测试输赢后换GNN。

为检验“本次修订优于v1.0”，另保留旧Q＋同规格LLM作为修订验证对照；它不替代冻结GNN主基线。没有同数据、同日程、同种子的现成旧Q证据时，需要额外训练，不能用历史旧架构结果或固定头内部替换顶替。按原3种子安排，新增旧Q需要3次完整训练，主比较6次与修订对照3次合计9次核心训练；先导和机制诊断另计。该安排是后续验证建议，本轮未执行或启动训练。

### 7.2.1 怎样补齐“真实未来相关性”证据

在训练数据中拟合表示，在独立开发确认时间块上评价，不用测试集挑读出。固定同一份已训练候选电路，从它同时提取原21维q与完整24维[q,q_app]，均附加相同本车历史和mask。分别训练同规格的小型时序预测头，一次输出真实20步未来位置修正，使用相同开发预算、种子及ADE/FDE目标；21维可补三个固定零通道，使头的名义输入尺寸和参数量相同。候选电路不得利用确认块标签进行表示训练或选择。

若24维版本在未参与训练、选择的确认块上稳定降低ADE/FDE，支持“新增读出对当前小头具有额外预测价值”。它不等于精确条件互信息、Bayes风险降低或因果机制证明。此处21维来自修订后的同一电路，不能称为原版QGNN；这一步仅分离新增读出的作用。与实际旧Q相比的整体收益由下一步独立检验。若24维只在训练集改善、确认集不改善，则不能认为已补上任务相关性证据。

### 7.2.2 怎样补齐“LLM能转化为预测收益”证据

在相同合法输入、完整训练数据、相同LLM规格与训练规则下，分别训练三个版本：冻结GNN＋LLM、旧Q＋LLM、新Q＋LLM。主比较回答新Q能否胜过冻结GNN；新Q对旧Q回答此次修改是否有效。两项都直接报告逐种子、逐SNR和全量ADE/FDE，使用相同原始时间块配对统计。两个比较、多个指标的正式显著性声明须在测试前明确主次及检验规则，不直接沿用只有一个J比较的统计结论。

判断矩阵：新Q胜旧Q但不胜G，说明修订有效但未建立相对G优势；新Q胜G但不胜旧Q，不能把胜利归功于这次修订；两者都胜且ADE/FDE均改善，才同时支持修订有效和相对基线的预测优势。小头有效而LLM无增量，说明任务信息未在完整系统中转化，不算最终目标完成。

完整模型选定后，可在开发数据上关闭新增读出或将接近权重在同一目标的有效邻车间置换，同时保留状态与原Pauli矩，观察预测依赖关系。该扰动只诊断聚合配对是否被模型利用；它可能引入分布变化，不能替代重训对照，也不证明量子独占性。不按扰动结果筛选有利测试场景。

固定头下关闭新增三维，可以检查已训练模型是否依赖它，但有分布变化，不替代独立重训的归因。首层仍有精确经典公式；这种加权聚合不消除可经典模拟性。若没有更强证据，结论停在“该参数化在该协议下有效”，不宣称量子不可替代。

### 7.3 将优化目标与最终措辞对齐

J=ADE+0.5FDE继续作为开发选择指标，避免额外引入不成熟的多目标优化器。v1.0的2%投入门槛只是是否值得继续的工程规则，不是本稿预言，也不能代替两项预测指标。

最终只有ΔADE=ADE_G−ADE_Q>0且ΔFDE=FDE_G−FDE_Q>0，才描述为“两项点估计均改善”。若要进一步写“两项均有统计支持”，必须对两项各自报告预声明的配对不确定性分析与对应联合主张检验；原来的J区间不能提供这个结论。三seed与少量时间块下仍须报告区间稳定性限制。

若J改善而其中一项变差，报告取舍，不认为满足本稿的双指标优势目标。若冻结GNN仍更强，接受相应结果；若停止于小头，仅有先导证据，不能写完整LLM预测优势。

### 7.4 合并映射

| 原文件位置 | 应同步的内容 |
|---|---|
| 方案§0、§2、§6.3—6.6 | 一个24维候选、Q内部五维边、三层48核心参数及新增初始化 |
| 方案§7.1—7.2 | GNN保持消息输入2d+4；Q token30维、图投影768×24；G token134维 |
| 方案T01—T05、T07—T11 | 原门结构结论适用性；新增权重导数与聚合界；原21维拓扑维数论证不能原文照抄为24维数值 |
| 方案§9—§11 | 保留有限开发入口；完整数据主比较；实际成本、双指标主张与训练充分性 |
| ROADMAP F02/F03/F04/F06/F07 | 增量一致性检查、成本、修订版唯一候选、依赖诊断、ADE/FDE证据 |
| 待补齐的protocol.yaml | 同时保存特征维数、ε/s_v、24维顺序、参数量、投影尺寸与版本 |

24维仍小于固定目标后七辆邻车的28维连续状态，因此不能普遍无损编码所有邻车；但实际任务只需哪些统计量仍未知。新增三维也不保证逐车最终token充分。所有原理论须按适用条件审查，不能只批量替换维数文字。

## 8. 文献依据与可写贡献

文献只用于支持设计方向和识别边界；本稿公式、权重、维数及修订选择是本项目候选设计，不表述为下列论文已经验证的结构。

- [Gilmer等，Neural Message Passing for Quantum Chemistry，ICML 2017](https://proceedings.mlr.press/v70/gilmer17a.html)：消息传递为非线性处理关系提供经典框架。保持正常非线性GNN，不把先线性平均的弱例当GNN普遍下界。
- [Schuld等，The effect of data encoding on the expressive power of variational quantum machine learning models，PRA 103, 032430，2021](https://arxiv.org/abs/2008.08605)：编码影响可访问频谱，支持研究任务与参数化的匹配；不保证本电路、真实轨迹或ADE/FDE领先。
- [Liao等，MFTraj，IJCAI 2024](https://www.ijcai.org/proceedings/2024/657)：车辆行为和动态图关系是有依据的建模对象；这同时构成强经典反例，不能把“重视车辆交互”单独作为量子优势。

本次可交付结论：一个保留旧Q函数、保持经典GNN基线、只增三维读出且不增加量子深度的具体优化候选；一项撤回重复残差监督的修正；一套通过表示探针、旧新Q对照和冻结GNN主比较补齐证据的要求。尚未交付的是实际性能提升、实测训练成本或量子独占性证明。
