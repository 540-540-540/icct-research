# Round3 feedback retraining ablation — pre-registration

Date: 2026-09-19. Starting HEAD: 68930104bf5bbd3d18e916fe6eadaaf2b274c531.
Purpose: answer the already documented minimum falsifiable question, not search a fourth architecture or reopen the failed full-training gate.

Keep the final phase_feedback computational graph and neutral initialization. Factorial arms: quantum/classical × feedback on/off, all four trained from scratch. Turning feedback off fixes core.feedback_enabled=False for the entire run; it does not change phase_mode, quantum circuits, ZZ/ZZZ, measured K2/K3 readout, scene/history controller, learned relation features, GPT-2, or any data permission. This is not the existing history mode, which would also change quantum phase parametrization. No scene-dependent switch, oracle label, or classical fallback.

All four arms use seed2026, SinD 0dB sensing history, exactly4096 train, all1880 validation, B32, 12epochs, cap16, same loss/optimizer/scheduler/order. No test or full-training run. Keep all parameters allocated; unused feedback weights have no update when feedback is off. Record initial mutable-state and common-GPT2 hashes, training indices, checkpoint source hashes, step counts and elapsed time. Two GPUs run one job each; every job has heartbeat, checkpoint, summary and a1200s budget. Existing training entry/model files remain unchanged; a separate audit trainer reuses their protocol.

Primary contrast: within each core, J_off-J_on, and both ADE/FDE at the checkpoint selected by the unchanged minimum-validation-J rule. Positive means training with feedback helped. Also report the fixed epoch12 metrics, the difference of within-core effects (quantum minus classical), and comparisons against the stronger retained R2 classical. Report all14 existing final/recent5/wedge/triangle/frozenK3 strata. No subgroup is an alternative primary endpoint; no iid-window significance claim.

Do not select a new deployment mode based on this ablation. In particular, a good off arm is not automatically a newly accepted architecture. Main remains phase_feedback+neutral; sole backup remains basis_feedback+specialized. Two structural revisions remain exhausted. The earlier full-training gate remains FAIL unless separately authorized after a new scientific decision; this diagnostic does not lower its thresholds or launch larger experiments. Stop after these four runs and evidence collection, irrespective of outcomes.

Separate engineering repair: selected-checkpoint auxiliary gradient audit had used eval-mode cuDNN GRU backward. Preserve GPT-2 evaluation/dropout settings, enable training reserve buffers only on the single-layer dropout-free GRU, restore its mode after backward, compare predictions against eval and perform no optimizer step. Retain failed logs and all earlier validation evidence. Native-CUDA alternative was rejected by a predeclared2e-4m replay guard (observed approximately0.0064m Q/0.0046m C), rather than loosening that guard.
