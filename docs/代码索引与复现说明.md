# 代码与运行说明

现有代码与数据作为本项目后续开发的基础。最新方案和 roadmap 尚在讨论，本文只说明已有入口及运行依赖。

## 代码入口

| 目录 | 主要内容 |
|---|---|
| [code/00_remote_shared_dependencies](../code/00_remote_shared_dependencies/) | 场景构建、三站回波生成、关联评估和其他共享脚本 |
| [code/01_classical_graph_llm](../code/01_classical_graph_llm/) | 经典 GNN、Graph＋LLM、预测对比与消融 |
| [code/02_pennylane_qgnn_core](../code/02_pennylane_qgnn_core/) | QGNN 核心、图模型、训练评估和机制诊断 |
| [code/03_qgnn_llm_interfaces](../code/03_qgnn_llm_interfaces/) | future-token、路由、三阶段信任、意图及长时域接口 |
| [code/04_quantum_association](../code/04_quantum_association/) | 量子关联分支的模型、训练和汇总 |
| [code/figure_tools](../code/figure_tools/) | 绘图工具及其资源 |
| [code/deepmimo_generate_data](../code/deepmimo_generate_data/) | 已有 DeepMIMO 数据生成脚本 |

原有模块文件名与内部层次保留。相同名称的模型文件属于不同实验版本，不合并覆盖。

## 数据和产物位置

| 资源 | 当前路径 |
|---|---|
| 原始轨迹 | `data/Lankershim_Vehicle_Trajectories.csv` |
| 主多目标缓存 | `data/multitarget_lankershim_v1.npz` |
| 长时域匹配缓存 | `data/multitarget_lankershim_h20_p40_matched_v1.npz` |
| 逐场景数据及划分 | `data/dataset_lankershim_clean_v1/` |
| GPT-2 配置与权重 | `models/gpt2/` |
| 后期开发结果摘要 | `results/summaries/` |
| QGNN 实验原目录 | `results/diagnostics/core_message_seed2026_v2/` |
| 双图及后期接口原实验目录 | `results/research_runs/` |

`results/` 中实验目录保留其内部代码、检查点和评估记录的关系。结果版本与适用范围见[历史实验版本索引](历史实验版本索引.md)。

## 运行条件

目标服务器目录为 `/home/dell/YrM/ICCT`。依赖记录见 [code/environment](../code/environment/)，记录的是原工程环境，不代表已在 YRM 为 ICCT 安装完成。

现有共享脚本使用同目录模块导入；早期 runner 按工作目录查找 CSV、`data/`、`results/`，GPT-2 加载逻辑检查工作目录或脚本目录的 `gpt2/`。后期 runner 还硬编码 `/home/js_cn/sensing`、`diagnostics/core_message_seed2026_v2` 和原“未来词实验”目录，并引用相应划分与检查点。

因此，当前目录已便于继续开发，但旧脚本尚未完成新运行目录的适配。本次没有修改算法、运行旧实验或安装训练环境。后续确定采用的实现后，再对实际入口统一配置项目根目录、数据、权重和输出路径。

Syncthing 同步本文件及 `code/` 中代码和环境记录。数据集另行传入服务器；模型权重与历史结果保留在本地，需要使用时再单独传入。
