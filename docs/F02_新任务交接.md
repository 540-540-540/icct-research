> 2026-09-12 A04最新状态：A04图核心适配与共同时间模型验收已通过（passed，70项）：正式量子后端为PennyLane，GNN复用师兄Plain图核心；两侧使用同规格GPT-2、LoRA和预测头，展示名仅QGNN/GNN。前端仍为A03-symbol-v4-cuda-stable-solver，Q数学仍为A02，F01-D已通过。当前汇总70项通过：量子48项、接口16项、源G图4组、主入口2项；两模型均通过真实train缓存前后向。F03未执行，正式训练由用户在服务器终端手动启动。
>
> 当前规则以版本1.4的研究方案书和ROADMAP为准。下文为交接时历史记录；当前G为2层4头128维Plain图核心、7维边和逐帧6维节点适配，接共同LLM。原A02三层4维边及“重建中”规则不再执行；共同LLM与Q30/G134接口保留。新增连接/表示宽度隔离实验已取消。
>
> 阶段清理历史保留见reports/F01D_cleanup_20260912.json。

# F02新任务交接（2026-09-12）

## 用户最新授权与工作边界

用户明确要求新开任务完成后续工作，模型GPT-5.6 Sol、High；本文件用于接续F02，不重新设计已确认的前端/模型方案。用户此前叫停的是在旧对话直接实施，随后明确要求转入新任务完成；新任务可推进F02，但不得擅自启动正式数据实验、正式模型训练或F03长基准。正式实验必须由用户在服务器终端手动输入命令、观察动态进度。提供可靠前台入口。不要持续轮询消耗token；仅在任务必要时读一次状态。用户不懂数据/模型细节，解释用自然中文、循序渐进。

重数值计算用GPU，不回退CPU估计/训练；CPU可作源数据读取、索引、写盘和轻量验证。已有两张RTX4090。F01D重跑占用GPU时先写代码和准备检查，避免抢占；小规模数学/接口验收不等于正式训练，仍须控制资源。代码任务先Ponytail full；用户AGENTS允许清晰独立任务并行委派。所有实验/代码运行在服务器，本地主要保留文档和报告。

## 权威位置与当前状态

本地项目 E:\NJUPT\ICCT会议；SSH别名 YrM_TwYhB；实际服务器根目录 /home/dell/YrM/ICCT；Python /home/dell/YrM/envs/ICCT/bin/python。历史/home/js_cn/sensing不再是本轮运行目录。当前根方案版本1.3，模型沿用A02，前端配置revision=A03-symbol-v4-cuda-stable-solver。

本次交接只读核验：reports/f01d/run_status.json为running、stage=generate、pid=1631419、started_unix=1789218054.6080492。这是用户已亲自启动的修复后重跑，禁止中断、重启或改它所使用的前端/配置文件。不要将历史F01D结果当新版本通过；重跑完成后以run_status.json、validation.json、F01D_REPORT.md为准。即便序列文件到1120，仍须等pack/normalize/validate。最终status可能complete或needs_review。尚未训练Q/G或LLM。旧任务刚委派过F02量子实现但立即被用户叫停，子代理已中断；交接时prediction目录未发现可交付文件，不假定已有F02实现。

## 已确认的研究路线

固定三基站、真实NGSIM Lankershim轨迹作为运动来源、最多8车场景、核心QGNN+LLM，与强经典GNN+相同LLM公平对照。不做基站数量对照。使用Wei等2024 IEEE TVT符号级多基站论文建感知背景（docs/references/Wei2024_symbol_level_multi_BS.pdf）。用户选择目标已分离、站间及时间对应关系已知，逐车生成信号/估计，再组成图。这不是已解决盲检测/关联，也不能宣称量子优势或实测系统厘米精度。

SNR必须沿用师兄的5、10、15、20dB，标称20。先前助手错误沿用论文-20到-5，已撤销；负档位结果仅历史。师兄旧RF包含衰落和距离幅度，不能将其简单说成处理后SNR；当前固定接收符号SNR定义=E|signal|²/E|noise|²，复噪声方差1，信号幅度10^(SNR/20)。本轮只沿用四档数值，保留已确认的论文符号模型。K256、N256、fc24GHz、B93.1MHz、T12.375us，df=B/K。K256因项目范围需约376m，论文K128仅约206m无模糊距离；K256约412m。固定二维站位和训练拟合公共原点/搜索边界，不能按车辆真值缩范围。

## F01D数据契约

原始data/Lankershim_Vehicle_Trajectories.csv；整理源data/f01_source/source_states.npy，仅仿真/标签侧，绝不作模型输入。源状态单位米/米每秒，速度用截至当前最多5帧因果拟合、至少3帧激活。episodes清单reports/f01a/episodes.jsonl：280个20秒片段，0.1..19.9秒199帧；最多8固定slot，退出不补车。train179片段/5549预测起点，V_select25/400，V_confirm22/352，test54/864。train源块21、Vselect3、Vconfirm4、test7。各锚点/重叠窗口/四SNR并非独立交通样本。已按A01固定种子、源块均衡执行6000上限，不再抽样/补齐。

data/f01d/sequences/snr_5等/episode_000.npz：state_hat[199,8,4]float32、track_exists/detected[199,8]bool、timestamp[199]float64秒。四档×280=1120份。inputs/{split}_snr_5.npz同四键，[S,20,8,4]和对应mask/time。labels/{split}.npz为future_position[S,20,8,2]、label_valid；metadata/{split}.json含episode/source_block/source_groups/source_keys/track_keys，仅路由。历史t-1.9..t，未来t+.1..t+2.0，源raw未来精确行位置，未来缺失不删起点、不改变输入。保留当前退出车辆的过去图上下文；预测对象资格为当前exists且20帧内至少3有效帧。

frontend/symbol_dataset.py提供SharedPredictionInputs(path,normalization可选)：getitem返回state_hat、standardized_state、track_exists、detected、timestamp、origin_eligible；无标签/ID可读。两图模型共用该loader。normalization.json仅训练四SNR有效缓存项同权（含窗口重复）拟合：Q尺度max(Q95(abs(state)),1)，不减速度均值；G和时间旁路(mean,std)，std下限1e-3。当前均值/标准差/Q95用CUDA双精度。数据尚重建时不能用旧normalization冒充新版本；纯数学检查可用明确合成尺度。

## GPU实现与两次修复历史

frontend/symbol_level_gpu.py完成合成、FFT、粗估、压缩、相关、融合搜索和有界GN全部CUDA；generate_symbol_gpu.py双GPU批16 FP64生成，固定每target独立CUDA seed=SHA256(ASCII 2026:episode:frame:slot:101)前8bytes little/int63，同SNR配对且不随batch拆分变化，但不与旧NumPy噪声逐样本相同。CPU生成入口已禁用。打包pack_symbol_dataset.py pack、归一化symbol_dataset normalize、验收check_symbol_dataset.py；scripts/run_f01d_gpu.py串联并显示进度。GPU报告reports/symbol_gpu/validation.json的通过字段是all_required_float64_checks_passed，不是passed，入口误读已修复。

第一次v3正式生成约1080.56秒、pack2.44秒、normalize2秒。结构验收最初因CPU float64累加标准差参考误差约1e-10失败；改用longdouble独立参考、保持1e-12门槛后通过（GPU与高精度参考差1.78e-15）。未改缓存。完整结构验收核对884458未来位置、28660历史样本，训练only/因果/共同loader通过。

但每档356171个目标状态中约11755-11763带优化失败标记，约3.3%。定位到有界GN比较两个几乎相等目标值的舍入问题。v4采用距离差有理化与稳定cost_change（2*r*dr+dr²），未放宽停止阈值/搜索步长。183个同回波训练样本，旧35失败→新0，最大状态分量差约.020000004（搜索离散化放大微小粗中心差），因此需要重建而不能只抹标记；GPU完整小规模验收再次通过。用户已经运行scripts/rebuild_f01d_gpu.py，旧缓存/报告在data/f01d_before_solver_fix、reports/f01d_before_solver_fix；不要再提让他取消重跑或擅自增量替代。详细证据reports/f01d/solver_fix/。

旧负档未完成缓存和无用快照已清理，保留原始数据、历史报告和代码备份。临时文件按用户AGENTS放.codex-work，Windows清理用指定Remove-CodexWorkTask.ps1；当前不应删除用户正在重跑的输出。

## F02交付目标与执行顺序

首先完整阅读研究方案书§6、§7、§8理论T01..T13和ROADMAP F02，核对F00资产/GPT2本地权重。历史code/02_pennylane_qgnn_core、03_qgnn_llm_interfaces仅参考，不假设符合当前A02。F02包含A-H全项，不能只写一个Q层或跑shape就称完成。新任务自行组织文件范围互斥的实现/独立校核。先N<=4，再N8；输出误差<=1e-10、梯度绝对<=1e-8，有限差分有步长收敛，近零梯度单报。重数值CUDA，complex128参考，不全密度矩阵。

A 首层精确经典式与态矢量的原始矩/连接量/共享梯度；B 编码反演、旧空通道、三车初始导数、配对公式；C 排列等变、mask、固定16比特与有效N收缩/增无关节点；D 新旧重编码/算符展开；E 多层零交互局部导数与二阶余项；F Q30/G134 token→[B*8,T,768]、单次位置嵌入、估计起点、冻结LLM输入梯度；G 读出缩放与噪声响应/Jacobian伪逆；H A02接近特征及追加三维/旧Q嵌套与梯度。最终reports/theory_validation.json分项证据，失败禁止F04。依赖新F01D的实际接口验收未完成时必须标pending，不假通过。

Q模型：每车P/V两比特，L3默认/L2有界备选；E1有pi/2中心，后续零中心；每层4增益+7边参数+5车内参数共16、L3共48；共享有界sigmoid参数，w/b零、Gamma pi/4、增益1、车内门恒等。有向Z_Pj X_Vi图门exp(-ig/2)，g=mask*Gamma*kappa*tanh(w·[r/45,u/15,a]+b)/7；a=tanh(-r·u/sqrt(||r||²+1)/15)。浅χ1在V1前，深ψL。读出顺序浅6+3、深6+2+4、浅追加a加权3，共24。边权固定除7，mask隔离，无邻车连通量0；旧Q21与四维边用于嵌套。

G模型按§7既定128维3层SiLU消息传递、4维边（不增加a），标准化本车4+两标记6输入，消息2d+4，更新2d，单节点LayerNorm。Q30/G134分别映射768；保留本车状态旁路。GPT2基础12层/12头/768，本地权重冻结，融合QKV LoRA r8 alpha16；每车独立时间序列不跨车LLM注意力；位置嵌入只一次。预测头768→256→40，输出=估计位置+h*.1*估计速度+修正。loss场景内ADE+.5FDE，未来终帧缺失不偷换末可见帧。F02检查前后向但不训练正式模型。

本轮只在新任务完成F02并交付下一阶段命令，不自动扩大到F03/F04/F05正式实验。所有状态以实时报告证据更新，说明哪些是数学检查、哪些是真实缓存接口、哪些尚未运行。


