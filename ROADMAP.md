# ROADMAP
## 当前主候选与资源决策（2026-09-14）

当前研究目标是：在保留强GNN对照和可比实验条件的前提下，研究如何扩大师兄双层QGNN在ADE、FDE上的优势，并使改善能够稳定复现。优先定位限制收益的关系表示、消息门控及后期泛化问题，再选择少量有机制依据的改动进行验证；不预设改动必然提升。QGAT继续暂停训练投入。

本节依据用户最新决定，优先于下方历史路线、候选优先级及训练启动安排。

- **主方案：师兄双层QGNN，保持不变。** 当前实现为`experiments/qgnn_inherited/graph.py`的`InheritedQGNNGraph`，接公共GPT-2/LoRA预测器；复跑协议见`reports/qgnn_inherited100/PROTOCOL.md`，配置为`configs/qgnn_inherited100.json`。这不是旧16比特A02路线的恢复，也不是师兄完整原系统的复现。
- **QGAT：暂停训练投入。** 所有现有QGAT分支保留代码、结果和检查点；不新增试跑、重训、加训或自动恢复。最新Value对照两组均已完成100轮，服务器2026-09-14 00:06北京时间核查无活动模型训练进程。
- **对照与证据：** 强GNN继续保留。QGNN最佳检查点ADE/FDE为0.547521/1.095579，较现有GNN改善0.742%/2.323%，但共同100轮排序反转；仅为seed2026、V_select开发证据，训练分批差异见复跑协议。主候选身份不等于已证明稳定优势。
- **三轮改进结果：均不替换原QGNN。** X读出较同轮原Q退步0.690%/0.777%；行驶方向关系编码较同协议原Q退步1.623%/1.766%；可学习有向上下文残差相对本轮从头训练原Q为ADE改善0.052%、FDE退步0.125%，未通过双指标开发门槛。三轮均为seed2026、V_select开发结果；见`reports/qgnn_readout/seed2026/RESULT.md`、`reports/qgnn_motionframe/seed2026/RESULT.md`、`reports/qgnn_directed_context/STATUS.md`与`reports/qgnn_cases/REPORT.md`。当前下一步待讨论，未授权新训练；不访问V_confirm/test，历史自动化继续暂停。

下方版本1.4及A06/A07内容为历史协议与当时状态，保留用于溯源；与本节冲突的当前路线及资源安排不再执行。

## 三基站ISAC：QGNN与GNN同输入研究执行计划

**版本：1.4（A04，后端与主要baseline修订）｜日期：2026-09-12｜配套：`研究方案书.md`、`configs/symbol_frontend.json`（当前前端配置）**  
**当前交接状态（2026-09-13，A07结果审查）：六个正式训练任务及六项V_confirm评估均已完成，结束于2026-09-13 03:55:02+0800，总耗时2.696小时。确认集三seed均值：QGAT ADE=0.7454015、FDE=1.5822786、J=1.5365408；GNN ADE=0.6560988、FDE=1.3720508、J=1.3421242。本批QGAT指标落后于GNN，结果不代表充分收敛。结果审查已完成，见`reports/qgat_audit/AUDIT.md`；未发现阻断性训练错误，但收敛充分性未解决，QGAT跨车诊断增益较弱，后续结构变更及加训均未启动；未授权延长训练或更换结构，无新增训练；test未读取，旧16-qubit路线继续停止，自动化保持PAUSED。**

### A07：历史正式实验协议（2026-09-13）

历史启动交付时，用户要求在服务器终端亲手启动。该批六任务及六项确认评估现已完成；当前授权仅为结果审查，不延长训练、不更换结构、不读取test。本节与[正式协议](reports/qgat_formal/PROTOCOL.md)覆盖下方A06试跑边界及A01—A05旧训练日程；历史正文仅供追溯，不恢复旧16-qubit路线。QGAT结构沿用A06，GNN沿用既有图模块，两侧连接同规格GPT-2/LoRA；seeds为2023/2024/2025，同seed共享完整可变时间模块初始权重（含图投影），在全量既有train上直接联合最多20epoch、耐心4，batch16/micro1。每起点每epoch一个SNR、四epoch平衡轮换；每epoch全V_select四档J选择best。无需旧本车预热、图适配或F04门禁。

六任务以双GPU动态队列运行，每GPU同时一个独立进程；逐步保存断点，恢复须通过来源一致性检查。六份best全部锁定后，统一在V_confirm四档作最终评价，不据此调整本批配置；当前启动入口不读test，test在best锁定后另行最终评价。结构差异包含图拓扑和容量，模型可在经典设备上高效精确收缩；不将可能差异直接归于量子优势，也不预设三seed必然得出优势。

### A06：历史候选试跑与路线变更（2026-09-13）

本节按用户最新授权覆盖下方旧路线的继续训练指令。A01—A05正文、门槛、预算和实测记录保留为历史证据；其中旧16-qubit图分支的F04/F05及归因日程不再是当前启动合同。用户中止不冒充旧方案已完成算法裁决，也不删除已产生的训练日志与检查点。旧路线不得恢复或启动F05；自动化仍为PAUSED，不因切换路线自行恢复。

新候选独立实现于`experiments/qgat_candidate/graph.py`与`experiments/qgat_candidate/run_trial.py`。本轮沿用共同感知前端、合法输入/mask和现有GPT-2/LoRA时间接口；使用128个train起点、64个V_select起点、20dB、3epochs，与同一GNN作有界试跑。两侧共享起点、监督与评价规则；V_select结果属于开发证据，不读取锁定测试。此试跑不等于旧F04 PASS，不触发正式主训练。

QGAT采用5-qubit PennyLane路线；架构参考Li等论文（DOI：[10.1088/2632-2153/ae32dd](https://doi.org/10.1088/2632-2153/ae32dd)），是针对当前接口的候选适配，不是原论文flatten读出的逐项复刻。6维节点输入经6→3角度encoder；Q/K/V各自使用独立U3参数，在节点间共享。图读出固定为8维可测、对有效索引排列不变的量：value寄存器X/Y/Z共3维、有效index均匀投影对应I/X/Y/Z共4维、成功概率p共1维；随后Linear(8,128)+LayerNorm接共同时间接口。

首轮有界试跑已完成，实测结果见[RESULTS](reports/qgat_candidate/RESULTS.md)。实际执行采用PennyLane单比特Q/K/V子线路与精确张量收缩；对完整五比特线路的等价验收通过，最大输出误差6.66e−16、梯度误差1.42e−14。该验收支持执行等价性，不构成量子硬件优势证据；小规模开发结果不等于正式优势或旧F04 PASS。完整路线变更见[ROUTE_CHANGE](reports/qgat_candidate/ROUTE_CHANGE.md)。


A03按用户选择改为目标已分离且对应已知的逐车感知；Q24数学沿用A02；G改为师兄Plain图核心适配加共同LLM，九次核心训练数量保留，训练合同依A04调整。已知对应是任务假设，不是算法已解决匿名关联。

### 使用规则

只以本文件及同版方案书为执行入口。旧版本中不同模型矩阵的九次主训练、C-S/C-P强制增强对照、将特定线性聚合碰撞外推所有GNN、以及无前置确认直接主训练的规则作废。展示名称统一GNN，但F00/F04必须保存实际公式、层数、宽度及代码hash。

A04候选：Q内部五维边、24维图读出、30维token及GPT-2；正式电路后端为PennyLane，旧PyTorch模拟器仅校核。G为方案§7.1图核心适配：6维节点MLP、固定2层4头图注意力、7维边、128维输出，接同规格GPT-2/LoRA及预测头。共同时间模型用于控制时间分支差异；图内经典特征、维数与参数化差异仍须披露，不能将全部差异归于量子门。

任务范围是产生可复核的研究证据，不承诺Q一定胜过G。**F04未通过，不启动F05六次完整主训练。**当协议的设计和优势结果分开时，设计可以先定稿；未出现的性能不得提前填入论文。

## 0. 依赖、产物与变更权限

```text
F00 资产审计
 ├─ F01 共用逐车符号感知/状态估计/数据/因果/划分
 └─ F02 数学与接口（部分可用小例先行，最终接口依赖F01）
F01 + F02 → F03 实测成本 → F04 小头选择/确认/至多一次灰区复核
F04 PASS → 封存selected_protocol + data/code/checkpoint哈希
         → F05 两模型×三种子主训练
         → F06-D 旧Q×三种子修订对照（另3次完整训练）
         → F06-E 固定电路读出探针及F06-A/B必要归因
F05 + 所需F06 → F07 锁定测试、统计、图表和论文初稿
F04 STOP/REVIEW未通过 → 归档原因和先导证据；不自动进入F05
```

任务记录字段统一为：`task_id,status,depends_on,config_hash,code_commit,input_hash,seed,device,started_at,finished_at,outputs,acceptance,notes`；status为planned/running/passed/failed/blocked/incomplete。passed必须关联真实日志。初始任务为planned；F00/F01-A/F01-B/F01-C执行状态以实际报告为准，其余未执行任务保持planned。

### 0.1 权限

允许的自动决策仅限protocol.yaml声明的开发集合、微批/精度校核、共同前端标定及一次灰区复核。改变数据集、任务、8车边界、波形数值、标签来源、主测试、增加后验或新量子结构，必须产生设计变更记录并确认未看测试。不能为获得Q优势更换弱GNN。主要GNN图核心与适配合同按方案§7.1冻结；不得换成原A02辅助GNN。

### 0.2 目录约定（建议创建，不声称已有）

```text
configs/protocol.yaml                 # 本版基准副本
configs/selected_protocol.yaml        # F04后封存结果
reports/asset_audit.md
reports/frontend_contract.md
reports/theory_validation.json
reports/cost_profile.json
reports/preflight_decision.json
reports/selection_audit.md
reports/claim_decision.md
cache/inputs/                        # loader可读
cache/labels/                        # 仅监督/评价可读
cache/manifests/
results/development/                 # V_select/V_confirm及所有失败
results/locked_test/                 # 配置封存后测试
checkpoints/own_only/, qgnn/, gnn/
theory_checks/
models/qgnn_core.py, qgnn_readout.py, gnn.py, numeric_temporal.py
frontend/scene_manifest.py, symbol_level.py, run_symbol_frontend.py, check_symbol_level.py
datasets/export_inputs.py
evaluation/known_correspondence.py, metrics.py, statistics.py
paper_assets/figures/, tables/, captions/, plotting_code/
```

## 1. F00：工程与数据资产审计

**目标：**把任务书报告的归档映射成实际可执行资产，不修改数据或先跑旧训练。

**输入：**任务书、当前两文档、用户授权可访问的实际工程；原始CSV、回波/关联代码、旧检查点及结果JSON（若存在）。路径`E:\\NJUPT\\ICCT会议`和`/home/js_cn/sensing`只是历史记录。

**操作：**定位实际操作系统/目录/仓库；记录Python、CUDA、PyTorch、量子后端、Transformers/PEFT版本，两卡是同机还是分机、各自可用显存和磁盘。核对CSV时间覆盖、单位、段ID重启与字段；确认GPT-2权重来源/hash。把旧可复用模块与待实现模块分开；提取既有GNN层公式，不仅看模型名字。读取旧失败记录、曝光状态，不重跑旧结果冒充新数据。

**输出：**`asset_audit.md,environment.json,asset_manifest.json,exposure_manifest.json,implementation_map.md`，真实入口只有在发现并核对后才加入命令。

**验收：**所有required资产有真实路径或明确missing；源CSV/hash确定；模型检查点和依赖可定位；不默认48GB合并显存；历史已知ID状态加噪缓存不能冒充感知估计；A03已知对应是显式任务假设，当前状态仍须由符号观测估计。

**失败分支：**资产缺失则对应任务blocked，先补/实现该模块，不以另一条真值加噪链替换。若调用权限不足，只报告边界，不编造可访问性。

**资源：**只读CPU/少量内存；不启动训练。**证据：**S0已有事实与研究方案书§1、§11。

## 2. F01：共用前端与数据契约

### F01-A：源时间、场景与信息权限

**依赖：**F00。**输入：**CSV和protocol的时间/场景配置。

**操作：**ft→m、毫秒→秒（先核对字段）；分段key防止ID重启冲突。按源时间60/20/20切分，验证再50/50成V_select/V_confirm；每边界±5s隔离。先切再构造20s固定队列episode，每片段至多3个起点锚点与45m内最近7邻车，不按未来存续/转弯筛选。episode不得跨60s统计块。按train→V_select→V_confirm→test、同集合内起点/锚点排序进行完整episode首次归属；与已有其他集合源组冲突则整条拒绝，被拒场景不抢占身份，不删邻车。只约束最终保留场景的源组两两不交叉，原始行跨界作为诊断。记录组归属与每次整条拒绝；不随机窗口补满6000。

使用最终保留训练队列位置行（跨锚点去重）的分位生成固定W/L、公共坐标和三站布局；中央站横向余量100m，外侧两站50m，朝向0°/180°/0°；按过去最近5帧因果拟合源速度，至少3帧才激活。当前实验是真实轨迹衍生最多8车的局部仿真；未选车辆不产生回波但其历史影响可能存在，记录局限。

**输出：**`split_manifest.json,geometry.json,episodes.jsonl,units_report.json`。

**验收：**同源锚点/SNR/噪声副本不跨集合；所有起点选择≤t；源轨迹未来标签不由预测器或响应定理生成；测试曝光标注真实。

**失败：**样本不足如实记录；几何覆盖不足在看Q/G预测前整体修订前端，不删难样本。**资源：**CPU，无LLM/量子训练。

**A01实际结果：**train/V_select/V_confirm/test保留179/25/22/54个队列场景，对应60/9/8/19个20秒时间起点、21/3/4/7个60秒块。确认集352个预测网格点并非352个独立样本。修订详情、同分母几何对比与验收见amendment_log.md及reports/f01a/；不得将几何覆盖当作A03感知估计验收。

### F01-B：A03逐车符号级观测与估计

**依据：**方案书§4及`docs/references/Wei2024_symbol_level_multi_BS.pdf`。目标预先分离且对应已知，每车独立三站虚拟通道，完成感知后组成最多8车共同输入；不执行盲CFAR、JPDA或匿名多目标分离。

**配置：**二维、A01三站几何，fc24GHz、B93.1MHz、T12.375μs、N_s256、项目K256、Δf=B/K；论文K128只作小区域参考。原始符号接收SNR为{5,10,15,20}dB，标称20dB；不沿用C06加窗输出20dB定义。位置范围由训练min/max±5m固定，最远角点376.37m小于约412m无模糊距离；两个速度分量各±40m/s。逐车固定接收SNR不代表共享资源或固定发射功率预算。

**操作：**`frontend/symbol_level.py`实现观测和估计，`frontend/generate_symbol_gpu.py`以双RTX4090执行当前批量生产，`frontend/symbol_level_gpu.py`实现GPU数值核心，`frontend/run_symbol_frontend.py`仅作CPU参考与既有源适配，`frontend/check_symbol_level.py`校核；配置`configs/symbol_frontend.json`。源速度使用过去5帧因果最小二乘，至少3帧激活；不以真实状态初始化估计器或按当前真值缩搜索区间。固定复噪声方差1、信号幅度10^(SNR_dB/20)，保存归一化和随机种子，不用状态加噪替代感知。

**输出与验收：**数值、符号、无模糊范围及SNR校核报告；流式观测/估计种子。论文参考与项目尺度分别记录，不把小区域测试当作完整区域验收。旧负档位曾通过两配置各15项校核及512次随机试验，属历史证据；当前GPU独立验收及F01-D v4全量通过；不继承旧SNR相关性能结论。

### F01-C：A03真实场景快照与因果验收

**历史A03-symbol-v1负档位结果（−20/−15/−10/−5dB，非当前成绩）：**37个真实episode、每个3个快照、四档原始符号SNR已完成，见`reports/symbol_level/real_scene_quality.json`。每档训练233个、V_select543个目标快照全部输出。V_select −5dB位置/速度RMSE为0.01181991m/0.01120123m/s；−20dB为0.03597457m/0.04513993m/s。这是逐车已知对应仿真的快照成绩，不是完整199帧缓存或Q/G预测收益。综合验收已通过：每档776个目标快照零搜索失败、零全局边界命中；两片段100帧、498个目标状态在删除5秒后源数据重放时逐项一致。见`reports/symbol_level/A03_REPORT.md`及`provenance.json`。

**当前正档位状态：**A03-symbol-v3-cuda-positive-snr验证及生产准备中，旧负档位完整生成已停止；F01-D v4已完成验收。

**检查：**训练固定位置搜索边界不随V_select改变；估计器只接符号观测及合法配置；已知对应用于路由，不成为模型数值。按对应直接计算位置/速度误差，记录全部失败，不做5m裁剪、IDSW、GOSPA或虚假确认比例验收。若保留压力条件，只在标称20dB上额外衰减功率20dB并单列。

**输出：**`reports/symbol_level/real_scene_quality.json`、`causal_checks.json`、核心验证报告及`A03_REPORT.md`。冻结源队列、训练预处理和随机流后，只扰动t后数据，≤t感知与估计不变。

**历史：**旧F01-B及C06在旧叠加/匿名前端上已执行，报告和代码保留于`reports/f01b/`、`reports/f01c/`；其20dB、覆盖/虚假门槛及自适应V_select曝光记录均为历史，不作为A03通过依据。V_select历史曝光不能撤销。

GPU生产使用`frontend/generate_symbol_gpu.py`与`frontend/symbol_level_gpu.py`，双RTX4090独立批处理；完整切窗使用`frontend/pack_symbol_dataset.py pack`，统一loader及标准化使用`frontend/symbol_dataset.py normalize`。旧CPU全量generate入口禁用；CPU只负责源读取、路由和写盘，标准化数值也使用torch CUDA。GPU独立验收PASS，见`reports/symbol_gpu/validation.json`；episode0/95整合试跑见`reports/f01d/gpu_smoke_generation.json`。全量完成情况见F01-D最终报告。

### F01-D：共同完整缓存与信息权限（v4全量已验收）

正式入口为`python scripts/run_f01d_gpu.py`，由用户在服务器项目根目录、专用ICCT环境中手动执行。入口依次运行生成→pack→normalize→validate，进度显示在前台，并写入`reports/f01d/run_status.json`与`reports/f01d/F01D_REPORT.md`。F01-D已由用户启动并完成；后续正式实验继续由用户启动。

**依赖：**A03数值、真实快照及因果验收完成。**操作：**沿用T20/H20、1s预留、训练0.5s及验证测试1s预测网格，生产完整时间历史；快照验收不能替代每episode完整生产。估计输入与真实标签物理分文件，按已知对应固定槽位，未来不重新匹配。

**产物与入口：**`data/f01d/inputs/`、`labels/`、`metadata/`、`sequences/`分别保存输入、监督、独立路由元数据与完整估计时序；`frontend/symbol_dataset.py`为统一loader。normalization只对train四SNR有效缓存状态项同权拟合，保留重叠窗口项，记录计数，不使用验证或测试数据。

**接口：**`state_hat[T,8,4]`、`track_exists/detected[T,8]`、`time[T]`保留A02维数。exists表示截至当前时刻具有合法估计历史，detected表示当帧有可用感知量测更新；历史不足3帧记输入不可用。source_ID/key只在独立元数据路由和核对，不传入forward或embedding。未来label_valid不影响输入对象或mask，不按真实估计误差筛选。

**验收：**Q/G共同input_hash、相同槽位和mask；loader无真实状态/未来mask/真实误差；按训练拟合归一化；所有未来变异检查覆盖最终输入。失败和删失保留账目。本阶段v4正SNR完整缓存已通过验收，模型训练仍遵守F02—F04门槛。

## 3. F02：数学、电路与模型接口验收

**依赖：**F00；纯数学小例可先行，最终接口需F01-D。**总体资源：**小规模参考，不训练预测模型；先N≤4，再N=8。双精度参考输出误差≤1e−10、梯度绝对误差≤1e−8，近零梯度另报绝对/相对误差；有限差分必须有步长收敛。

| 子任务 | 输入/操作 | 对应理论 | 输出与通过标准 |
|---|---|---|---|
| F02-A | 随机合法状态/门角度，同首层精确经典式与态矢量计算每个原始矩、连接量及共享梯度 | T03/T04 | first_layer_exact.json，先矩后聚合；数值一致，不用shots=None随机性解释差异 |
| F02-B | 首次编码反演；旧4恒零通道；三车全零初始导数；首层配对差2ab sin2g | T01/T03/T04/T05 | readout_checks.json，原21维及新增3维索引正确，旧空通道不再保留 |
| F02-C | 节点重排、固定16位与按N收缩、padding隔离、断开新增有效节点 | T02 | permutation_masks.json，逆重排和原节点输出/梯度一致；不改变8槽选择 |
| F02-D | 旧E₀³=−I；新零特征后续E=I；两源作用下Z算符四项展开 | T05/T06 | reencoding.json，门序/半角/Y相位一致 |
| F02-E | 多层零跨车导数与两车局部插入，强度a的小扰动 | T05/T11 | tangent.json，误差扣一阶后呈二阶或更高阶收敛（数值底噪外） |
| F02-F | Q30/G134维token及共同LLM输入梯度；G源图核心/边函数一致性、6维节点适配、历史20/未来20、动态mask/排列/因果与真实缓存 | 第7节 | interface.json及A04增量报告；Q/G共同LLM位置嵌入单次，未来mask不入forward |
| F02-G | 人工缩放读出同时检查表示差与噪声响应；局部Jacobian伪逆秩 | T07/T09 | noise_direction_check.json，只作数学一致性，不声称真实鲁棒性 |

**A02增量验收（F02-H）：**采用c=−rᵀu/√(‖r‖²+ε²)、a=tanh(c/s_v)，ε=1m、s_v=15m/s，Q边为[r/45m,u/(15m/s),a]。检查符号、同位置有限值、同速度零值、单位、源目标交换、车辆重排和未来变异。新增三项按浅层连接量C(Z_Pj,Y_Vi)、C(Z_Pj,Z_Vi)、C(X_Pj,Z_Vi)顺序作Σ_j m_i m_j a_ji C/7，附于原21维后；复用原逐边矩交叉核对，检查无邻车零值、[-1,1]界、mask及a和连接量两条梯度。新增边权第五分量及投影新增三列置零，复现同深度A01旧Q；核查图启动后新增路径梯度，不要求第一步全非零。原A02四维边/2d+4消息检查仅保留为历史辅助；当前主GNN须另通过图核心一致性及动态mask验收。沿用原精度标准，不重做不受影响的前端生产；本项历史A02已通过；A04受影响路径需增量回归。

**总输出：**`reports/theory_validation.json`，按子任务保存输入、dtype、公式/代码版本和结果。**失败分支：**修正实现或明确理论假设变化；任一核心子任务失败均禁止F04。已有理论是纸面结果，不能把本版“planned”预先标passed。

## 4. F03：实际成本与预算封存

**依赖：**F01/F02。**输入：**实际N分布、Q默认L3/T20/24维的正式PennyLane后端、GNN图核心适配加共同LLM、Q的GPT-2/LoRA与先导小头及两卡环境。

**操作：**A04必须重新实测PennyLane与当前GNN共同时间接口，不复用旧后端/共同LLM估计；预热后100—200训练步，分开电路前向、读出、反向、LLM输入反向、优化器、数据加载；各记录微批/累积批、精度、重计算方式、GPU峰值。单步过慢可减少测量步数但记录理由，不编造稳定吞吐。测每CPI和episode流式时间、储存增速，确认4TB预算。

**输出：**`cost_profile.json,budget_revision.md`，包含total_front/checks/dev/confirm/review/warmup/main/attr/eval/paper，不仅6次主训练。

**验收：**每卡≤24GB并留运行裕量；核对有效批量16；资源计划≤实际剩余容量70%；F04≤其中20%；六次主训练、共享前端与统计有独立预算，保留灰区一次复核成本。未测项写null。F06-D三次旧Q完整训练、F06-E四次小头及可能的一次表示准备均单列预算。新增读出不增加Pauli种类、量子门或整图态调用，但经典乘加、输入梯度和后端内存须实测；默认Q核心45→48、投影新增2304权重，合计新增2307参数；两层30→32，合计新增2306。

**失败分支：**先批量矩读取/等价首层优化/重计算/微批；后使用既定L2，不增量子复杂度。预算仍无法覆盖则blocked，不删GNN或种子假装可行。架构不变的等价优化需要F02回归。**证据：**方案书§11算术与本任务实测记录。

## 5. F04：低成本任务匹配、确认和停止门

**依赖：**F00—F03全部通过。**目的：**将“可能有优势”的数据条件放在昂贵主训练之前检验，既不以纯公式假装已证实，也不靠无界调参延后裁决。


### 前置门槛G1：选择、确认与一次复核

**当前状态。**A04模型的A05等价执行提速与F03成本封存已完成，见reports/cost_profile.json及configs/training_budget.json。有效批量16、微批1，完整训练轮数沿用以下合同。F04尚未产生完整任务收益裁决，不记PASS。

**数据和先导模型。**选择使用按训练时间块均衡抽取的最多512个真实起点，标称20dB训练，V_select四档评估。Q/G均采用同规格2层因果Transformer小头（hidden128、4头、FFN256、dropout0，独立图投影，最后hidden→128→40），保留相同本车状态与标记旁路。先导时间模型与正式GPT-2不同，不能当完整主结果。两侧共用训练manifest、合法mask、SNR日程、损失和评价分母。

**有界选择。**开发seed=2025。Q仅A=(3图层,图lr3e−4)、B=(2图层,图lr3e−4)、C=(3图层,图lr1e−4)；G固定源2层、hidden128，仅学习率3e−4或1e−4两项，不复制第三项、不搜索结构。每项最多8epoch、耐心3，仅V_select选择和早停。相对J差≤0.5%取实测每步成本低者，再取参数少者，仍相同按固定配置顺序。F03若显示预算或训练充分性不成立，先记录共同停止规则修订再封存，不临场加候选。

**独立种子确认。**固定所选配置，seed={2023,2024}分别从头训练；Q/G均为各自图分支加同规格小头。两侧用同一最多2048训练起点manifest、同一四档SNR平衡日程、最多12epoch/耐心3，只用V_select早停；每真实起点每epoch仅一档条件。起点不足则用全部并报告。V_confirm只在模型固定后评价，不再选择结构或学习率。

定义r_s=(J_G,s−J_Q,s)/J_G,s；分母近零时记录退化比较。宏平均按共同场景/条件权重重算，不平均不同分母百分数。

| 状态 | 确认条件 | 后续动作 |
|---|---|---|
| PASS | 平均J相对改善≥2%，两确认种子ΔJ均>0，合并ADE/FDE各不恶化>0.5%，数据/数值/预算门通过 | 封存后允许F05六次主训练 |
| STOP | 两种子Q均更差且平均J恶化≥2%，或数据/梯度/成本门失败 | 归档原因，不启动F05 |
| REVIEW | 其余完整有效结果 | 仅允许一次预先封存的短复核 |
| INCOMPLETE | 日程未封存、缺运行或检查失败未解决 | 不判PASS |

**唯一灰区复核。**固定图配置与学习率，Q/G均使用同规格GPT-2/LoRA，seed={2023,2024}。每seed在共同最多2048训练起点上作本车预热最多2epoch，再从该共同本车检查点分别初始化图分支并联合3epoch；不复用小头权重、不新增图适配阶段。两侧同SNR日程、损失与评价规则，仅V_select早停，V_confirm作一次最终判定。采用同一PASS条件，未通过则暂停本周期完整训练；没有第二轮结构搜索。F03须先确认该对称复核预算可承担，不把未运行的门槛标为PASS。

2%、0.5%为工程投入规则，不是显著性门槛或预测收益。先导小头与正式Q时间模型不同，结果只用于有界投入决策，全部探索成本进入总账。

### F04执行产物与适用限制

每次保存`trial_id,graph_formula,depth,width,readout,lr,seed,training_origins,validation_split,stop_epoch,J,ADE,FDE,cost,checkpoint`。所有试验（含失败）进入`dev_trials.jsonl`，最终`preflight_decision.json`严格枚举PASS/STOP/REVIEW/INCOMPLETE及触发条件。

只有PASS才输出`selected_protocol.yaml`，封存Q/G公式、图层数和学习率、dtype、waveform/frontend/data/split/code/checkpoint哈希。显示名仅GNN，不掩盖真实方法。若旧代码与默认式不同，F04开始前记录实现合同；对照选择不能依Q输赢事后更换。

可在训练/开发独立片段上补充T09局部Jacobian、配对信号、关图输出差及本车小头参照，解释为何通过/不通过；这些诊断不再组成额外训练模型族。分类探针不估计精确TV，不把大特征幅度或纠缠量当PASS标准。确认数据经历灰区复核后明示已参与开发，不重置其曝光记录。

**失败分类：**A=前端/单位/标签；B=图信息相对本车几乎无增量；C=读出压缩或尺度；D=优化/梯度；E=资源。修复A/D中的真实错误需共同重跑受影响配置并计入实际试验历史，不能将调参伪装为bug修复。非bug的STOP不再增加深度、后验、理论标签或更弱对照。

## 6. F05：六次主比较完整训练

**依赖：**F04 PASS、A04日程及selected_protocol封存。**输入：**100%合法训练数据，QGNN和GNN，seeds2026/2027/2028；只用V_select选检查点，测试不可读。

**操作：**每seed从头产生同一份最多3epoch本车预热；Q/G复用该seed本车投影、LoRA和头，分别初始化图及非零W_g。图适配2epoch（冻结其余参数但保留LLM输入反向），再联合最多15epoch、耐心4。Q图深/学习率与G学习率来自F04，G图深固定2；两侧LLM规格、损失、mask、起点及标签契约相同。Q24/G128各自图投影至768，不以压缩G表示制造对手不足。F03封存微批和可承担预算，未完成依赖不启动F05。

**训练充分性：**使用全部合法训练数据；少样本或相同墙钟优势不能替代主预测优势。若任一模型到上限仍有明显V_select改善趋势，在测试解封前共同修订停止规则并给予两侧同规则延长，或明确仅为当前日程结果且训练充分性未解决；不得截短G或挑弱检查点。F06-D沿用同规则。

**主epoch计数：**每个真实运动起点每epoch只使用一个SNR条件，按固定序列平衡轮换，四epoch覆盖全部条件；Q/G同seed序列相同。验证与测试每次评四档。全批无监督时只记录不更新，不改采容易SNR。

**输出：**6个完整验证选定checkpoint、每epoch日志、优化器/模型状态、可训练参数表、代码/配置hash、每seed成本。共同预热成本在总账与单模型账中同时说明；Q/G各自图适配及联合训练成本分别实记。

**验收：**6次完整独立记录；无NaN/漏样本/测试驱动选择；same input_hash/split/hash；参数冻结和学习率符合所选版本；不将旧单种子成绩混表。三seed只是最低起点，不宣称充分覆盖训练不确定性。

**失败分支：**运行错误先修复并标明重跑；有效但验证不占优也继续记录，不在主测试上搜索新电路。资源不足声明未完成，不补造种子或择优只报一个。F04 PASS不是F05必胜保证。

## 7. F06：必要归因，不无限扩展实验矩阵

**依赖：**F05选定检查点；机制选择仅训练/开发，最终固定诊断可在测试作一次报告。

### F06-A：Q图路径与非线性作用（无需新主训练）

输入为已训练Q、固定共同时间头和合法历史；计算q(1)、q(0)、q^[1]及最终预测差。关闭各层跨车门、检查浅/深投影幅值和输入梯度；需要时对少量实际邻车做混合响应。输出`mechanism_audit.json`，包括真实ADE/FDE变化和表示变化。

通过/解释：图关闭后不退化，不能主张交互是主贡献；余项大但误差更差，不能宣称非线性更有用。固定头替换有分布漂移，不等于独立重训GNN；不得以内部消融单独证明量子优势。不最大化纠缠或余项来强迫看到“作用”。

### F06-B：时间模块增量（主张LLM作用时必需）

仅在主张LLM增量且预算封存时，复用F05两个GPT-2模型组；追加Q/G各3seed时间头对照：从头训练4层Transformer、hidden128、4头、FFN512、dropout0，相同输入、目标及开发检查点规则，共6次训练，单列预算。

输出时间模块指标与成本；比较整体时间模块，不单独分离语言预训练、容量和微调方式。缺此证据只能报告LLM接口可行，不能写稳定LLM增量。不自动给旧Q再追加该组。

### F06-C：可选有限数据与信息探针

只有核心任务和时间对照预算充足时才做50%时间块Q/G×3seed，或冻结表示后用小型读出比较本车+Q、本车+G、本车+二者。前者未执行则不写样本效率已证；后者不是Bayes风险精确测量，也不加入主架构。100%仍是主表，不以少数据胜利替代全数据。

### F06-D：旧Q对照，验证A02整体修订（追加三次完整训练）

旧Q指A01同类ZX电路的四维边、21维读出、默认三层45核心参数，不是历史六比特架构或旧数据成绩。seed={2026,2027,2028}，当前共同前端和100%合法训练集、同规格LLM和F05日程；匹配新Q在F04选定的图深度、图学习率，不另开旧Q调参。从头训练，复用该seed共同本车预热，图及投影重新初始化；两层旧Q核心30个。关闭新Q通道后的固定头输出不替代独立旧Q训练。

输出三份验证选定checkpoint、逐seed/SNR ADE/FDE/J和成本，F07作新Q对旧Q配对评价。F05新Q/G×3=6次，加本项3次，合计9次核心完整训练，先导和其他归因另计。缺本项可以报告主比较，但必须说明缺少此次修订有效的直接证据。此对照不能分离边特征与读出贡献，也不覆盖所有旧Q最优配置。

### F06-E：同候选电路21/24维小头探针（四次小头训练）

检查新增读出对真实未来的预测价值，不新增F04门槛。F04选择完成后，以选定结构/lr准备同一份候选电路：仅train拟合、V_select早停选checkpoint，不用V_confirm标签训练或选择表示及种子。可复用满足条件且来源锁定的合法checkpoint；否则以seed2025、完整train、F04小头、最多12epoch/耐心3重新准备，额外成本单列。V_confirm已参与F04裁决时明确开发曝光，不称全新独立盲证据。

冻结该电路，同时提取原21维q与完整24维[q,q_app]。21维补三个固定零，均附相同本车历史和mask；相同128宽F04小头和名义参数量，seed={2023,2024}×{21补零,24}=4次，完整train、同噪声日程、最多12epoch/耐心3，V_select早停，V_confirm仅固定后确认ADE/FDE。保存表示来源、manifest、checkpoint、逐seed指标和成本；不得按确认结果换图、头或超参。

24维改善支持当前小头的额外预测价值，不等于条件互信息、Bayes风险或因果证明；这里21维来自修订电路，不是旧Q。小头改善但完整LLM不改善，不算最终目标完成。可关闭新增通道或置换同目标有效邻车接近权重作预定诊断，但分布变化使其不能替代重训。F06-D/E均不得触发新调参。

**资源优先顺序：**F05、F06-D、F07、F06-E/A；主张LLM增量时F06-B必需，F06-C最后可选。缺对应证据则收窄主张。后验/三车读出/新架构不在本版预算。

## 8. F07：锁定测试、可信统计与论文证据包

**依赖：**F05全部；主张修订有效加F06-D，主张LLM增量加F06-B。**输入：**封存checkpoint、同一A03共同缓存、标签侧车、曝光manifest、方案书§10固定评价器。

**操作：**

1. 核对Q/G前端hash、已知对应的槽位路由、输入有效mask、label_valid和可评分分母完全一致；对应不按未来预测重新分配。
2. 表A仅一套前端指标；表B主表为新Q/G两模型×三seed逐SNR及宏平均ADE/FDE/J，修订附表加入旧Q三seed；表C输入/监督可用性、删失和分母。NaN或无输出不被静默过滤。
3. 主ΔJ=J_G−J_Q保留主要95%区间；60s原始块×seed双维配对bootstrap2000次，统计seed9100。保存块内错误和/计数并重算宏平均；附30/120s敏感性、小块数限制和逐块逐seed原始差。
4. 报告预设2%工程点估计门槛和0.5%单项保护线，与统计区间分开。区间跨0不叫等效；J好但某单项差必须写取舍。双指标确认固定四项家族：ADE_G−ADE_新Q、FDE_G−FDE_新Q、ADE_旧Q−ADE_新Q、FDE_旧Q−FDE_新Q，正向为新Q更好。沿用配对bootstrap，各取98.75%单侧下界（1.25分位），对应Bonferroni家族α=0.05目标控制，不声称小样本bootstrap精确覆盖。相应两项下界均>0才支持该比较双指标统计优势，四项均>0才同时支持相对G及修订双指标优势；缺项不重分配家族名额。两项点估计>0仅称点估计均改善，不用J区间替代。
5. 图表F01—F06、表A—C保留原数据/脚本/配置；机制小例和oracle明确独立，不能混为真实主结果。维护claims_to_evidence.md。
6. 完成方法/理论/结果/局限正文，逐项对照实际运行版本；会议届次/模板/官方投稿日期另行核验，不猜。

**输出：**`main_results.csv,per_seed.csv,paired_errors.parquet,availability.csv,bootstrap_report.json,costs.csv,claim_decision.md,paper_assets/,reproduction_index.md,论文初稿`。

**验收：**每个优势句子追溯到真实主比较或明确条件命题；已曝光源区间不称全新外部盲测；无结果图不造数。支持与负面证据同样保留。模型和协议哈希与两份文档一致，差异进入amendment_log.md。

**失败分支：**输入/评价不同则结果无效；有效但不支持H1时直接写未建立优势。不能更换GNN、标签、测试集、指标或删难场景挽救。旧测试一旦用于调整，永久标为开发历史。

## 9. 任务—理论—图表映射与状态表

| ID | 主要依据 | 产物核心 | 当前状态 |
|---|---|---|---|
| F00 | S0边界/资产；方案§1/11 | 真实资产及实现映射 | 审计已执行；ICCT环境依赖及硬件/模型/梯度冒烟检查通过 |
| F01-A | 方案§4/5、A01修订 | 源场景与几何准备 | 已执行并通过本阶段检查 |
| F01-B | 方案§4、A03 | 逐车符号级观测和核心校核 | GPU独立验收及v4全量通过；负档位记录仅历史 |
| F01-C | T12；方案§4 | 真实快照及因果验收 | GPU独立验收及v4全量完成；不继承负档位性能结论 |
| F01-D | T12；方案§5 | 共同完整估计缓存和标签权限 | v4完整缓存验收通过；四档搜索失败均为0 |
| F02-A…H | T01—T07/T09/T11 | 数学/数值与接口一致性 | 历史A02 passed 64项；A04图核心适配与共同时间接口passed，共70项 |
| F03 | 方案§11成本式 | 实测预算与资源门 | planned |
| F04 | H1、T08—T10条件；G1 | 前置选择/确认及PASS/STOP | planned |
| F05 | H1，统一训练 | 两模型×三seed | planned，依赖F04 PASS |
| F06-A/B | T11、LLM接口 | 机制/时间模块作用 | planned |
| F06-C | 有限样本/表示解释 | 可选曲线/探针 | planned_optional |
| F06-D | A02整体修订 | 旧Q三seed完整对照 | planned |
| F06-E | 新增读出任务信息 | 同电路21/24小头四次确认 | planned |
| F07 | T10/T12，第10节协议 | 真实统计和论文 | planned |

## 10. 排程、预算和冻结含义

9月12—14日完成资产/前端与并行数学检查；15—17日完成成本、前置裁决和配置封存；18—21日安排核心训练与必要归因；22—25日汇总统计和初稿。这是工作窗口，不是无实测的耗时承诺。任一依赖延误必须重算预算，不顺延出一个未请求的默认十月缓冲。

预算分开：前端、数学、F04最多5个开发trial＋4个确认训练＋灰区最多4个短LLM复核、每seed共同本车预热、6次主训练＋F06-D三次旧Q完整训练（9次核心）、F06-E四次小头及可能一次表示准备、主张LLM增量时6次Q/G小Transformer归因、可选50%曲线、评价与图表。根据F03实测步数/耗时计算，总额≤可用GPU小时70%，F04≤计划额20%。不填未经实测的GPU小时，也不使用“只需六次训练”掩盖开发总成本。

**两份文档的工作已经从讨论整理成协议。仍未冻结的是实验结果，不是执行者可以任意发挥的架构。** F04的有限选择完成后只回填事先声明字段；结构之外的新设计要明确更版。本文没有把当前QGNN实际净优势视为通过，也没有将缺少优势证据的决定无限延期为继续增加模块。

**A02变更摘要：**Q内部接近编码、24维保留式读出及相应数学/接口/验证统计；不追加重复残差损失，不增加比特或默认深度，不加强GNN。保留最新F00/F01执行事实；该条为历史设计摘要；A04当前状态见页首，训练仍planned。



### 最新任务决定（2026-09-12）
用户取消新增连接与24/128/256/512/768表示宽度隔离实验；审阅稿仅保留历史分析。PennyLane同电路交叉验证已完成；A04正式实现与GNN增量验收后重新执行F03成本核验，不将数学一致性当作预测优势。

上述PennyLane校核已完成，23组404项通过（含8车3层全有效）；见reports/f02/pennylane_crosscheck.json。原F02 A–H64项通过状态保留，F03仍待执行。
