# 研究方案书
## 当前主候选与资源决策（2026-09-14）

当前研究目标是：在保留强GNN对照和可比实验条件的前提下，研究如何扩大师兄双层QGNN在ADE、FDE上的优势，并使改善能够稳定复现。优先定位限制收益的关系表示、消息门控及后期泛化问题，再选择少量有机制依据的改动进行验证；不预设改动必然提升。QGAT继续暂停训练投入。

本节依据用户最新决定，优先于下方历史路线、候选优先级及训练启动安排。

- **主方案：师兄双层QGNN，保持不变。** 当前实现为`experiments/qgnn_inherited/graph.py`的`InheritedQGNNGraph`，接公共GPT-2/LoRA预测器；复跑协议见`reports/qgnn_inherited100/PROTOCOL.md`，配置为`configs/qgnn_inherited100.json`。这不是旧16比特A02路线的恢复，也不是师兄完整原系统的复现。
- **QGAT：暂停训练投入。** 所有现有QGAT分支保留代码、结果和检查点；不新增试跑、重训、加训或自动恢复。最新Value对照两组均已完成100轮，服务器2026-09-14 00:06北京时间核查无活动模型训练进程。
- **对照与证据：** 强GNN继续保留。QGNN最佳检查点ADE/FDE为0.547521/1.095579，较现有GNN改善0.742%/2.323%，但共同100轮排序反转；仅为seed2026、V_select开发证据，训练分批差异见复跑协议。主候选身份不等于已证明稳定优势。
- **三轮改进结果：均不替换原QGNN。** X读出较同轮原Q退步0.690%/0.777%；行驶方向关系编码较同协议原Q退步1.623%/1.766%；可学习有向上下文残差相对本轮从头训练原Q为ADE改善0.052%、FDE退步0.125%，未通过双指标开发门槛。三轮均为seed2026、V_select开发结果；见`reports/qgnn_readout/seed2026/RESULT.md`、`reports/qgnn_motionframe/seed2026/RESULT.md`、`reports/qgnn_directed_context/STATUS.md`与`reports/qgnn_cases/REPORT.md`。当前下一步待讨论，未授权新训练；不访问V_confirm/test，历史自动化继续暂停。

下方版本1.4及A06/A07内容为历史协议与当时状态，保留用于溯源；与本节冲突的当前路线及资源安排不再执行。

## 三基站ISAC车辆追踪与预测：QGNN与GNN

**版本：1.4（A04，后端与主要baseline修订）｜日期：2026-09-12｜配套：`ROADMAP.md`、`configs/symbol_frontend.json`（A03前端配置）**  
**当前交接状态（2026-09-13，A07结果审查）：六个正式训练任务及六项V_confirm评估均已完成，结束于2026-09-13 03:55:02+0800，总耗时2.696小时。确认集三seed均值：QGAT ADE=0.7454015、FDE=1.5822786、J=1.5365408；GNN ADE=0.6560988、FDE=1.3720508、J=1.3421242。本批QGAT指标落后于GNN，结果不代表充分收敛。结果审查已完成，见`reports/qgat_audit/AUDIT.md`；未发现阻断性训练错误，但收敛充分性未解决，QGAT跨车诊断增益较弱，后续结构变更及加训均未启动；未授权延长训练或更换结构，无新增训练；test未读取，旧16-qubit路线继续停止，自动化保持PAUSED。**

### A07：历史正式实验协议（2026-09-13）

历史启动交付时，用户要求在服务器终端亲手启动。该批六任务及六项确认评估现已完成；当前授权仅为结果审查，不延长训练、不更换结构、不读取test。本节与[正式协议](reports/qgat_formal/PROTOCOL.md)覆盖下方A06试跑边界及A01—A05旧训练日程；历史正文仅供追溯，不恢复旧16-qubit路线。QGAT结构沿用A06，GNN沿用既有图模块，两侧连接同规格GPT-2/LoRA；seeds为2023/2024/2025，同seed共享完整可变时间模块初始权重（含图投影），在全量既有train上直接联合最多20epoch、耐心4，batch16/micro1。每起点每epoch一个SNR、四epoch平衡轮换；每epoch全V_select四档J选择best。无需旧本车预热、图适配或F04门禁。

六任务以双GPU动态队列运行，每GPU同时一个独立进程；逐步保存断点，恢复须通过来源一致性检查。六份best全部锁定后，统一在V_confirm四档作最终评价，不据此调整本批配置；当前启动入口不读test，test在best锁定后另行最终评价。结构差异包含图拓扑和容量，模型可在经典设备上高效精确收缩；不将可能差异直接归于量子优势，也不预设三seed必然得出优势。

### A06：历史候选试跑与路线变更（2026-09-13）

本节按用户最新授权覆盖下方旧路线的继续训练指令。A01—A05正文、门槛、预算和实测记录保留为历史证据；其中旧16-qubit图分支的F04/F05及归因日程不再是当前启动合同。用户中止不冒充旧方案已完成算法裁决，也不删除已产生的训练日志与检查点。旧路线不得恢复或启动F05；自动化仍为PAUSED，不因切换路线自行恢复。

新候选独立实现于`experiments/qgat_candidate/graph.py`与`experiments/qgat_candidate/run_trial.py`。本轮沿用共同感知前端、合法输入/mask和现有GPT-2/LoRA时间接口；使用128个train起点、64个V_select起点、20dB、3epochs，与同一GNN作有界试跑。两侧共享起点、监督与评价规则；V_select结果属于开发证据，不读取锁定测试。此试跑不等于旧F04 PASS，不触发正式主训练。

QGAT采用5-qubit PennyLane路线；架构参考Li等论文（DOI：[10.1088/2632-2153/ae32dd](https://doi.org/10.1088/2632-2153/ae32dd)），是针对当前接口的候选适配，不是原论文flatten读出的逐项复刻。6维节点输入经6→3角度encoder；Q/K/V各自使用独立U3参数，在节点间共享。图读出固定为8维可测、对有效索引排列不变的量：value寄存器X/Y/Z共3维、有效index均匀投影对应I/X/Y/Z共4维、成功概率p共1维；随后Linear(8,128)+LayerNorm接共同时间接口。

首轮有界试跑已完成，实测结果见[RESULTS](reports/qgat_candidate/RESULTS.md)。实际执行采用PennyLane单比特Q/K/V子线路与精确张量收缩；对完整五比特线路的等价验收通过，最大输出误差6.66e−16、梯度误差1.42e−14。该验收支持执行等价性，不构成量子硬件优势证据；小规模开发结果不等于正式优势或旧F04 PASS。完整路线变更见[ROUTE_CHANGE](reports/qgat_candidate/ROUTE_CHANGE.md)。


> 本文冻结研究边界、默认实现、有限开发范围、理论适用条件与继续/停止规则；不冻结“QGNN已经优于GNN”的结论。当前机制具有可分析的任务表示路径，但相对GNN的实际净收益仍是H1。完整主训练须通过ROADMAP的F04前置门槛。

### A01执行修订（2026-09-12）

用户授权解决确认集不足与基站覆盖不足。本版按实际保留场景实施源身份隔离，并调整共同站位/朝向。时间比例、5秒隔离、20秒队列、8车上限和Q/G共同输入不变；原始数据及原始未来标签不变。详见[修订记录](amendment_log.md)。此次依据为源数据元信息及训练几何，未运行或查看Q/G模型测试成绩；历史曝光状态仍保留。

**历史C06执行修订（2026-09-12，已由A03替代，不再作为当前规则）：** 用户要求继续解决F01-C失败。本轮采用训练残差拟合的双高斯角度误差模型：窄分量保留常规观测精度，宽分量固定5°描述少量较大误差；按分量似然参与JPDA，每个Joseph更新分支及均值散布共同形成后验。R公开字段为两分量先验加权协方差，不能把它当作单高斯更新的实际全部模型。采用既定q_a=9，连续第7个漏检周期删除（前6个保留），2/3周期确认不变。保留期限在训练检出间断分位给出的5—8周期范围内比较，6/7在训练均通过后按覆盖选择7，再验证开发集。阈值、全部确认池口径、5m一对一绑定、8槽和源车辆未改变。V_select经过自适应开发，不能称盲测；V_confirm/test未使用。所有失败候选、选择顺序、因果与数值证据见reports/f01c/F01C_REPORT.md。协方差一致性仍有偏乐观迹象，工程通过不等于后验概率完全校准。

### A03执行修订（2026-09-12）

用户明确选择以Wei等2024符号级多基站感知论文为主：逐车独立感知，假设目标已分离且对应已知，再组成最多8车的共同预测输入。撤销当前主线的盲CFAR、匿名关联、JPDA及5m评价绑定；旧C06证据保留为历史。A03当时只改上游感知、评价及接口语义；当前模型与训练规则已由A04更新。

### 阅读顺序与权威性

**A02历史模型设计修订。**用户已授权将本次优化合并至两份权威文档：Q内部追加接近关系，保留原21维并增至24维；经典GNN沿用冻结实现，不增加派生特征或专门模块。F05的新Q/G六次主训练之外，F06-D增加旧Q三次同条件重训，F06-E检查新增读出的真实未来预测价值。A01前端及最新F01-B执行记录完整继承。A02只完成设计合并，未实施电路或训练；不得将设计结论标成实验通过。A02当时的合并记录为历史；当前以A04更新后的本文与同版ROADMAP为执行依据，优化修订稿只保留为来源，不另设并行规则。合并前快照见reports/amendment_A02_before/，合并交付快照见reports/amendment_A02_final/。

先读第0—3节了解裁决和默认配置，第6—9节查看模型、推导和训练，第10—12节查看评价、成本与证据交付。第4—5节给出共同前端和数据契约。只需本文及ROADMAP即可理解当前方案，不需要逐份拼接旧版本。

用户任务书及后续明确变更是边界来源；本版是当前实施规则。原任务书中的“目标数可评估”被用户后续“最多8车固定”覆盖；旧版的特制增强经典对照要求被用户后续普通GNN决定覆盖。**展示名称仅QGNN/GNN；实际后端和源模型合同在方法中说明。** 新波形、阈值、训练参数是研究设计决定，不表述为用户已逐项确认的事实或实测最优值。

---

## 0. 冻结前审查裁决

### 0.1 对QGNN的结论

**保留有向联合ZX电路作为唯一量子研究候选，有条件进入验证；不宣称它是全部QGNN中优势最大的架构，不批准跳过前置筛选直接运行全部主训练。**

保留的依据是：它对有符号位置—速度关系有明确编码；跨车计算实际发生于电路中；浅层读出有可解析信号；后续混合/重编码可产生超出初始逐边线性化的函数；最多8车时有明确的模拟成本表达。与其他尚未细化的量子结构相比，这些是可审查的设计依据，不是实绩排序证据。

其主要风险也明确：普通GNN能非线性地处理相同原始关系；首层存在精确经典计算；24维有限读出可能丢失任务信息；弱交互附近整体近似逐边消息；感知误差和学习过程可能抵消表示收益。不能用“量子空间大”“纠缠复杂”“只有48个核心参数”跨越这些缺口。新增接近特征及加权聚合属于Q内部经典计算，主比较检验完整混合方案的效果，不单独证明量子门不可替代。[S0][S3][S4]

### 0.2 四项工作关闭到什么程度

| 项目 | 本版处理 | 当前证据状态 |
|---|---|---|
| 机制裁决 | 主张收敛为同输入下的可靠任务关系表示与净预测收益H1 | 结构与条件性推导完成；条件是否在真实数据成立未获证实 |
| 配置 | 第2节给出唯一默认、3个有界开发配置/主要模型；完整protocol.yaml待落实 | 设计决定，不是实测最优 |
| 前置门槛 | F04明确小头、数据、预算、通过/复核/停止 | 尚未执行；通过前不启动6次完整主训练 |
| 文档整合 | 两主模型、同LLM、旧Q修订对照及读出探针；任务/公式/图表映射统一 | 既有前端执行状态保留；A02模型及训练配置待落实 |

**“实际优势已经证明”未通过验收。**本文保留详细正面构造和负面限制，不用协议定稿替代优势证明。若用户要求交付前已经拥有该数据上的实际优势证据，当前文件不满足这一更强要求。

### 0.3 本版相对旧稿的显式变更

1. 展示名称统一为GNN，撤回把某一种线性先聚合结构的碰撞当作所有GNN下界的论证。
2. 主比较为QGNN和GNN两个完整系统，共6次主训练；两侧使用同规格LLM与预测头，仅图分支按各自合同实现。旧Q三次修订验证保留，共9次核心训练。不研发特制增强GNN，旧Q不作为新的经典对照。
3. 3个量子块为默认，2块为有界开发备选；1块仅作机制校核。主Q固定24维读出、内部五维边；旧Q保留21维、四维边作为修订对照，不开放28/32维搜索。
4. 新增可执行的小头前置门槛：独立开发选择与确认、明确2%工程门槛、一次受限LLM复核、未通过即暂停当前主线完整训练。
5. 保留新Q对G的J主要95%区间；增加新Q对G及新Q对旧Q的ADE/FDE四项预声明确认性差异，检验族及区间规则见§10.5。J改善不替代两单项同时改善。
6. 补齐未匹配分配成本、同源开发确认切分、运行失败处理与可训练参数/输入梯度要求。
7. 理论附录的旧构造不全部转为主实验。代数等价、内部替换、小头结果、完整测试分别记录，不互相冒充。

---

## 1. 研究边界、已有事实与问题定位

### 1.1 固定边界

| 项目 | 内容 |
|---|---|
| 数据 | NGSIM Lankershim；真实轨迹衍生的受控感知仿真，不是实测雷达数据 |
| 站数与车辆上限 | 3个基站；每个建模场景最多8辆真实车辆，不要求恰好8辆 |
| 固定数据流 | 真实轨迹→已分离且对应已知的逐车三站符号级观测→逐车状态估计→最多8车共同历史→预测 |
| 主线 | QGNN负责核心跨车关系计算；LLM负责本车及交互历史的时间建模 |
| 主要比较 | QGNN 对 GNN；GNN采用师兄Plain图核心适配并接共同LLM，见§7.1 |
| 算力 | 2×24GB RTX 4090、512GB RAM、4TB硬盘；不假设两卡显存自动合并 |
| 时间目标 | 2026-09-25前后完成必要实验和论文初稿；不是已经核验的会议投稿截止 |
| 本文工作范围 | 理论、文献、协议与文档；不执行正式实验、不伪造实测预算 |

边界来自任务书第2节及用户后续决定。[S0] 关联、融合和航迹维护必须存在，但不要求每个环节都成为创新。Q不反馈追踪器，故追踪指标只报告一套。

### 1.2 已有结果必须正确继承

任务书记录：旧缓存以真值ID整理并对状态加噪；另有匿名量测和关联代码，但与旧最新Q模型的贯通未确认。速度可能用到后一帧位置，是待核验的因果风险，不是已量化泄漏。[S0 §4.1—4.4]

旧单种子2026、991场景开发结果，四档SNR宏平均（m）：

| 旧模型原名称 | ADE | FDE |
|---|---:|---:|
| Plain GNN | 0.685107 | 1.328154 |
| 纯QGNN | 0.675118 | 1.303763 |
| future-token QGNN＋LLM | 0.670060 | 1.296719 |
| 有效关系三阶段QGNN＋LLM | 0.669766 | 1.296094 |
| 对应经典GNN＋LLM | 0.667706 | 1.296894 |

量子相对匹配经典的ADE改善为−0.309%、FDE为+0.062%，两项配对区间跨0；不是新方案结果。旧均匀路由略好于学习路由、其他复杂接口收益有限，均保留为反面研究线索。旧测试曾被查看，不能把重新命名/加噪叫全新盲测。[S0 §4.3]

### 1.3 唯一主假设H1

**在相同三站估计历史、同规格时间模型与预测头、训练划分、监督与评价规则和有界开发预算下，QGNN图分支能否相对GNN图分支降低全量ADE/FDE。**共同LLM控制时间模型差异；图分支仍有特征处理、维数和参数化差异，结果不能单独证明量子门不可替代。

任务动机是不同位置上的车辆具有不同速度、接近/分离趋势及历史变化；邻车状态的配对比孤立均值更有意义。近期动态图车辆预测工作支持这些信息值得建模[R07]，但没有证据认定它就是本数据的主导瓶颈。常规GNN也能够编码这些关系。H1涉及表示、有限学习和噪声的净取舍，不是宣称量子独有抗噪性、普适表达力分离或硬件加速。

“有限训练”是学习理论对现实过程的描述，不是优先限制GNN的规则。主比较使用100%合法训练数据和合理训练；不得靠少样本、过早停止GNN或删难场景制造优势。A02另设修订假设H2：在同一当前数据和训练规则下，新Q的ADE/FDE均优于旧Q；H1成立不自动证明H2。这里旧Q专指A01的ZX四维边、21维读出版本，不是§1.2的历史六比特模型。

---

## 2. 唯一默认配置与允许变化的边界

| 字段 | 本版默认 | 允许变化与决定方式 |
|---|---|---|
| 历史/预测/间隔 | T=20、H=20、0.1s；历史跨度1.9s、未来2s | 本周期不变 |
| 量子寄存器 | 每车P/V两比特；固定最多16比特 | 本周期不变 |
| 量子块 | 3 | F04仅允许2作为成本/开发备选 |
| 量子编码 | 首次π/2中心，后续零中心；增益[0.25,4]、初始1 | 增益正常训练；不额外扩大范围 |
| 边范围/尺度 | 同一8车图，平滑距离权45m，相对速度尺度15m/s | 不按Q测试分数修改 |
| Q内部接近特征 | ε=1m，速度尺度15m/s；Q边维数5，G边维数7 | 仅由共同估计状态计算，不调成量子有利测试值 |
| 读出 | 原浅层9＋最终12＋浅层加权3＝24 | 固定顺序；旧Q对照21维，不开放额外搜索 |
| GNN | §7.1 Plain图核心适配，2层4头、hidden128，接共同LLM | 图深固定2，仅F04两档学习率 |
| 时间主模型 | GPT-2基础规格，12层、12头、hidden768 | 不替换更大LLM |
| 微调 | 主体冻结，融合QKV投影LoRA r8、α16、dropout0 | 模块名/形状由F00核验；不擅自改单独Q/K训练 |
| 时间预测头 | 768→256→40，SiLU；一次输出20个二维偏移 | 固定 |
| 基线状态 | 估计位置＋估计速度恒速外推 | 训练/测试都不得换成真值起点 |
| 主损失 | J=ADE_macro＋0.5FDE_macro | 固定，单项/四SNR均报告 |
| 额外组件 | 后验采样、三车累积量、纠缠奖励关闭 | 开启即新协议，不占本版主线 |
| 主训练种子 | 2026、2027、2028 | 新Q/G共6次＋旧Q修订对照3次＝9次核心；探针与时间归因另计 |
| 主训练日程 | Q/G共同3epoch本车预热、2epoch图适配、最多15epoch联合、耐心4 | 实际微批及预算由F03封存 |
| 优化默认 | 图3e−4，适配器/头1e−4，LoRA3e−5；裁剪1；有效16场景/批 | F04有限配置；微批由F03成本决定 |
| 主比较阈值 | J相对改善≥2%，ADE/FDE均值各不恶化>0.5% | 工程投入标准，不是预言；测试前封存 |

GNN结构按用户原意适配师兄Plain图核心，共同时间模型保留。执行者不可在测试后替换为更弱结构。源文档中所有“默认建议”在本版作为研究协议执行；真正的最优值并未获证。

## 3. 文献证据与选型依据

本轮主要重新核验R01—R08、R10—R13的原始页面/指定正文。近三年窗口为2023-09-12至2026-09-12；基础图框架、数据说明、评价指标是明示例外。元数据或摘要核验不能说成全文复现。

| 编号 | 原始来源、核查层级 | 支持的设计 | 不支持的结论 |
|---|---|---|---|
| R01 | Gilmer等，ICML 2017，PMLR原始页 | 非线性消息先于聚合是常规GNN能力 | 一切GNN先平均、无法保持配对 |
| R02 | Ye等，ICLR 2025，官方摘要 | 节点/边编码、共享图结构和置换一致性 | MILP图判别结论自动适用本ZX电路 |
| R03 | Jaderberg等，PRA 2024，官方摘要/发表信息 | 编码频率和任务相关参数化值得学习 | 频率越大越抗噪、相对GNN必胜 |
| R04 | Rodriguez-Grasa等，PRR 2025，作者稿III/VI及正式信息 | 针对任务学习量子表示优于部分无任务嵌入 | QNN训练风险关系自动等于本LLM测试风险 |
| R05 | Shin等，PRR 2026，正文引言/核解释和正式信息 | 目标与特征谱的匹配、经典替代需分析 | 任意高维特征都更好；模型定制标签能作真实证据 |
| R06 | Bowles等，2024作者稿摘要 | 经典对照、训练预算和“量子性”归因需严谨 | 所有QGNN无效 |
| R07 | MFTraj，IJCAI 2024官方摘要 | 车辆行为与动态几何有建模价值 | 本数据已经证实该瓶颈或现有得分可混表 |
| R08 | Traj-LLM，2024作者稿，沿用v0.4方法核对 | 连续轨迹特征适配GPT-2/LoRA的接口路径 | 本模型LLM预训练贡献已经成立 |
| R09 | Tan等，NeurIPS 2024，沿用官方摘要 | LLM增量需要独立检查 | 所有轨迹时间模型都不需要LLM |
| R10 | Xiao等，TSP 2024，官方发表/摘要；信号模型沿用v0.5 | OFDM延迟/角度/多普勒关系 | 本文已复现其完整联合估计算法 |
| R11 | Bai等，2025作者稿摘要 | 多站未知关联与追踪有概率研究依据 | 本Hungarian-EKF就是该BP算法 |
| R12 | Rahmathullah等，FUSION 2017原始页 | 集合定位、漏检、虚警评价 | 单帧GOSPA评价身份连续性 |
| R13 | FHWA官方Lankershim说明及数据字典 | 轨迹来源、0.1s、完整官方记录30min | 本地文件已核实完整；这是雷达实测 |

已有文献与本项目推导共同支撑“值得有条件验证的结构”，没有直接给出当前三站、16比特、24维、真实未来上的Q对G优势。最接近的正面证据来自其他任务；相对优势仍不能由引用数量推出。

---


## 4. 共同三站前端

### 4.1 布局：给出生成规则，不虚构本地坐标

在源数据完成时间切分及场景身份隔离后，仅用最终保留训练episode中真实队列的位置行（跨锚点去重）确定Local_X/Local_Y各自5%与95%分位边界。被拒场景和验证/测试车辆不参与几何拟合。以边界中心为公共原点，坐标轴沿源坐标轴；记宽W、长L。

A01修订后作为本协议的默认仿真布局，三个站的平面坐标为

\[
 b_1=(-W/2-50,-L/4),\quad
 b_2=(W/2+100,0),\quad
 b_3=(-W/2-50,L/4)\quad[\mathrm m].
\]

A03仅保留A01平面站坐标，使用二维距离；旧站高、阵列朝向、±70°视场和10—300m斜距截断不参与当前逐车虚拟通道。中央站横向余量100m。站位在所有片段、划分、模型间保持不变。**不能围绕每辆目标车重新放置三个站，也不能按当前八车质心重置坐标。**

这些站位是归一化源场景上的仿真几何，不是声称存在于Lankershim的真实路侧安装位置。没有CAD/地理坐标文件，不宣称建筑遮挡真实性。F00输出具体W/L及三站数值，F01检查覆盖和几何，不按Q预测分数选择站位。若训练范围不足以形成正常覆盖，须在训练/开发前端审查中整体改版并留下记录。

### 4.2 逐车独立感知与适用边界

以Wei等2024符号级多基站感知论文[R14]为当前前端依据，原文保存在`docs/references/Wei2024_symbol_level_multi_BS.pdf`。每辆车独立生成三个站的虚拟感知通道，假设目标已分离且站间/时序对应已知；各车估计完成后才组成最多8车图。真实轨迹仅驱动物理仿真及提供评价标签，不直接作为模型状态输入。

本实验不验证多目标盲分离、匿名关联或多用户共享资源调度，不主张真实遮挡、固定发射功率预算或城市数字孪生。各车独立虚拟通道的接收SNR固定，不把它解释为同一发射功率下全部车辆的真实链路预算。C06的CFAR、JPDA、确认/删除和误检门槛停止用于当前主线，旧代码与结果保留。

### 4.3 符号级配置与项目尺度

| 参数 | A03当前配置 |
|---|---|
| 载频 | 24GHz |
| 带宽B | 93.1MHz |
| 符号周期T | 12.375μs |
| 符号数N_s | 256 |
| 子载波数K | 项目256；论文参考128仅作小区域检查 |
| 子载波间隔 | Δf=B/K |
| 状态及站位 | 二维位置/速度；三个站沿用A01共同几何 |
| 原始符号接收SNR | 5、10、15、20dB；标称20dB |

项目K256对应无模糊距离约412m。位置搜索边界仅由训练位置min/max向外各扩5m确定；三站到该区域最远角点约376.37m，低于约412m。训练队列已核对的最大站距约363m。论文K128参考不承担当前完整区域覆盖，其小区域结果不冒充完整项目验收。速度搜索边界固定为两个分量各±40m/s，不由验证/测试真值拟合。

### 4.4 信号生成与估计接口

`frontend/symbol_level.py`实现逐车符号级观测及估计；`frontend/run_symbol_frontend.py`保留为CPU参考及源适配历史；当前生产入口为`frontend/generate_symbol_gpu.py`，GPU核心为`frontend/symbol_level_gpu.py`；`frontend/check_symbol_level.py`执行数值校核，配置为`configs/symbol_frontend.json`。信号符号、相位、归一化和搜索方式以配置及验证报告中的实际实现为准，保留可复算参数与随机种子。不能以状态真值加噪替代符号观测的参数估计。

对一辆车，平面站距r_b=‖b_b−p‖、接近为正的径向速度v_rb=(b_b−p)ᵀv/r_b；已知QPSK符号X_b[k,n]的观测为

\[
Y_b[k,n]=A e^{j\phi_b}X_b[k,n]
 e^{-j4\pi\Delta f k r_b/c}e^{j4\pi f_c T n v_{rb}/c}+W_b[k,n].
\]

当前W为独立单位方差圆对称复高斯，A=10^(SNR_dB/20)；除以非零已知X后处理距离/多普勒相位。T是完整符号间隔，Δf=B/K，不将T设为1/Δf；本版不模拟时域CP。未知散射相位按站生成，不编码车辆身份。估计输出来自这些符号观测及固定搜索边界，真实状态只存在于仿真与误差评价侧。

每个时刻分别获得每车估计位置和速度，再按已知对应组成共同槽位。接收观测只使用截至当前时刻的源状态；源速度继续使用过去最近5帧位置的因果最小二乘，至少3帧后才激活。估计器不能读真实位置/速度来初始化、缩小当前样本搜索区间或挑选最接近真值的候选。

GPU生产使用`frontend/generate_symbol_gpu.py`与`frontend/symbol_level_gpu.py`，双RTX4090独立批处理；完整切窗使用`frontend/pack_symbol_dataset.py pack`，统一loader及标准化使用`frontend/symbol_dataset.py normalize`。旧CPU全量generate入口禁用；CPU只负责源读取、路由和写盘，标准化数值也使用torch CUDA。GPU独立验收PASS，见`reports/symbol_gpu/validation.json`；episode0/95整合试跑见`reports/f01d/gpu_smoke_generation.json`。这不代表正式全量完成。

正式入口为`python scripts/run_f01d_gpu.py`，由用户在服务器项目根目录、专用ICCT环境中手动执行。入口依次运行生成→pack→normalize→validate，进度显示在前台，并写入`reports/f01d/run_status.json`与`reports/f01d/F01D_REPORT.md`。当前正式任务未启动，不提前写完整缓存通过；助手只准备入口和说明，不代启动或自动监视正式全量。

### 4.5 SNR与随机条件

四档数值指原始接收符号SNR。实现固定圆对称复高斯噪声方差为1，将信号幅度设为10^(SNR_dB/20)，因此信号功率与噪声功率比为10^(SNR_dB/10)；不是固定信号幅度再调噪声方差。它与旧C06的100m参考加窗相干输出SNR不同，旧C06处理后20dB成绩不能与当前接收符号20dB直接比较。Q/G读取同一份持久化估计输入，复用相同观测实现；噪声副本不增加独立交通样本。

### 4.6 可选压力条件

若执行压力诊断，沿用预声明两状态衰减：坏状态接收功率降低20dB、平均持续0.3s、平稳概率0.1；100ms步长坏→好1/3、好→坏1/27。它是在标称原始符号SNR 20dB基础上的额外功率衰减，不是将SNR档位改为−20dB。仅为受控诊断，不代表真实遮挡，不进入主模型搜索。

### 4.7 三站估计与工程验收

#### 4.7.1 已知对应的融合

同一车辆的三个站观测按已知对应联合估计或融合，不能重复使用同一观测，也不借助真值恢复二维速度。保存各站参数误差及最终位置/速度误差，数值失败单独记录，不删除难例以降低RMSE。

#### 4.7.2 速度可辨识性（理论P01）

在位置及对应已知、径向误差局部近似无偏的条件下，z_v=Uv+ε。U各行为车指向站的单位方向(b_b−p)ᵀ/‖b_b−p‖，径向速度以接近站为正；满列秩时加权估计为v̂=(UᵀR_v⁻¹U)⁻¹UᵀR_v⁻¹z_v。几何病态会放大误差，单站一个瞬时径向速度不能唯一确定两个速度分量。该条件解释共有感知限制，不是量子优势。实际符号级联合估计不以此线性公式替代其实现。

#### 4.7.3 验收范围与版本边界

**历史A03-symbol-v1负档位验收（−20/−15/−10/−5dB，不是当前正档位成绩）：**已完成小规模前端综合验收：两配置各15项核心校核，共512次随机试验零异常；37个真实episode、每个3个快照、四档SNR，每档776个目标快照，零搜索失败、零全局边界命中；两片段共100帧、498个目标状态删除5秒后的源数据重放，逐项一致。此结果不是每episode199帧的完整输入生产。证据见`reports/symbol_level/A03_REPORT.md`、`provenance.json`、`real_scene_quality.json`及`causal_checks.json`。必须核对有限输出、误差单位、源权限、固定搜索范围、逐车对应、SNR定义及未来变异不改变当前估计。此轮不沿用旧覆盖90%/虚假5%或5m绑定门槛，不虚构新性能阈值。

当前A03-symbol-v3-cuda-positive-snr的5/10/15/20dB验证与生产准备中，不能将上述历史数值换标签或直接认作本版通过。

### 4.8 几何、版本与失败处理

A01训练来源、共同坐标与三站几何继续复用。算法若数值失败，先修符号、单位、搜索和接口，再用相同预声明样本验证；失败必须计数。变更模型配置或搜索边界需留下记录，不利用Q/G预测分数选前端。小规模验收通过后仍须F01-D完整共同缓存及其信息权限验收，才能为后续训练提供输入。

---

## 5. 数据、场景与因果性

### 5.1 八车场景怎样从真实道路抽取

不能假定真实Lankershim同时只有八辆车。本研究是**从真实轨迹抽取的有界八车感知仿真场景**，不是整条道路的全目标实测。

先按源时间切分。每段连续记录内，按20s不重叠片段生成episode；每个片段开始时，仅根据该时刻已出现车辆与固定随机种子选择至多3个不同锚点。每个锚点选择自身及45m内最近至多7辆车。不得要求车辆在未来20s一直存续、不得按未来转弯/预测误差选择。

这批真实对象构成该episode的仿真队列；其记录结束后移除，不拿新真值车辆补槽，也不把剩余车辆重新编号。逐车感知使用已分离/对应已知假设；各车仍须从其符号观测估计状态。其余真实道路车辆在此受控场景中不产生回波；它们对已记录运动的潜在影响仍在源轨迹中，必须作为部分观测局限说明。

后续episode按其起点队列重新初始化；当前episode内感知源满足因果历史条件后才激活，缺少合法估计时保持mask=0。A03不使用旧FoV进入或匿名追踪出生规则。基础固定队列不声称复现所有自然动态进入事件；压力/扩展需另定义。不同锚点共享源时间，统计时不能被视为完全独立道路记录。

episode前1s作为状态估计预留区间，首次评分起点至少保证历史区间落在烧入后；T=20覆盖过去1.9s，H=20预测未来2s。建议训练起点步长0.5s、验证/测试1s。episode末尾统一保留2s标签空间；不因某辆车未来先离开而删掉整个起点，按真实可见标签mask记录删失。

### 5.2 参考速度与时间内插

NGSIM位置先按ft→m乘0.3048，时间先核查Global_Time单位、单调性、重启和重复ID；key至少包含记录段及源Vehicle_ID。参考速度仅在仿真源侧用截至当前时刻的最近5帧位置做因果线性拟合，少于3帧暂不激活回波源；不要先全轨迹中心差分再切历史。

A03在每个100ms deadline构造逐车符号级快照，源状态只取截至该deadline的记录；使用最近已知样本的因果外推。符号观测采用该状态的冻结参数近似，不能读取deadline之后的记录；不再按旧三站CPI中心取源。参考未来位置仍使用源数据原始标签，不通过同一个模型预测标签。

**因果验收：**固定训练阶段已封存的站位、坐标尺度、搜索范围、episode清单和观测随机种子，仅改变t之后的全部源位置/速度/标签，再生成直到t的逐车符号观测、状态估计与模型输入，应完全不变。不在这个推理因果测试中重新拟合全训练集尺度；预处理参数的训练/测试权限另行审计。允许t之前的滤波使用截至t的观测，但主版不做回看t之后的平滑。该检查也覆盖episode选择、ID、mask及速度差分。[S0]

### 5.3 先切源记录，再派生样本

默认按实际连续源时间60%/20%/20%划分训练/验证/测试。若本地确有官方完整30min，对应约18/6/6min；这是推导而非本地已核实统计。[R13]

边界两侧各5s禁用，所有含烧入/历史/未来的episode支撑都必须落在同一划分。不得把重叠窗口随机分进不同集合。共享源时间的不同锚点、SNR和随机回波重复始终在同一集合。

身份交叉检查（A01）：先生成各时间段的完整候选episode，再按train→V_select→V_confirm→test的固定时间顺序处理；集合内按起点时间、锚点key排序。某episode包含已归属其他集合的源组，则整体拒绝，不删邻车；否则保留完整队列并将其源组归属当前集合。被拒episode不取得任何源组归属。最终保留集合的源组必须两两互斥。原始轨迹跨时间边界只作诊断，不因其在另一时间段有未被使用的原始行就全局删除。记录每条拒绝原因及源组归属，不按模型成绩或未来运动质量选择。

训练规模最多6000个干净运动起点，按源时间块均衡、固定种子抽样；噪声副本不计作新增独立运动起点。验证/测试使用全部合法起点，不强制与旧缓存1200数目相同。若训练候选不足6000，使用全部，记录真实数量。

旧测试曾被查看。重新切分或重新加噪不能自动获得“从未见过”的交通行为测试集。`exposure_status`默认`unknown_or_previously_exposed`；只有工程历史证明某源区间未用于选择时才标为真正未使用。论文可报告预先封存的回顾性分块评价，不能冒称独立外部泛化。[S0]

### 5.4 两个严格分离的数据文件

**预测输入缓存（可提供模型）：**

| 字段 | 形状/类型 | 含义 |
|---|---|---|
| state_hat | float32 [S,T,8,4] | 估计x,y,vx,vy，m/m/s |
| track_exists | bool [S,T,8] | 当前槽位有合法因果估计历史；不等于当帧有感知更新 |
| detected | bool [S,T,8] | 该100ms帧有可用的本车感知量测更新 |
| timestamp | float64 [S,T] | 秒；统一预测时钟 |
| track_key | int64 [S,8] | 已知对应的路由键，仅元数据；不传入forward或embedding |
| source_block/episode_id | metadata | 分组与去重，禁止模型利用身份捷径 |
| input_hash/frontend_hash | string | 同输入审计 |

**监督/评价侧车文件（推理不可访问）：**原始队列真值、未来位置、label_valid、已知对应的标签路由、输入不可用目标、真实遮挡/源参数标签、SNR条件及源ID。协方差P和感知估计统计可存前端审计文件，但基础预测器不读取它们；未来启用必须同时开放Q/C并改输入契约。

F01-D全量生成、打包和验收已通过，当前产物位于`data/f01d/inputs/`、`labels/`、`metadata/`、`sequences/`：inputs为估计输入，labels只供监督/评价，metadata保存对齐与溯源，sequences保存完整估计时序。统一入口为`frontend/symbol_dataset.py`，模型数值接口不读取标签或路由身份。旧负档位生成已停止；当前正SNR v4全量缓存已验收通过。

同一前端输出先持久化，再分别训练Q/C。禁止各模型loader自行重做状态估计或改变对应、给状态另加不同噪声或按不同条件删样本。

---


### 5.5 本轮补齐：开发选择与确认分离

训练/验证/测试仍按原始时间60%/20%/20%。将原验证段再按时间前半为V_select、后半为V_confirm，分界两侧各5s隔离；一个episode的烧入/历史/未来全部位于同一段。两者不足以形成足够原始时间块时，记录规模限制，不随机打散窗口扩充独立样本。

V_select用于Q的3个和G的2个既定开发配置选择与早停。V_confirm只用于所选配置的两种子前置确认以及一次预声明的灰区复核；不在看到确认结果后继续选层数、改GNN或增加模块。确认经历一次复核后不再称从未用于开发；最终测试仍独立留存。旧测试曝光状态按实际工程历史报告。

小头预筛选的最多512个训练运动起点和后续最多2048个确认训练起点，都从训练源时间块均衡取样，固定种子，不跨集合、不生成量子有利标签。前者只在标称原始符号SNR 20dB训练；后者每epoch按固定日程平衡轮换四档SNR，重复噪声不当作新独立运动。V_select/V_confirm均报告四档宏平均，最终主模型仍使用100%合法训练数据。

### 5.6 缺失、跨度与身份细节

一个槽位对应预先假设已知对应的车辆；估计尚不可用的左侧历史填充并mask。预测起点的合法对象要求track_exists=1且至少有3帧有效估计历史，不能要求未来完整才选择。不能因为未来标签缺失而改变输入对象。量子每个时刻的填充比特均与有效节点隔离；已退出当前队列的车辆不以新的车辆替换槽位。source_ID只用于独立元数据的路由与核对，不是模型数值输入；track_exists/detected只依赖当前和过去的估计可用性，不由未来label_valid或真实估计误差决定。保留估计四维状态及两个标记的A02六维接口。

事件/难场景标签只作评估侧元数据。用源参考历史定义停车、近距或转向的统计，不是额外模型输入；用估计历史定义的分组另加estimated前缀，二者不可混用。假设检验始终以预先定义的全量结果为主。


## 6. QGNN完整数学定义

### 6.1 每车两比特，最多16比特

车辆i使用P_i、V_i两个比特，初始分别编码位置和速度。每个历史时刻单独准备量子态；时间长度不增加比特数。

定义

\[
F_s(a)=\frac2\pi\arctan(a/s),\quad
r_{\ell,c}(a)=\frac\pi4F_{s_c}(\omega_{\ell,c}a).
\]

编码增益ω为每层每状态维度共享的正数，建议约束[1/4,4]，初始化为1。正值区间是设计选择；不为车辆槽位分配单独参数。量子速度编码只做尺度归一化，不减去训练均值；理论T06的零速度指物理零速度。Q时间模型的数值标准化另按训练块固定；G节点使用相同训练标准化状态，边按§7.1从SI状态计算。

### 6.2 首次准备与后续重编码分开

首次编码保持v0.2修订后的中心：

\[
E_{1,i}=
R_z^{P_i}(\pi/2+r_{1,y}(y_i))R_y^{P_i}(\pi/2+r_{1,x}(x_i))
\otimes
R_z^{V_i}(\pi/2+r_{1,v_y}(v_{y,i}))R_y^{V_i}(\pi/2+r_{1,v_x}(v_{x,i})).
\]

对于ℓ≥2，改为零中心更新：

\[
E_{\ell,i}=
R_z^{P_i}(r_{\ell,y}(y_i))R_y^{P_i}(r_{\ell,x}(x_i))
\otimes
R_z^{V_i}(r_{\ell,v_y}(v_{y,i}))R_y^{V_i}(r_{\ell,v_x}(v_{x,i})).
\]

首次编码负责建立可区分状态；后续操作负责再次注入数值变化，零特征对应恒等操作。门序为右侧先作用。

**首层单射性仍成立：**角度均处于(π/4,3π/4)，由Bloch分量反演θ、φ，再反演输入。此性质不升级为最终24维读出充分，也不保证极端大数值的有限精度分辨率。

### 6.3 有向图角度与图门

\[
e_{j\to i}=[(p_j-p_i)/45,(v_j-v_i)/15],\quad
\kappa_{ij}=\exp[-\|p_i-p_j\|^2/(2\cdot45^2)].
\]

距离单位为米。平滑权重是当前默认，不把任意车辆编号或真实车道关系作为输入。

**A02接近关系仅在Q内部计算。**记r_ji=p_j−p_i、u_ji=v_j−v_i，全部取共同缓存中的估计状态，定义

\[
c_{ji}=-\frac{r_{ji}^{T}u_{ji}}{\sqrt{\|r_{ji}\|^2+\varepsilon^2}},\quad
a_{ji}=\tanh(c_{ji}/s_v),\quad \varepsilon=1\mathrm m,\quad s_v=15\mathrm{m/s},
\]
\[
e^+_{j\to i}=[e_{j\to i},a_{ji}]\in\mathbb R^5.
\]

c>0为接近、c<0为分离；它是平滑径向接近速度代理，不是TTC或碰撞概率。ε与s_v是预声明尺度，不是实测最优值。GNN按§7.1保留源七维关系特征；两侧原始状态相同，内部特征处理不同。由交换源目标可知a_ji=a_ij；其本身不提供有向因果信息，方向差异仍由原始差分与P/V图门体现。

\[
g_{j\to i}^{(\ell)}=
\frac{m_i m_j\Gamma_\ell\kappa_{ij}}7
\tanh((w_\ell^+)^T e^+_{j\to i}+b_\ell),
\quad 0<\Gamma_\ell<\pi/2.
\]

Γ可用sigmoid参数化，初始π/4。分母固定7，避免添加无关车辆后缩小既有关系。

\[
U_{j\to i}^{(\ell)}=\exp[-ig_{j\to i}^{(\ell)}Z_{P_j}X_{V_i}/2],
\quad Q_\ell=\prod_{i\ne j}U_{j\to i}^{(\ell)}.
\]

同层所有生成项可交换，反方向使用另一对比特。图的“方向”不是量子门的信息单向性或交通因果证明。

### 6.4 车内混合和整图更新

\[
V_\ell=\bigotimes_i\{[R_z(\alpha_{\ell P})R_y(\beta_{\ell P})
\otimes R_z(\alpha_{\ell V})R_y(\beta_{\ell V})]
R_{XX}(\eta_\ell)\}.
\]

\[
|\psi_\ell\rangle=V_\ell Q_\ell E_\ell(S)|\psi_{\ell-1}\rangle,
\qquad |\psi_0\rangle=|0\rangle^{\otimes2N}.
\]

主配置L=3；只保留L=2作为F04预先限定的开发/成本备选。L=1仅用于校核，不新增主要训练模型。不得仅为了难以经典模拟而增加深度或角度。

各层4个编码增益、7个边函数参数（五维w⁺、b、Γ）、5个车内参数，L=3合计48个量子核心参数，L=2为32个。旧Q对应每层15个、三层45/两层30个；这些都不是整个学习系统参数量，不能据此直接使用小样本复杂度结论。

初始化仍取w=b=0、车内混合恒等，便于明确定义与局部理论比较。Γ非零，图输出投影非零初始化。零交互是初始点而非整个训练的约束；不加入人为鼓励纠缠或非线性余项变大的损失。

### 6.5 24维主读出

浅层状态为χ1=Q1E1|0〉，不包括V1；深层为ψ3。

记C(A,B)=〈AB〉−〈A〉〈B〉。

| 位置 | 输出项 | 维数 |
|---|---|---:|
| 浅层 | P_i和V_i各自的X,Y,Z | 6 |
| 浅层 | 分别对j≠i求和C(Z_Pj,Y_Vi)、C(Z_Pj,Z_Vi)、C(X_Pj,Z_Vi)，除以7 | 3 |
| 深层 | P_i和V_i各自的X,Y,Z | 6 |
| 深层 | C(X_Pi,X_Vi)、C(Y_Pi,Y_Vi) | 2 |
| 深层 | 对j≠i求和C(Z_Pj,X_Vi)、C(Z_Pj,Y_Vi)、C(Z_Pj,Z_Vi)、C(X_Pj,Z_Vi)，除以7 | 4 |
| 浅层追加 | 对上述浅层三类逐边连接量分别以a_ji加权，除以7 | 3 |

前五行保持原21维顺序，最后追加三维，合计24维。新增量明确为

\[
q^{app}_{i}=\frac17\sum_{j\ne i}m_im_ja_{ji}
\big[C(Z_{P_j},Y_{V_i}),C(Z_{P_j},Z_{V_i}),C(X_{P_j},Z_{V_i})\big],
\quad q_i^+=[q_i^{21},q_i^{app}].
\]

q²¹在这里指当前同一电路上的原21个读出，不自动等于独立训练的旧Q。新增三项复用已有逐边Pauli矩，无新增量子门、Pauli种类或整图态调用；若原实现已流式求和，可同步累计三个加权和。经典乘加、反向与实际内存仍要计入成本。用存在掩码隔离填充节点，没有邻车时双车项为零。不同层与新增读出未被证明独立或充分，须检查冗余与任务利用。

三车累积量和7维扩展均不进入本版执行矩阵。局部读出本身能够含有多车作用，见理论T05。

计算期望值使用纯态解析模拟；不构造全密度矩阵，不凭空引入硬件噪声。Pauli矩可采用统一基变换或直接算符作用批量读取，具体后端需通过数学一致性检查。
### 6.6 实施与读出的补充契约

**数值尺度。**量子节点每个状态维度c使用s_c=max(Q95(|state_hat_c|),1)，分位仅在训练输入缓存中track_exists=1的记录上拟合，位置下限1m、速度下限1m/s；不减去速度均值，物理零速度保持为0。公共位置原点按第4节固定。Q时间模型本车输入用训练缓存μ_c、σ_c标准化，σ下限1e−3对应物理单位。GNN节点读取相同训练标准化状态和标记，边特征单独从SI状态按§7.1计算，不能将标准化数值当作米或米每秒。V_select、V_confirm和测试都不得参与拟合，结果保存normalization.json；F01-D明确以train四档SNR缓存中track_exists为真的每个状态项同权拟合，保留窗口重叠的项，不另作去重或按噪声条件加权，并记录有效项计数。若做50%数据解释，必须说明这是预测标签样本量比较；共同前端标定仍是固定资产，不能称整个系统只见过50%原始数据。

每个历史时刻只运行一份整图态，读取所有有效车，不为8辆车重新重复8套整图。实际q位寄存器可固定16并屏蔽填充，或按有效N收缩后恢复槽位；两者输出/梯度须通过F02等价校核。mask分支不可依赖真实ID。

初始化图权w⁺=b=0、Γ=π/4，车内参数0；增益初始1。Γ和增益用有界可微参数化，不用硬截断造成意外零梯度。增益可写ω=0.25+3.75 sigmoid(ξ)，初始ξ=logit(0.2)；Γ=(π/2)sigmoid(ζ)，初始ζ=0。48指L3情况下自由标量数，不代表每参数只发生一次门操作。ξ/ζ与接近量a/c不是同一变量。

6个单比特读出的顺序为[P_X,P_Y,P_Z,V_X,V_Y,V_Z]。先按原浅层9维、深层12维排列，再追加三个加权浅层量；边项含源/目标存在掩码并除以固定7。输出24维不是已证明的充分统计量。即使初态对4N维单射，有限测量与时间头也可能丢失关键关系；这是F04/F06检查对象，而非用高维Hilbert空间掩盖。

正式量子实现为prediction/quantum.py中的PennyLane解析纯态模拟，Snapshot在同一次演化中保留浅/深态；quantum_torch_reference.py仅作校核。complex128用于参考校核。生产complex64只有在F02输出/导数误差及一个共同开发精度核验通过后启用，先确定精度再搜索模型。不得把有数值误差的版本与另一侧双精度差异解释为量子机制。


## 7. GNN、共同LLM接口与预测输出

### 7.1 展示名称与实际GNN定义

展示名仅**QGNN**和**GNN**。GNN复用师兄Plain的图核心并适配当前逐帧输入，输出接共同时间模型；这是图核心适配，不是完整原Plain预测器复现，不继承历史成绩。来源链为`code/02_pennylane_qgnn_core/run_final_test_report.py`的`plain_gnn` → 同目录`model.py`的`build_graph('plain')` → `code/00_remote_shared_dependencies/target_interaction_graph.py`的图注意力类与边函数。

每帧4维标准化状态及detected/exists组成6维输入，使用现有MLP 6→128→128节点适配；节点与层参数按原接口共享规则组织。图核心固定2层、4头、hidden128的`DenseEdgeGraphAttention`，保留源`build_edge_features`七维边、45m半径+self、门控、LayerNorm及FFN。七维边为相对位置2维、相对速度2维、距离、接近量、TTC；从SI状态计算，保留源30m/15m/s/45m/15m/s/20s尺度及TTC截断。源dropout=0.10，消息及前馈残差门按源类零初始化。输入和图输出按合法exists隔离，输出为[B,T,8,128]。

图输出接`TrajectoryPredictor(128)`，与Q的`TrajectoryPredictor(24)`使用同规格GPT-2、LoRA、预测头、CV及loss。师兄原GRU、132维节点projection、24维时间embedding和原decoder不纳入本轮主模型；它们不能与逐帧6维适配混称同一完整模型。验收应核对复用图核心及边函数的一致性，验证6维节点适配、动态mask、排列、因果、共同时间接口和真实缓存前后向。旧A02四维边MLP图实现保留历史辅助，不自动追加其训练。

### 7.2 数值token

新QGNN每车每时刻的输入为4维本车状态、2个标记、24维图特征，共30维；旧Q修订对照为4+2+21=27维；G为4+2+128=134维。各自用逐车共享线性投影到768维，时间模型规格相同：
\[
z_{i,t}=W_s\widetilde s_{i,t}+W_m[m_{i,t},d_{i,t}]+W_g^+ q^+_{i,t}+b+p_t.
\]
不同图维度有各自W_g，其余接口类型相同。时间位置嵌入只加一次；不得手工加入后又让GPT-2再次加入。同场景N个车辆序列变为B·N条长度20序列；主体逐车共享，不提供跨车辆LLM注意力。本车原始估计状态保留旁路，不能用移除本车信息强迫Q产生收益。

上式写新Q；G和旧Q代入各自图输出与投影。新Q的W_g⁺为768×24，旧Q为768×21。新增三列初始化为0，其余21列按原非零规则；图分支并非整体零初始化。新增核心边权第五分量与三列投影均为0时，可逐点恢复任意旧Q参数解（T13）。初始连接量可能为0，不声称每条新增路径第一步即有非零任务梯度。

首次合法估计前的token左填充，attention mask来自track_exists而不是detected。有效历史时间位置按实际0.1s网格，不能因短历史而改为不同物理时间跨度。遇到全部mask的序列直接跳过；不足3帧者无预测并计输入不可用。输入禁止含true_ID、future mask、未来轨迹、测试真实误差。

### 7.3 时间模型

Q/G共同时间模型为GPT-2基础规格（12层、12头、hidden768），连续inputs_embeds接口；冻结基础权重，在融合QKV投影上使用LoRA r8/α16。GPT-2常用融合投影，不照搬其他模型的模块名；F00固定checkpoint提交、文件hash、框架版本和可训练参数清单。[R08]

最后一个有效时刻hidden进入768→256→40的SiLU预测头：
\[
\widehat p_{i,t+h}=\widehat p_{i,t}+h\Delta t\,\widehat v_{i,t}+\Delta p_{i,h},\quad h=1,\ldots,20.
\]
起点和恒速项训练时也使用估计状态，真实位置只用于未来监督。输出单条轨迹；不通过增加K改报minADE。

### 7.4 损失及训练层次

训练采用每场景有效对象/时刻归一化后的ADE+0.5FDE。不存在末帧标签的目标不计算FDE，不把其末个可见位置当20步终点；一个场景无FDE有效对象则只计算其ADE，并记录该批缺失权重。模型选择与最终评价则按第10节固定宏平均分母计算，二者差异在代码测试中显式记录。

**主训练一个epoch的定义。**对训练manifest中的每个真实运动起点，每epoch只读取一个SNR实现，采用按起点/epoch平衡轮换四档的固定日程，四个连续epoch覆盖各条件；Q/G同seed采用同一条件序列。验证/确认/测试每次均评全部四档。完全没有合法监督的场景保留在输入/监督可用性账，训练损失为0；全批无监督时跳过optimizer步并记录，不临时替换成容易SNR。该规则使实际step数可复算，不把同源噪声副本当成四倍独立运动数据。

Q/G共同主训练阶段A最多3epoch：只训练本车/标记投影、LoRA和预测头，图分支输入固定零；每种子一份共同本车检查点，Q/G及旧Q按同seed复用。阶段B2epoch：只训练图模块及非零初始化W_g，冻结其他参数但保留LLM对输入的反向图。阶段C最多15epoch、验证耐心4：图、适配器、LoRA、预测头联合学习，LLM基础权重仍冻结。阶段化日程为本协议默认，只有F04灰区预声明复核可采用短直接联合日程。

优化器AdamW，图学习率来自F04选定{3e−4,1e−4}，输入投影/头1e−4，LoRA3e−5，梯度裁剪1；普通矩阵衰减1e−2，量子有界标量不套用同一种衰减。有效场景批量16，微批和累积由F03确定。随机种子、数据shuffle和模拟噪声分开记录。Q/G同seed共享本车预热，分别初始化图分支与投影并按共同图适配/联合日程训练；同一模型的已训练头不能直接强塞给另一模型作为公平主对照。

**A02不增加重复残差损失。**令恒速基线b_h=估计位置+hΔt·估计速度、真实修正r*_h=p_h−b_h，则||Δp_h−r*_h||₂=||b_h+Δp_h−p_h||₂。在同一mask、权重和范数下，额外残差ADE/FDE只重复原目标。修正还包括感知误差和本车行为，不标为纯邻车交互真值。维持原损失，新增信息的作用通过F06-D/E检验。

**训练充分性。**完整训练使用100%合法训练集；若任一模型到预定上限仍有明显开发改善趋势，不声称已充分收敛。测试解封前可记录对所有受比较版本适用的日程修订与停止规则；资源不能支持时限定结论为该日程下结果，不人为截短G训练。旧Q按新Q已选定的图深度/学习率和同一日程从头训练，复用同seed共同本车预热，不独立开启旧Q结构搜索。



## 8. 理论审查：完整条件、推导与优势边界

本节是对当前数学构造的纸面分析，统一编号T01—T14。T01—T12继承原ZX门结构，A02新增关系权重的边界见T07/T13/T14；没有进行数值电路测试。它们不是外部文献已经证明的Lankershim优势。理论P01在第4节描述共有速度可辨识性。

### T01：首次编码保留四维状态，但读出不因此充分

每个纯态比特写为
\[
|\varphi\rangle=\cos(\theta/2)|0\rangle+e^{i\phi}\sin(\theta/2)|1\rangle.
\]
首次θ、φ均在(π/4,3π/4)，因此sinθ>0，且相位不会绕2π发生重合。测量期望为
\[
\langle Z\rangle=\cos\theta,\quad
\langle X\rangle=\sin\theta\cos\phi,\quad
\langle Y\rangle=\sin\theta\sin\phi.
\]
由θ=arccos〈Z〉、φ=atan2(〈Y〉,〈X〉)可唯一反演。给定正ω及s，有
\[
r=\tfrac12\arctan(\omega a/s),\quad a=(s/\omega)\tan(2r).
\]
故单个比特的两个初始特征、每车两个比特的四个初始状态，在理想精度下均单射。

**界限。**一次有限shots测量不能精确读出这些期望；重编码是数据依赖操作，不能仅以每个固定酉矩阵可逆就推导整个数据映射可逆。有限24维读出未被证明充分。若固定目标状态且7辆邻车的28维连续状态在开放集合变化，连续映射到24维仍不可能普遍单射；实际道路可能低维、任务可能不需要全信息，因此这只是风险，不是已证明性能退化。

### T02：车辆重排等变与无关节点一致性

设π是车辆重排，对每车P/V成对重排的算符为P_π。共享节点编码、共享关系函数、共享车内混合分别满足
\[
E(\pi S)=P_\pi E(S)P_\pi^\dagger,
\quad Q(\pi S)=P_\pi Q(S)P_\pi^\dagger,
\quad V=P_\pi V P_\pi^\dagger.
\]
对任意两条图生成项，P寄存器只含Z，V寄存器只含X，故
\[
[Z_{P_j}X_{V_i},Z_{P_k}X_{V_l}]=0.
\]
因此图门顺序不产生额外依赖。原始节点读出随节点变换，邻车连接量求和对其顺序对称，得到q(πS)=πq(S)。共享逐车LLM延续这一性质。

若加入与旧图完全断开的新有效节点，局部操作使其始终与旧图乘积，所有跨图连接相关量为0。在公共坐标不变、原车辆未被8槽筛选替换、分母固定7的条件下，旧节点输出不变。旧分母N−1会把原角度乘(N−1)/N，即使新边为0仍改变旧关系，故已撤回。严格正距离核可考察新边趋近0的极限。上述性质不是任意空间旋转等变，也不要求现实中增加一辆有实际关系的车后输出不变。

### T03：首层局部关系响应与可区分配对

首层各车独立编码、所有跨车门角度为0时，令u_j=〈Z_{P_j}〉、y_i=〈Y_{V_i}〉，读出c_{ji}=C(Z_{P_j},Z_{V_i})。对U=exp(−igZ_{P_j}X_{V_i}/2)有
\[
\partial_g\langle Z_{V_i}\rangle|_0=u_jy_i,
\quad \partial_g\langle Z_{P_j}Z_{V_i}\rangle|_0=y_i,
\quad \partial_g\langle Z_{P_j}\rangle|_0=0.
\]
代入连接量：
\[
\boxed{\partial_gc_{ji}|_0=(1-u_j^2)y_i\ge1/4.}
\]
下界因为sin²θ_P≥1/2且sinθ_V sinφ_V≥1/2。除以7后的聚合对单门角度响应至少1/28。**这不是共享权重或最终损失的梯度下界**：g对w的导数、跨边抵消、W_g和LLM均可能改变响应。旧零速度相位中心0使y_i=0，新首次中心π/2修复的是这一具体失敏问题。

配对例：目标0位置取公共原点、前后位置±d；初始源Z期望为−a和+a，a>0。有符号速度关系使两种情形的边角度分别为(−g,+g)、(+g,−g)。目标初始Y期望b>0，令c=cosg、s=sing。首层两个联合因子为
\[
F_A=(c+ias)^2,\quad F_B=(c-ias)^2,
\]
目标Z读出为z_0 ReF+b ImF，故
\[
\boxed{q_A-q_B=4abcs=2ab\sin(2g)>0,\qquad 0<g<\pi/14.}
\]
这是本电路存在关系区分能力的构造，不证明训练会学到该参数，也不证明常规GNN输出相同。例如两组邻车(r,u)为{(+1,−1),(−1,+1)}与{(+1,+1),(−1,−1)}，普通消息ReLU(r+u)求和分别是0和2；无需专门增强即可区分。因此不再用这两点作为所有GNN误差下界。

### T04：首层精确经典公式、恒零通道及其边界

固定目标i，乘积输入下u_j=〈Z_{P_j}〉，有效联合因子
\[
F_i=\prod_{j\ne i}(\cos g_{ji}+iu_j\sin g_{ji}).
\]
若目标初始Bloch分量为(y_i,z_i)，则
\[
\langle Y_{V_i}\rangle=y_i\Re F_i-z_i\Im F_i,
\quad\langle Z_{V_i}\rangle=z_i\Re F_i+y_i\Im F_i.
\]
写F=A+iB，每个邻车可按
\[
A' = A\cos g-Bu\sin g,\quad B'=Au\sin g+B\cos g
\]
递推，初始(A,B)=(1,0)。全部目标O(N²)。关系乘积真实存在，但这个首层原语有精确经典计算，不能凭它宣称量子独占。

**全部低阶矩的构造。**对全部V作Hadamard换基，Q化为
\[
D=\exp[-\tfrac i2\sum_{ab}g_{ab}Z_aZ_b].
\]
输入仍为乘积\(a(z)=\prod_ba_b(z_b)\)，z_b=±1。任意Pauli串M翻转的比特集合为F，附带可分解相位η(z)，有
\[
\langle M\rangle=\sum_z a(z^F)^*a(z)\eta(z)
 e^{i[H(z^F)-H(z)]},\quad H(z)=\tfrac12\sum_{ab}g_{ab}z_az_b.
\]
相位差只保留F与补集之间的边：
\[
H(z^F)-H(z)=-\sum_{a\in F,b\notin F}g_{ab}z_az_b.
\]
固定z_F后，补集比特独立求和。令h_b=Σ_{a∈F}g_{ab}z_a：若M在b为I，局部因子cos(h_b)−iu_b sin(h_b)；为Z，因子u_b cos(h_b)−i sin(h_b)。F内部的振幅与X/Y相位直接相乘，再枚举2^|F|项。最多2比特Pauli矩每项O(N)，首层所需全部节点对原始矩可直接O(N³)计算；新增加权聚合复用这些矩，不改变此首层上界。若分析旧三比特矩，则直接上界O(N⁴)。该乘积输入公式不能直接用于多块后的深层读出。

**首层已删除的4个旧通道。**C(X_Pi,X_Vi)、C(Y_Pi,Y_Vi)、Σ_jC(Z_Pj,X_Vi)、Σ_{j<k}κ₃(Z_Pj,Z_Pk,X_Vi)为0。前两项因无P_i—V_i自边时后向算符支持互不重叠，在乘积初态中分解；后两项因所用量及乘积都与Q对易，保持初态独立值。此结论只在第一层图门之后、车内混合之前成立；深层不得照抄删除。

图已经有多层非对易混合后，输入不再是此乘积形式，不能逐层套公式并宣称整个QGNN被同样O(N³)替代。首层公式仅用于F02校核或明确等价的实施优化，不再形成一个额外需主训练的增强GNN。

### T05：多层一阶仍是逐边消息；局部读出也可含多车项

把所有跨车角度置0，本车局部编码与混合可非平凡甚至车内纠缠，但整个态仍按车分解。第ℓ层插入一次跨车门，导数为
\[
\partial_g\langle O\rangle|_0
=\tfrac i2\langle[A_{j,\ell}\otimes B_{i,\ell},\widetilde O]\rangle_{\rho_j\otimes\rho_i\otimes\rho_{rest}}.
\]
只有两个车辆块（各至多4维矩阵）参与；不相关第三辆在乘积中消去。连接相关量的减项抵消第三辆的单次贡献。共享参数的一阶导数是各次插入的和，所以
\[
q_i(S;\theta_0+\delta)=a_i(s_i;\delta)+\sum_{j\ne i}\Psi_\delta(s_i,s_j,e_{ji},e_{ij})+O(\|\delta\|^2).
\]
这解释初始化附近为何可表现为普通消息函数，不证明训练结束后只有一阶。零跨车点的三辆不同车辆累积量κ₃及其一阶导数均为0：单个二体插入无法连接第三辆；因此已撤回默认三车累积量，不能将双车响应下界套给它。

另一方面，对两个源j,k作用目标i：
\[
U^\dagger Z_iU=c_jc_kZ_i+s_jc_kZ_jY_i+c_js_kZ_kY_i-s_js_kZ_jZ_kZ_i.
\]
局部Z_i测量可以含三个车辆的共同作用，不必额外输出三车累积量。这个首层例子仍可经典计算；“含多车项”不等于“所有项帮助预测”或“纠缠不可替代”。

### T06：初始准备与后续重编码的区分

旧零二维输入编码为E₀=Rz(π/2)Ry(π/2)，Bloch向量经历+Z→+Y→−X→+Z，故E₀³=−I。图关闭和车内恒等时，反复三次准备零速度会回到初始比特（忽略整体相位）。这不是全部梯度为零的证明，只是无任务依据的固定旋转循环。

新编码首次建立π/2中心状态，后续只用r(a)零中心角。零特征使后续E=I，不额外增加参数。首次单射性、重排性质与T03不受影响。此修正是规范参数化，不是已证实ADE改进。

### T07：输入扰动界与共同信息上限

对两个相同掩码/边集合的输入，数据相关旋转角从α_k变成α'_k；单个Pauli旋转的算符差≤|α_k−α'_k|/2。逐门望远镜展开、酉范数1，得到态差≤Σ_k|δα_k|/2。对范数≤1的单个Pauli观测：
\[
|q(x)-q(x')|\le\sum_k|\delta\alpha_k|.
\]
连接量还需对其两个矩乘积使用三角界，不能直接沿用系数1。该上界可很松，也不覆盖身份交换、突然漏检或边集合跳变。

A02加权读出还含输入相关权重a。固定mask时，因|a|≤1且|C|≤1，有|δq_app|≤(1/7)Σ_j(|δa_ji|+|δC_ji|)，不能只计算量子态变化而漏掉权重变化。由§6.3可得||∇_r c||≤||u||/ε、||∇_u c||≤1，继而||∇_r a||≤||u||/(s_v ε)、||∇_u a||≤1/s_v；平滑避免奇点，不证明抗噪。r=0时c=0也不代表没有碰撞风险。

对任何固定表示Z_M=Φ_M(X)，事件的原像在输入空间中可测，因此
\[
\operatorname{TV}(P^A_{Z_M},P^B_{Z_M})\le\operatorname{TV}(P_X^A,P_X^B).
\]
Q/G拥有同一输入信息上限，不可能从量子态维度凭空恢复丢失的证据。

### T08：表示重叠与ADE/FDE的精确条件关系

定义完整表示Z_M=(本车历史,q_M(X))，M∈{Q,G}；把旁路包含在比较里。条件于独立训练数据及已固定参数。分析两个等概率情形A/B，未来原型Y_A,Y_B确定，轨迹距离D=||Y_A−Y_B||_w>0，\(||Y||_w=H^{-1}\sum_h||Y_h||_2+\lambda||Y_H||_2\)。相应表示分布的TV为V_M。

给定z，π=P(A|z)。当π≤1/2时，由三角不等式
\[
\pi\|y-Y_A\|_w+(1-\pi)\|y-Y_B\|_w
\ge\pi D+(1-2\pi)\|y-Y_B\|_w\ge\pi D,
\]
取y=Y_B达界；另一情形对称。用ν=P_A+P_B作支配测度积分：
\[
\boxed{B_M=\inf_fE\ell(f(Z_M),Y)=\frac D2(1-V_M).}
\]
实际风险R_M=B_M+a_M，a_M≥0为有限容量/训练利用差距，于是
\[
\boxed{R_G-R_Q=\frac D2(V_Q-V_G)+a_G-a_Q.}
\]
这直接使用实际轨迹范数，不依赖高斯假设。**未证明本项目V_Q>V_G或a_Q<a_G。**若先验不是各1/2，风险为D∫min{πp_A,(1−π)p_B}dν。

如果实际未来满足E||Y−Y_C||_w≤ε_Y，由反三角不等式，任意固定预测和其最优风险与原型问题都相差至多ε_Y。因此，以实际分布定义a_M，得到充分下界
\[
R_G-R_Q\ge\frac D2(V_Q-V_G)-2\varepsilon_Y+a_G-a_Q.
\]
误差原型只作解释，不构造新的主标签。真实类别混杂和类内差异大时下界可能无用；不能事后筛掉复杂场景让它变好。有限分类探针准确率不等于精确TV，两个探针下界顺序也不等于真实TV顺序。

### T09：局部可靠方向，而不是放大表示

在固定掩码和边集合的小扰动区域，X在x̄附近、误差协方差Σ≻0，表示一阶近似J_M(X−x̄)。两种情形均值差Δx，局部表示差J_MΔx，协方差J_MΣJ_Mᵀ。用伪逆定义
\[
d_M^2=\Delta x^TJ_M^T(J_M\Sigma J_M^T)^\dagger J_M\Delta x.
\]
令A=J_MΣ^(1/2)、w=Σ^(−1/2)Δx，通过SVD可知Aᵀ(AAᵀ)^†A是其行空间正交投影，所以
\[
\boxed{d_M^2=\|P_{row(A)}w\|^2\le\Delta x^T\Sigma^{-1}\Delta x.}
\]
等号要求任务方向w落在row(A)。标量读出乘c≠0时，分子/分母同乘c²，区分比不变。候选编码r'(x)=ω/[2s(1+(ωx/s)²)]，增大ω提高近原点斜率却可能加剧远端饱和；大梯度或大幅度不等于优势。

若另假设两个局部表示高斯、同协方差Σ_M，最优二分类错误为Φ(−d_M/2)，T08的原型风险为DΦ(−d_M/2)。量子期望有界，不严格服从无限支撑高斯，这一子模型仅作局部近似。Σ来自开发离线校准，不允许将测试逐样本真实残差供模型使用。

### T10：整个模型的净收益与全量发生率

在任意共同损失下，固定Z_Q、Z_G及Z_U=(Z_Q,Z_G)，定义B_M=inf_fEℓ(f(Z_M),Y)，则B_U≤B_Q,B_G。设G_{Q|G}=B_G−B_U、G_{G|Q}=B_Q−B_U、a_M=R_M−B_M，有
\[
\boxed{R_G-R_Q=G_{Q|G}-G_{G|Q}+a_G-a_Q.}
\]
因此Q新增信息也可能被丢失的信息和更大的学习差距抵消。拼接小头仅作诊断，不能精确估计B_M，也不加入主系统。

若E是预定义交互条件、占比π，则
\[
\Delta_{all}=\pi\Delta_E+(1-\pi)\Delta_{E^c}\ge\pi g-(1-\pi)b
\]
在Δ_E≥g、Δ_Ec≥−b时成立。一个精确构造的正例不说明π足够大，更不能保证全量净收益。

### T11：非线性余项必须真的改善任务

在训练后固定编码/局部混合/头，令全部跨车角度乘a，q(1)为完整图，q(0)为无跨车，q^[1]=q(0)+q'(0)。有
\[
r=q(1)-q^{[1]}=\int_0^1(1-a)q''(a)\,da.
\]
若共同头D在所比较域满足||D(q)−D(q')||_w≤L_D||q−q'||，则
\[
|\ell(D(q),Y)-\ell(D(q^{[1]}),Y)|\le L_D\|r\|.
\]
余项包含单边高阶和多边共同作用，不全部是纠缠贡献。内部替换可能使头输入分布变化，退化不等于独立重训GNN劣于Q；更不能最大化r来制造机制。F06同时记录真实预测变化及误差方向。

### T12：共有前端、覆盖与理论可迁移范围

共同前端X=F(Z)且预测不反馈，则任何只依赖共同X和参考的感知估计及输入有效性指标在Q/G之间一致。若不一致，先判为数据/筛选不同，不写Q改善了前端。

设I为是否形成可评分预测，共同未覆盖惩罚c相同，则
\[
R_G^{all}-R_Q^{all}=P(I=1)\{E[\ell_G|I=1]-E[\ell_Q|I=1]\}.
\]
共同输入可用率影响总体可评分比例，不能把后端预测改进解释为解决盲分离或匿名关联。

若源状态、随机流、静态预处理配置在≤t一致，每个回波窗口在t前结束，逐车参数估计仅用该车已分离观测，状态递推仅用到达量测，历史组装只读≤t记录，则逐阶段归纳得到X_t相同。未来标签只在监督/评价侧使用。这是因果契约的组合性证明，F01需检查实现是否符合。

### T13：新增聚合与原Q函数类包含

原聚合S=(1/7)Σ_j C_ji保留相关量的和；新增T=(1/7)Σ_j a_ji C_ji保留它与接近关系的加权对应。例如两边相关量为z、−z时S=0，权重为a、−a则T=2az/7。此例仅说明聚合算子的区别，未证明当前电路一定产生这些值、原其他21维不能区分它们，或它们对应不同真实未来；不能作为GNN误差下界。

将新Q每层w⁺第五分量设0、W_g⁺后三列设0，余下参数与任意旧Q一致，则两者所有合法输入的预测相同，故F_old⊆F_new。对任一固定损失，函数类最优风险不增；这是存在旧解的保证，不是训练优化或泛化保证。分别针对ADE/FDE的下确界不增，也不保证同一个J最优解两项同时改善。G保持原函数类，不能从Q函数类包含推导Q优于G。

新增a由旧状态确定，没有新增观测信息；它令单边角度能直接依赖平滑位置—速度内积，而不是只对原四维做线性组合。但不据此证明旧整图电路无法间接表达该关系。首层Pauli导数和配对构造仍成立；T03的非零下界只对应原未加权通道，新增权重可以为0或正负抵消，不能沿用该下界。

### T14：真实未来相关性与LLM利用必须分别取证

F06-E固定同一候选电路，比较本车历史＋原21维（补零至24维）与本车历史＋完整24维的小头未来预测。确认集改善支持新增读出在该模型与数据下的预测价值，不是精确TV、条件互信息或因果贡献。原21维在此来自新电路，不是旧Q完整版本。

F06-D则重训旧Q，与F05新Q使用同一当前数据、LLM和日程验证H2；H1由新Q与G在共同数据及同规格LLM条件下比较图分支。新Q只赢旧Q而不赢G，说明修订有增量但尚无相对G优势；只赢G而不赢旧Q，不能将优势归于本次修订；小头改善而完整LLM不改善，不能宣称信息已转化为最终收益。条件理论与先导探针都不替代完整ADE/FDE评价。

### 8.1 最终机制裁决，不再由辅助性质替代主比较

| 结论层级 | 本版结论 |
|---|---|
| 结构与任务连接 | 有符号关系→联合电路→可观测配对/多车项→时间预测，有明确构造 |
| 理论优势条件 | T08—T10给出可辨识/学习/全量收益条件，适用范围明确 |
| A02相对旧Q的严格结论 | T13保证旧函数可保留；不保证实际训练后误差下降 |
| A02新增预测价值证据 | T14对应F06-D/E，均未执行 |
| 当前条件是否成立 | 未知；没有真实输入分布、所实施GNN及训练后表示证据支持排序 |
| 可写贡献 | 提出并分析该参数化及同输入验证；真实结果后补相对所实施GNN的增量 |
| 不可写贡献 | 已解决普通GNN普遍瓶颈、量子不可替代、理论保证全量ADE下降 |
| 研究决定 | 当前Q候选有条件晋级，先执行有限F04；不无条件进入六次主训练 |

冻结协议不把用户要求的“优势依据”降为装饰。已找到的是正面构造及严格限制；实际比较缺口保持显式，F04用于最早检查该缺口，而不是用九个版本的篇幅掩盖它。



## 9. 开发选择、晋级与冻结规则

### 前置门槛G1：选择、确认与一次复核

**当前状态。**A04模型的A05等价执行提速与F03成本封存已完成，见reports/cost_profile.json及configs/training_budget.json。有效批量16、微批1，完整训练轮数沿用以下合同。F04尚未产生完整任务收益裁决，不记PASS。

**数据和先导模型。**选择使用按训练时间块均衡抽取的最多512个真实起点，标称20dB训练，V_select四档评估。Q/G均采用同规格2层因果Transformer小头（hidden128、4头、FFN256、dropout0，独立图投影，最后hidden→128→40），保留相同本车状态与标记旁路。先导时间模型与正式GPT-2不同，不能当完整主结果。两侧共用训练manifest、合法mask、SNR日程、损失和评价分母。

**有界选择。**开发seed=2025。Q仅A=(3图层,图lr3e−4)、B=(2图层,图lr3e−4)、C=(3图层,图lr1e−4)；G固定源2层、hidden128，仅学习率3e−4或1e−4两项，不复制第三项、不搜索结构。每项最多8epoch、耐心3，仅V_select选择和早停。相对J差≤0.5%取实测每步成本低者，再取参数少者，仍相同按固定配置顺序。F03若显示预算或训练充分性不成立，先记录共同停止规则修订再封存，不临场加候选。

**独立种子确认。**固定所选配置，seed={2023,2024}分别从头训练；Q/G均为各自图分支加同规格小头。两侧用同一最多2048训练起点manifest、同一四档SNR平衡日程、最多12epoch/耐心3，只用V_select早停；每真实起点每epoch仅一档条件。起点不足则用全部并报告。V_confirm只在模型固定后评价，不再选择结构或学习率。

定义r_s=(J_G,s−J_Q,s)/J_G,s；分母近零时记录退化比较。宏平均按共同场景/条件权重重算，不平均不同分母百分数。

| 状态 | 确认条件 | 后续动作 |
|---|---|---|
| PASS | 平均J相对改善≥2%，两确认种子ΔJ均>0，合并ADE/FDE各不恶化>0.5%，数据/数值/预算门通过 | 封存后允许F05六次主训练 |
| STOP | 两种子Q均更差且平均J恶化≥2%，或数据/梯度/成本门失败 | 归档原因，不启动F05 |
| REVIEW | 其余完整有效结果 | 仅允许一次预先封存的短复核 |
| INCOMPLETE | 日程未封存、缺运行或检查失败未解决 | 不判PASS |

**唯一灰区复核。**固定图配置与学习率，Q/G均使用同规格GPT-2/LoRA，seed={2023,2024}。每seed在共同最多2048训练起点上作本车预热最多2epoch，再从该共同本车检查点分别初始化图分支并联合3epoch；不复用小头权重、不新增图适配阶段。两侧同SNR日程、损失与评价规则，仅V_select早停，V_confirm作一次最终判定。采用同一PASS条件，未通过则暂停本周期完整训练；没有第二轮结构搜索。F03须先确认该对称复核预算可承担，不把未运行的门槛标为PASS。

2%、0.5%为工程投入规则，不是显著性门槛或预测收益。先导小头与正式Q时间模型不同，结果只用于有界投入决策，全部探索成本进入总账。

### 9.1 预算限制和开发数据权限

F04总预算上限取F03估计“剩余可用GPU小时×0.7”的20%；主训练、前端、复核和论文所需推理全列入总账。未用完预算不增添候选。预算无法完成对称确认时记录INCOMPLETE，不省去G的正常训练或声称PASS。一次灰区复核的预计开销必须预留。

训练/开发模型的测试标签不可访问；改过一轮确认后，该确认段只能称开发确认，不能称全新盲测。最终测试只在F05模型与配置hash封存后读。F04所选图层数/学习率作用于后续独立主训练；不复用与主种子混杂的探针结果。协议允许预定义选择，选择完成后另存selected_protocol.yaml，这不是无限开放的设计空间。

### 9.2 主研究终点和退出分支

F04未通过时不自动改任务、延长预测时间、加入量子独占信息、弱化GNN或只选择有利场景。可形成方法/理论/先导负面结果材料，但不能写完整精度优势。需要新候选时另建设计变更，旧测试不得当未用数据。

F05主比较失败也不能用T03正例、T08充分条件、无图消融或外部论文结果补成成功。统计与实际意义分别报告，见第10节。普通GNN效果相近是允许但不满足本项目优势目标的真实结论。



### 9.3 A02修订验证与任务相关性证据

**F06-D旧Q修订对照（必需）。**在F04 PASS、版本封存后，为seeds2026/2027/2028分别从头训练A01的四维边、21维读出旧Q；三层核心45个参数，两层30个，token27维。用新Q已选定的深度/图学习率、相同原始估计历史、100%合法训练数据、同seed共同本车预热、LLM规格、训练日程及V_select检查点规则。没有额外旧Q调参族。此处比较限定于共同日程，不用§1.2的旧六比特历史结果。三次旧Q加F05六次主训练，共9次核心训练；旧Q缺失时H2未完成，不把新Q赢G当修订证明。

**F06-E读出增量探针（必需的机制证据，非新主模型）。**固定F04选定的候选结构/学习率，表示准备seed2025，使用完整train、原F04两层小时间头、最多12epoch、耐心3、仅V_select早停。可复用满足该数据与选择权限的合法检查点；否则额外训练并计入表示准备成本。冻结候选电路，从相同样本提取21维与24维，均附相同本车历史和mask；21维补三个零输入，两个小头名义输入维数相同。各头使用2层因果Transformer、hidden128、4头、FFN256、dropout0，最后hidden→128→40预测真实未来修正；heads seeds2023/2024，每种子两头从头训练、最多12epoch、耐心3、仅V_select早停，共4次小头训练。V_confirm只用于最终成对评价，不选择新结构/读出。

候选表示的训练或选择不得使用V_confirm标签；若V_confirm已参与F04的PASS/灰区裁决，明确其开发曝光，F06-E是封存结构后的开发机制证据，不能叫全新独立盲测。E结果不触发新增调参或改主模型，独立完整证据留给F07。同时报告两个head种子、各时间块ADE/FDE；若只在训练集改善，或确认结果不一致，不写稳定新增预测价值。

F06-A的去门、一阶替换继续执行；可补新增读出关闭及同目标有效邻车间权重置换，保留状态/原Pauli矩以检查聚合配对的依赖，但扰动具有分布变化，不能替代D的重训或证明量子独占性。F06-B仅在主张LLM增量时必需：Q/G各3seed小Transformer对照，共追加6次训练；不默认对旧Q追加该组。F06-C的50%曲线仍可选。预算优先顺序为F05、F06-D、F07、F06-E/A、主张LLM作用所需F06-B，F06-C最后。

## 10. 评价、统计与论文主张

### 10.1 起点身份与合法评分集合

每个预测起点采用预先假设已知的车辆对应组织估计输入与监督，未来20步始终评价同一对象，不以预测误差重新分配标签。对应只在路由元数据/监督侧使用，模型不读取源ID。不存在5m真值绑定、ID纠错或虚假确认航迹门。

记录源队列起点存在数、合法估计输入数、历史不足数、数值失败及未来标签删失。Q/G的输入有效mask、车辆对应和可评分分母必须完全相同；未来标签缺失不改变输入对象。已知对应是任务假设，不作为算法成功解决关联的证据。

### 10.2 主损失的精确聚合

对场景s中起点按已知对应确定、存在有效未来参考的目标i，记V_si为有效未来步集合：
\[
ADE_s=\frac1{|I_s|}\sum_{i\in I_s}\frac1{|V_{si}|}\sum_{h\in V_{si}}\|\widehat p_{sih}-p_{sih}\|_2.
\]
FDE_s只在第20步参考存在的目标中平均；没有末步有效目标的场景对FDE标NA并单独计数。先在每个SNR条件内按有效场景取均值，再对四档SNR等权宏平均，J=ADE_macro+0.5FDE_macro。Q/G使用完全相同的分母、侧车mask和场景列表。

不因模型误差大、输出为NaN或Q耗时长删除场景；无输出作为执行失败记录，修复前该运行不产生合格主成绩。目标离开基站FoV但源数据仍有未来参考时继续评分；源记录截断的未来不能臆造，删失比例及可评分步数分布同时公开。

### 10.3 三张结果表

**表A：共有感知前端。**逐原始符号SNR报告距离/径向速度等实际估计参数误差、最终位置/速度RMSE、数值失败数及输入可用率。评价直接沿用已知对应，不执行5m匹配；不报告本实验未验证的检测精确率、ID切换、匿名GOSPA或虚假确认指标。

**表B：主预测。**QGNN和GNN各三独立种子，报告各SNR及宏平均ADE/FDE/J与成本；本车参照单列。修订验证子表加入旧Q三seed同条件重训，F06-E小头探针另列，A02统计与训练矩阵不变。

**表C：输入与监督可用性。**逐条件报告当前队列对象、可用估计历史、历史不足/数值失败、各未来步label_valid及FDE可评分对象数。共同分母必须可追溯，不把噪声重复当新交通样本，不以标签删失改变输入。停止使用旧匿名链路的固定身份漏检/虚警截断代价；主ADE/FDE不截断。

### 10.4 难场景、诊断与样本效率

保留v0.5预声明分组：停车/起步（参考历史速度<0.5m/s持续≥0.5s）、近距（历史末帧任意车对<10m）、转向（历史方向变化≥15°且速度≥2m/s）、不利测速几何（UᵀR⁻¹U条件数>100）。全部用t及之前参考历史/几何作评价侧标签，不给模型；分组可重叠，报告大小和全量成绩，不事后选择新主组。

标称原始符号SNR 20dB上的额外20dB功率衰减只作预声明压力补充。理想状态D0可作oracle诊断；旧理想关联D1/匿名D2划分不再用于当前主线。主结果明确标注已分离且对应已知的逐车感知假设。输入退化与各自重训分别报告。

50%完整训练时间块与100%曲线为可选解释；未执行不能主张样本效率获证。100%始终为主比较，不用删数据制造Q优势。

### 10.5 主要比较、修订确认家族与配对统计

主要比较仍为ΔJ=J_G−J_newQ，正值为新Q更好。训练seed={2026,2027,2028}，新Q、G、旧Q按对应seed独立重训并保持配对，模拟噪声流共同且与训练seed分开。

以60s原始时间块聚类：同块的锚点、重叠窗口和各SNR一起抽样，episode不跨统计块。双维配对bootstrap共2000次、统计seed=9100：分别重采样3个训练seed索引及原始时间块索引，保留各抽中组合的Q/G配对和所有条件；每次从每条件的误差和/计数重算宏平均，不把不同样本数的百分数直接平均。

对主要ΔJ保留95%双侧百分位区间。A02另预声明四项确认性差异：ADE_G−ADE_newQ、FDE_G−FDE_newQ、ADE_oldQ−ADE_newQ、FDE_oldQ−FDE_newQ。共用上述配对重采样，三版本在抽中块和seed上对齐；每项以bootstrap分布1.25百分位作98.75%单侧下界，Bonferroni分配家族α=0.05/4。下界>0才支持对应单项有统计支持的正向改善。新Q对G两项均通过才写相对G两指标均有支持；新Q对旧Q两项均通过才写本修订两指标均有支持。四项是固定完整家族，缺旧Q不能缩小家族阈值后挑结论。

J的95%区间只支撑主要组合损失结论，不用它替代四项确认家族，也不宣称J加四项的任意五项联合主张具有统一5%错误率。逐SNR、难场景、小头及其他未纳入指标为描述或探索性结果；新增正式检验需测试前记录。以上为预声明统计方案，Bonferroni控制依赖单项区间校准；小样本bootstrap不保证精确名义覆盖。

三个训练seed只刻画有限优化随机性，不等于训练样本方差。测试若只有约6个60s块，bootstrap区间可能不稳定；必须同时提供逐seed/逐block原始差和30/120s块长敏感性，不把数千窗口当独立观测、不保证名义95%具有精确覆盖，不宣称跨日/跨城市泛化。

### 10.6 结果用语和工程门槛

工程标准继承F04：平均J相对改善≥2%，ADE/FDE各不恶化>0.5%，成本可承担；这不是理论预言或已实测最小业务价值。

| 实際结果 | 允许结论 |
|---|---|
| ΔJ的95%区间下界>0，且点估计达到工程门槛 | 本回顾性同输入协议下有统计支持的净预测增量；2%是点估计，不声称区间下界也≥2% |
| 区间下界>0，但点估计未达2% | 小幅统计差异；未达预设投入目标 |
| 点估计达2%，但区间跨0 | 观察到方向信号，未建立稳健优势 |
| Q更差或指标取舍明显 | 如实报告未通过/取舍，不用条件定理替代 |
| 缺种子/缺块/执行失败 | 不完成对应稳定性主张，不能假装完整主比较 |

两个单项都改善才写“两个指标均改善”；组合J更好不能隐去某个单项退化。区间跨0不等于等效。前端无反馈时不写Q降低ID切换。模拟性能增量不叫真实量子硬件加速。

## 11. 成本、资源与时间安排

### 11.1 算术成本，不冒充实测

16比特单个complex128态为16×2^16=1,048,576字节=1MiB。每块4N编码门＋4N局部旋转＋N车内门＋N(N−1)跨车门；N=8时128个抽象门，L3共384个。T20、后验采样关闭，每场景20次整图调用，不是8倍目标重复。朴素逐门保存所有态约384×20MiB=7.5GiB/场景，未计读出、LLM输入反向与优化器；这只用于说明单态内存不等于训练峰值。

首层参考可用T04经典公式核对。A04生产使用PennyLane可微状态演化和PyTorch输入反向；求导方式及重计算须以正式后端实测记录为准，不能把未实施的伴随或参数移位写成运行事实。新Q三层48个共享核心参数多处出现，不能默认每参数仅两次全电路求值。冻结LLM仍需输入梯度，阶段B成本不可按纯推理计。

A02不增加门数或Pauli种类，但增加接近特征和三个加权和的经典计算/反向。新Q相对旧Q：三层核心新增3个标量，W_g新增768×3=2304个权重，整模型共新增2307个可训练参数；两层新增2+2304=2306个。A04的GNN为§7.1图核心适配加共同时间模型，参数量须重新统计。F03须将24维与旧21维各自的真实成本、F06-D三次完整训练、F06-E四次小头和必要表示准备计入，而非把九次核心训练叫全部预算。

A03每车每站的基本符号观测为K×N_s=256×256复数样本；若用complex64，单矩阵0.5MiB，三站每车1.5MiB，最多8车为12MiB/时刻（不含额外中间量及审计数据）。完整生产仍采用逐车流式生成与估计，不沿用旧阵列立方体的9.89TiB算术。实际峰值显存、耗时及落盘大小另测，当前快照验收不等于完整缓存预算。

### 11.2 实施模块与资产事实

建议模块为frontend/{scene_manifest,symbol_level,run_symbol_frontend,check_symbol_level}.py、datasets/export_inputs.py、models/{qgnn_core,qgnn_readout,gnn,numeric_temporal}.py、evaluation/{known_correspondence,metrics,statistics}.py、theory_checks/、experiments/。其中前端已落实部分以F00/F01报告为准，当前模型位于prediction/；A04图核心适配与共同时间接口已验收通过（70项），正式训练入口与成本仍待后续阶段。

任务书提到E:\\NJUPT\\ICCT会议和历史/home/js_cn/sensing；A01记录当前实践路径为/home/dell/YrM/ICCT，F00/F01-A已完成情况以amendment_log.md及reports/f01a/为准。历史A02只修改模型协议；当前A03v4前端缓存已经验收通过，未进行预测训练；后续需核对实际模型模块、权重及运行资产。禁止对未知目录直接运行历史绝对路径脚本。

### 11.3 两卡与总预算

默认两卡独立运行：一张安排Q相关任务，另一张安排GNN或前端；只有确认同机互连后才可分配Q与LLM跨卡串接。每卡各24GB，不按48GB单卡。

F03先记录每CPI、每scene训练步、前/反向、读出、加载和LLM的耗时及峰值显存，预热后100—200步。总账
\[
C_{total}=C_{front}+C_{checks}+C_{dev}+C_{confirm}+C_{review}+C_{warmup}
 +\sum_{M,seed}n_{steps}\,t_{step}+C_{attr}+C_{eval}+C_{paper}.
\]
墙钟秒、GPU小时、CPU小时和存储分别记账；并行作业耗时不能简单相加后当作截止日期，CPU时间也不能记成GPU小时。按实际可工作D天、每卡每日h小时得容量2Dh；计划最多使用70%，保留故障重跑空间。未测量前GPU小时和每epoch时间为null，不填虚构数字。F04上限为这部分可计划容量的20%，且不能侵占F05/统计必需预算。

### 11.4 倒排目标（非耗时保证）

| 窗口 | 目标 |
|---|---|
| 9月12—14日 | F00/F01共用资产与数据；F02结构检查可并行；方法与理论正文同步形成 |
| 9月15—17日 | F03成本、F04小头选择/确认及至多一次灰区复核；通过后封存执行配置 |
| 9月18—21日 | F05六次主训练＋F06-D旧Q三次；F06-E/A机制证据，LLM作用主张需要时另作时间对照 |
| 9月22—25日 | F07锁定统计、图表、论文初稿；核对代码/配置/公式一致性 |

前置延误时按依赖顺延并重算预算，不隐去；优先取消可选样本效率/额外压力分析，不能退回真值ID加噪、删GNN或少报失败来满足日期。若F04未通过，本周期不启动F05，转为明确的研究裁决材料。

## 12. 论文证据、图表与最终交付

| 主张 | 理论 | 执行记录 | 图/表 |
|---|---|---|---|
| 合法三站逐车估计输入 | P01、T12 | F01输入hash/因果/参数与状态误差 | F01系统图，表A |
| 编码与有向图操作设计正确 | T01—T06 | F02数值/梯度/重排/填充 | F02模块与读出图 |
| 量子关系对任务有作用 | T03、T11 | F06去门/一阶替换与标签误差 | F04机制图 |
| 当前协议下Q对G净收益 | H1、T08—T10条件 | F05＋F07完整比较，不能由探针替代 | F03主指标，表B |
| A02相对旧Q修订有效 | H2、T13/T14 | F06-D＋F07，旧新Q均在当前数据独立重训 | 表B修订验证子表 |
| 新增读出与真实未来有关 | T14 | F06-E固定电路21/24小头确认；不是完整LLM成绩 | 机制表 |
| 输入与监督可用性 | T12 | F07共同有效mask/删失/分母 | 表C |
| LLM额外作用 | 第7节、R08/R09 | F06时间头对照，区分容量与预训练 | F05时间模块 |
| 可执行成本 | 第11节算术 | F03及全程实测 | F06资源与失败案例 |

所有图表保留原始CSV/JSON和绘图代码；未执行的图不填分数、不造曲线。机制构造与oracle图只在标明条件的附图中，不进入真实主成绩表。论文标题可使用“有向联合QGNN”或“量子图关系建模”，不能预写“已实现量子优越”。会议届次/模板/投稿时间属于执行中的独立事实核验，不把9月25日项目目标当官方截止。

### 12.1 历史版本兼容与本版唯一入口

v0.5的三主模型、九次训练及双经典主比较规则不恢复；A02的九次是新Q/G六次主训练加旧Q三次修订对照，含义不同。v0.8特定平均聚合碰撞仅保留其原假设，不支撑整个GNN比较；v0.9风险判据纳入T08—T10。A01源划分、队列及共同几何继续复用，旧匿名感知结果仅作历史；其四维边/21维Q作为F06-D对照，新主Q采用A02内部五维边/24维读出。旧附件与优化修订稿保留为来源，未同步条目不再具有执行优先权；未来实施只以两份1.4文档及其一致配置为准。参考文献保留真实标题。

冻结的是本版设计、数据权限、有限选择和否定规则。最终selected_protocol.yaml及config/code/data哈希在F04结束、首次最终测试之前另封存；本轮没有假定那些运行事实已经存在。

## 13. 来源与复核记录

### 13.1 用户与会话来源（S）

- S0：《GPT-6-pro研究交接与深度调研任务书.md》（内标题ICCT论文研究交接与深度调研任务书），2026-09-11，§2边界、§4事实、§5优势链、§7交付。
- S1：用户本对话后续决定：最多8车固定；仅讨论不执行；参考优先近三年；QGNN核心；普通经典对照且名称仅GNN、具体实现开放。
- S2：v0.5《研究方案书_完整整合审议稿》共同前端、数据和统计设计；本版显式更新的规则优先。
- S3：v0.3—v0.4的首层精确式、零通道、多层一阶及重编码分析；作为纸面命题保留，数值校核未执行。
- S4：v0.8配对构造及v0.9含噪风险判据；原对照限定被后续命名/实现决定收窄，不保留过度外推。
- S6：2026-09-12用户要求PennyLane作为正式版本，主要baseline适配师兄Plain图核心并保留共同LLM，展示名称仅QGNN/GNN；A04据此替换实现合同及训练规则，新增连接/表示宽度隔离实验仍取消。
- S5：2026-09-12本任务用户确认优化、要求经典GNN保持冻结实现、询问真实未来与LLM证据，并授权更新两份权威文档；设计来源为《QGNN预测优势优化修订稿.md》，A02合并后的规范以本文与ROADMAP为准，授权不被表述为已运行实验。

### 13.2 原始文献与精确标识（R）

[R01] Gilmer et al. Neural Message Passing for Quantum Chemistry. ICML 2017, PMLR 70. 基础框架例外。https://proceedings.mlr.press/v70/gilmer17a.html

[R02] Ye et al. On Designing General and Expressive Quantum Graph Neural Networks with Applications to MILP Instance Representation. ICLR 2025. https://proceedings.iclr.cc/paper_files/paper/2025/hash/ab59d149fc0c2c9039d3e3049f7914b1-Abstract-Conference.html

[R03] Jaderberg et al. Let quantum neural networks choose their own frequencies. Physical Review A 109, 042421 (2024), published 2024-04-22; early author manuscript 2023-09-06. DOI:10.1103/PhysRevA.109.042421. https://journals.aps.org/pra/abstract/10.1103/PhysRevA.109.042421

[R04] Rodriguez-Grasa, Ban, Sanz. Neural quantum kernels: Training quantum kernels with quantum neural networks. Physical Review Research 7, 023269 (2025), published 2025-06-16. DOI:10.1103/xphb-x2g4. https://journals.aps.org/prresearch/abstract/10.1103/xphb-x2g4 ; 方法与经典比较作者稿：https://arxiv.org/html/2401.04642

[R05] Shin, Sweke, Jeong. Quantum kernels through the lens of entangled tensor kernels. Physical Review Research 8, 023181 (2026), published 2026-05-19. DOI:10.1103/c53t-rybw. https://journals.aps.org/prresearch/abstract/10.1103/c53t-rybw ; https://arxiv.org/html/2503.20683

[R06] Bowles, Ahmed, Schuld. Better than classical? The subtle art of benchmarking quantum machine learning models. arXiv:2403.07059 (2024). 此处按作者稿，不虚构正式发表状态。https://arxiv.org/abs/2403.07059

[R07] Liao et al. MFTraj: Map-Free, Behavior-Driven Trajectory Prediction for Autonomous Driving. IJCAI 2024, 5945–5953. DOI:10.24963/ijcai.2024/657. https://www.ijcai.org/proceedings/2024/657

[R08] Lan et al. Traj-LLM: A New Exploration for Empowering Trajectory Prediction with Pre-trained Large Language Models. arXiv:2405.04909 (2024)，仅承接方法参考。https://arxiv.org/html/2405.04909 ; GPT-2固定配置来源：https://huggingface.co/openai-community/gpt2/blob/1a43f7d38310adb1a52f267670dc678fafaa3db2/config.json

[R09] Tan et al. Are Language Models Actually Useful for Time Series Forecasting? NeurIPS 2024. https://proceedings.neurips.cc/paper_files/paper/2024/hash/6ed5bf446f59e2c6646d23058c86424b-Abstract-Conference.html

[R10] Xiao et al. A Novel Joint Angle-Range-Velocity Estimation Method for MIMO-OFDM ISAC Systems. IEEE Transactions on Signal Processing 72, 3805–3818 (2024), published 2024-08-13. DOI:10.1109/TSP.2024.3442886. https://ieeexplore.ieee.org/document/10634583/ ; https://arxiv.org/abs/2308.03387

[R11] Bai, Ge, Wymeersch. Belief Propagation-based Target Handover in Distributed Integrated Sensing and Communication. arXiv:2506.23118 (2025). 本版按作者稿标识，不继承未重新核对的会议录状态。https://arxiv.org/abs/2506.23118

[R12] Rahmathullah, García-Fernández, Svensson. Generalized optimal sub-pattern assignment metric. FUSION 2017. DOI:10.23919/ICIF.2017.8009645. 指标奠基例外。https://arxiv.org/abs/1601.05585 ; https://isif.org/media/generalized-optimal-sub-pattern-assignment-metric

[R13] FHWA. Lankershim Boulevard Dataset, FHWA-HRT-07-029, January 2007. 数据来源例外。https://www.fhwa.dot.gov/publications/research/operations/07029/index.cfm ; 字段定义：https://ops.fhwa.dot.gov/publications/fhwahop08054/sect7.htm

引用只支持第3节对应判断；其他数据/电路上的优势不自动属于本模型。数学构造和本版工程阈值分别标为T/设计，不冒称来源已有结论。

[R14] Wei等（2024），Symbol-Level Multi-BS Sensing论文。当前前端主依据为项目保存的原始PDF：`docs/references/Wei2024_symbol_level_multi_BS.pdf`；具体标题与书目信息以该原文为准，不将项目K256写作论文原始K128配置。
