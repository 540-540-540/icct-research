# 代码索引与复现说明

## 1. 执行环境

- 权威工程：`/home/js_cn/sensing`
- SSH别名：`02`
- GPU：NVIDIA GeForce RTX 4090
- PennyLane环境：`/home/js_cn/sensing/venv_pennylane`
- 量子后端：PennyLane 0.31.1 `default.qubit`，`shots=None`

所有训练、评估、数据处理和绘图应在远端执行。本文件夹是论文归档快照，部分脚本使用远端绝对路径，不能直接在Windows本机运行。

## 2. 代码目录

### `code/01_classical_graph_llm`

早期经典基线与Graph＋LLM：

- `target_interaction_graph.py`：目标交互GNN；
- `motion_tokenizer_v7.py`：运动词和软token；
- `MultiTargetTimeLLM.py`：多目标LLM模型；
- `run_multitarget_graph_llm.py`：Graph＋LLM训练；
- `run_multitarget_comparisons.py`：GRU/LSTM/TCN/Transformer/GNN对比；
- `run_multitarget_ablation.py`：经典LLM组件消融；
- `run_multitarget_snr_evaluation.py`：多SNR评估；
- `plot_multitarget_snr_results.py`：早期论文结果图。

### `code/02_pennylane_qgnn_core`

PennyLane QGNN主实验和诊断：

- `pennylane_core.py`、`quantum_core.py`：量子关系核；
- `physics_aligned_dual_model.py`：物理对齐双层QGNN；
- `model.py`：QGNN＋LLM基础模型；
- `run_physics_aligned_dual_experiment.py`：双层QGNN/经典匹配训练；
- `run_quantum_llm_frozen_continuation.py`：冻结QGNN后的LLM训练；
- `run_final_test_report.py`、`evaluate_test.py`：最终测试；
- `run_mechanism_ablation.py`：纠缠、深度和重上传消融；
- `benchmark_complexity.py`：参数量与推理时间；
- `bootstrap_complexity_strata.py`：复杂度分层统计；
- 其余`run_*`、`analyze_*`、`diagnose_*`文件记录所有替代核、失败路线和瓶颈诊断。

### `code/03_qgnn_llm_interfaces`

后续QGNN与LLM衔接实验：

- `future_token_model.py`、`run_future_token_qgnn_llm.py`：因果未来token；
- `attention_routed_fusion_model.py`：QGNN注意力路由LLM状态；
- `interaction_trust_model.py`、`run_effective_segment_trust.py`：有效关系三阶段主方案；
- `intent_token_planner_model.py`：分阶段意图规划器；
- `run_long_horizon_qgnn_llm.py`：4秒预测；
- `run_llm_component_ablation.py`：运动token、图token和future query消融；
- `analyze_llm_component_ablation.py`：配对时间块bootstrap；
- `temporal_pair_refinement_model.py`及相关runner：边级未来交互诊断。

### `code/04_quantum_association`

- `model.py`：量子/MLP关联消息分支；
- `run.py`：训练和固定开发验证；
- `summarize.py`：12条件结果汇总；
- `launch.sh`：远端启动脚本。

## 3. 复现顺序

推荐依次复现：

1. Plain GNN、匹配经典双层GNN和物理对齐双层QGNN；
2. 冻结所选图检查点，训练QGNN＋LLM；
3. 训练future-token分支；
4. 训练有效关系三阶段信任接口；
5. 在固定开发集选择checkpoint；
6. 运行全开发集、困难场景分层和bootstrap；
7. 所有模型和协议冻结后再运行独立测试；
8. 多训练种子时每个种子必须完整重复上述流程。

## 4. 数据、权重和实验产物

`cloud_snapshot/home_js_cn_sensing/`保留了远端工程中与本课题有关的可直接展开内容：

- `Lankershim_Vehicle_Trajectories.csv`：原始轨迹数据；
- `data/`：两个多目标NPZ数据集；
- `dataset_lankershim_clean_v1/`：清洗后的逐场景数据；
- `results/`、`diagnostics/`、`preserved_experiments/`：检查点、评估输出、诊断和保留实验；
- `gpt2/`：本地GPT-2配置、tokenizer和权重；
- `logs/`：训练日志；
- `figures/`、`paper_figures/`、`paper_figures_nature/`、`newnew_figure/`、`assets/`：全部论文图和中间图；
- `root_documents/`：云端根目录的六份中文研究记录；
- 根目录Python文件及两个PennyLane离线wheel。

`cloud_bundles/`保留完整压缩包：

- `icct_revision_unified_20260907.tar.gz`；
- `icct_revision_r1c1_20260907.tar.gz`；
- `icct_root_files.tar.gz`。

其中`revision_unified_20260907`含超长检查点路径，不应直接在Windows目录展开；请在Linux上恢复。

## 5. 环境恢复

环境快照位于`environment/`：

- `requirements_pennylane_freeze.txt`：PennyLane环境的完整包版本；
- `requirements_main_freeze.txt`：主环境的完整包版本；
- `remote_environment.txt`：Python、系统和GPU环境摘要。

远端Linux虚拟环境本身未复制，因为它约5.6 GiB、含绝对路径和平台二进制，复制到Windows不能直接运行。归档保留了可重建环境所需的版本清单，根快照还包含`PennyLane-0.31.1-py3-none-any.whl`与`autoray-0.6.3-py3-none-any.whl`。

在Linux恢复压缩包示例：

```bash
mkdir -p /home/js_cn/sensing_restore
tar -xzf icct_root_files.tar.gz -C /home/js_cn/sensing_restore
tar -xzf icct_revision_unified_20260907.tar.gz -C /home/js_cn/sensing_restore
tar -xzf icct_revision_r1c1_20260907.tar.gz -C /home/js_cn/sensing_restore
```

代码来自不同实验阶段，部分脚本仍使用远端绝对路径。`MANIFEST_SHA256.txt`记录最终本地归档的文件校验值，`LOCAL_DIRECTORY_VERIFICATION.json`和`KEY_FILE_VERIFICATION.json`记录源端与本地核验结果。
