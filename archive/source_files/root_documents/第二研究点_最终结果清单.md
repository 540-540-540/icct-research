# 第二研究点最终结果清单

更新时间：2026-08-31

## 1. 保存位置

- 远程权威项目目录：`/home/js_cn/sensing`
- 本机同步目录：`C:\Users\22274\Desktop\LLM2\remote_second_point`

## 2. 最终实验结果

- `comparison_snr_results.json`：全部对比模型在 5、10、15、20 dB 下的原始结果；
- `ablation_snr_results.json`：六组严格消融在四档 SNR 下的原始结果；
- `snr_calibration.json`：SNR 与位置、速度观测噪声的映射；
- `trajectory_examples_comparison.npz`：代表性四目标场景的干净历史、真实未来以及 LSTM、Transformer、Target GNN、Proposed 预测；
- `trajectory_example_model_metrics.csv`、`trajectory_example_model_metrics.json`：代表性场景的 ADE/FDE；
- `trajectory_selection_candidates.csv`、`trajectory_selection_report.json`：代表性场景的客观选择依据。

## 3. 最终论文图

目录：`figures/second_point_snr`

- `fig_snr_model_comparison.*`：多 SNR 对比实验图；
- `fig_snr_ablation.*`：多 SNR 消融实验图；
- `fig_snr_trajectory_prediction_comparison.*`：`2 × 4` 分目标轨迹对比图，两行对应 5/20 dB，四列对应四个目标；采用局部位移坐标，不显示观测噪声和车辆编号，方法由颜色、线型和稀疏标记共同区分；
- `fig_snr_trajectory_scene_overview.*`：全局多目标场景概览，只显示 Clean history、Ground truth 和 Proposed；
- 每张图均保留 PDF、SVG、600 dpi PNG、灰度预览和绘图预览；
- `comparison_snr_metrics.csv`、`ablation_snr_metrics.csv`、`proposed_gain_vs_gnn.csv`：三张图的整理后数据；
- `figure_data_metadata.json`、`visual_qa_report.json`：绘图元数据与版面检查记录。

## 4. 关键复现脚本

- `run_multitarget_snr_evaluation.py`：多 SNR 对比评测；
- `run_multitarget_ablation.py`、`evaluate_multitarget_ablation_snr.py`：消融训练与评测；
- `select_representative_trajectory_scene.py`：代表性场景选择；
- `export_representative_scene_baselines.py`：导出固定场景的各方法预测；
- `prepare_multitarget_snr_figure_data.py`：整理绘图数据；
- `plot_multitarget_snr_results.py`：生成最终论文图和灰度预览。

## 5. 已清理的旧结果

- `trajectory_examples.npz`：最初轨迹导出，场景表现不具有代表性；
- `trajectory_examples_revised.npz`：仅含 Target GNN 与 Proposed 的过渡版本；
- `trajectory_diagnostics.json`：旧轨迹图问题诊断中间结果；
- `fig_snr_trajectory_prediction.*`：第一版轨迹图；
- `fig_snr_trajectory_prediction_revised.*`：包含 Noisy observations 且缺少完整对比方案的过渡版轨迹图。

数据集、训练 checkpoint、最终对比/消融结果和代表性场景选择依据均未删除。

## 6. 最终质量检查

- 图内文字：英文；
- 字体：Times New Roman；
- 图内标题：无；
- 分目标轨迹图 PNG：4296 × 2850，600 dpi；
- 其余双栏图 PNG：4296 × 2070 或对应设计尺寸，600 dpi；
- PDF：嵌入 TrueType 字体，不含 Type 3 字体；
- 彩色和灰度预览：方法可通过线型和终点标记区分；
- 当前实验为单随机种子点估计，不能表述为统计显著优于。
