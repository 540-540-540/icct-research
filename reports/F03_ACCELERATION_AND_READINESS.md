# 训练提速与启动进度

更新时间：2026-09-12 23:45（北京时间）。权威运行目录：`/home/dell/YrM/ICCT`。

## 实测提速

相同真实训练起点顺序、有效批量16、微批1，预热后5步：QGNN整步中位数由198.328秒降至20.302秒，约9.77倍；峰值显存8.24GiB。该结果是短样本成本证据，不是长期稳定吞吐或预测性能结论。

实施了按活跃节点数合批历史帧、最多20帧广播计算和按输入规模选择重计算。PennyLane后端、量子数学、双精度与输入权限保持一致。输出、输入梯度及参数梯度对照最大误差4.63e-14；含空帧、不连续槽位、完整8车、两种图深度及旧Q21。

验证阶段在同一次评估中按完整状态与存在掩码精确去重，不跨权重更新复用。80个真实验证起点的图计算由23.90秒降至11.33秒；整模型逐场景ADE/FDE与未缓存路径差为0。

## 训练入口与验收

`run_f04.py`已具备5个候选选择、4次独立种子确认和最多一次灰区复核；仅PASS后封存正式配置。`run_f05.py`已具备三种子共同预热、图适配、联合训练、四档验证、检查点、断点恢复及防重复写入。

共享训练10项CPU检查通过；真实GNN图适配、Q/G小头前后向、量子验证缓存等价检查通过。训练损失按场景有效点归一化，评价按目标、场景与SNR宏平均，二者有意分离。代码、训练/开发数据、规范化、GPT-2权重和检查点来源在封存与恢复时检查。

## 当前运行

F04已于23:42启动，两卡独立分派候选；日志`reports/f04_execution.log`，进程记录`reports/f04_launcher.json`，进度`results/development/f04/`。尚未获得F04最终结果，F05未启动。只有F04 PASS且正式入口检查成功，才交付可运行的主训练命令。

当前预计全部必需工作268.02 GPU小时，预留413 GPU小时，其中F04预留48 GPU小时（含灰区复核9小时）。估算包括最大轮数、多种子、旧Q对照和必要机制证据；提前停止及验证去重可能减少实际开销。用户确认两卡可持续使用；13日仅为推算预算容量的规划周期，并非用户截止时间。

## 主要证据

- `reports/cost_profile.json`、`configs/training_budget.json`：成本与预算。
- `reports/f03/optimized_*`：五种模型/头配置的真实短测。
- `reports/quantum_speed/summary.md`、`reports/evaluation_cache/summary.md`：等价性与提速。
- `reports/training_checks.json`、`reports/f05_training_smoke.json`、`reports/f05_quantum_evaluation_smoke.json`：训练与缓存验收。
- `reports/preflight_decision.json`：F04最终裁决，以实际运行后文件内容为准。
