> 2026-09-12 用户最新决定：取消本次讨论的新增连接方式、24维与128/256/512/768宽度隔离实验。本稿仅保留为历史分析，不作为执行任务。当前只追加PennyLane同电路输出与梯度交叉验证。

# 跨车连接是否限制预测：隔离实验方案（审阅稿）

日期：2026-09-12。依据：当前《研究方案书.md》与《ROADMAP.md》1.2（A02），尤其§6.3—6.5、T03—T14及F06-D/E。

权限与状态：用户授权理论分析和准备可审阅实验方案；尚未授权新增训练、模型代码修改或覆盖权威协议。本稿不改变A02的执行范围；全部新实验均未执行。文中成本为试验次数和门数的设计算术，不是实测GPU小时。

## 1. 判断：问题成立，但“没有直接VV门”不是已证实的瓶颈

当前有向边采用

\[
U_{j\to i}=\exp[-ig_{ji}Z_{P_j}X_{V_i}/2].
\]

P、V是初始位置/速度编码的通道名，经过车内混合和多层演化后并非各自只含位置或速度；Pauli算符也不是经典位置、速度或加速度算子。

邻车B的速度至少存在三类路径：

1. 进入相对速度和接近特征，改变g_BA及g_AB，这是经典角度控制路径。
2. 图门并非单向通信。反向标号的门Z_PA X_VB也能使A的位置通道读出依赖B的速度通道；再经A车内混合影响V_A。
3. B车内P/V混合可使后续P_B携带B速度信息，再通过下一图块影响A。

因此“VB没有出现在Z_PB X_VA中”不等于“模型看不到VB”。但这些路径可能压缩过强、选择了不合适的Bloch分量，或需要多层才能有效利用；路径存在也不证明预测足够。需要对实际分布和实际读出验证。

一个可分析例子：乘积输入、单门角度为0时，原边有

\[
\partial_g\langle Z_{V_A}\rangle
=\langle Z_{P_B}\rangle\langle Y_{V_A}\rangle.
\]

首次编码中〈Z_PB〉=−sin(r_xB)，它并不单独包含y_B；y_B仍可通过边角度、其他观测量和多层路径出现。同理，反向标号门Z_PA X_VB对〈X_PA〉的一阶响应为−〈Y_PA〉〈X_VB〉，所以量子态中的VB也不是完全无效。以上为固定输入与局部角度的算符推导，不是实际损失梯度或ADE/FDE增益保证。

## 2. 推荐：两个核心连接版本，加一个有界算符对照

首选只检验“第二图块的通道分配是否值得改变”。若还要区分算符轴本身，再加入一个固定对照；首轮最多三个版本，不搜索所有Pauli组合。

记H_PV=Z_Pj X_Vi。量子块数使用A02已选定的共同L∈{2,3}，始终只改变第二图块；L=3时是中间块，L=2时是最终块，分层解释，不把两种深度结果混成一组。

| 版本 | 第一图块 | 第二图块 | 第三图块（若L=3） | 回答的问题 |
|---|---|---|---|---|
| A：A02参考 | Z_Pj X_Vi | Z_Pj X_Vi | Z_Pj X_Vi | 当前结构的匹配参考 |
| B：通道替换 | 与A相同 | Z_Vj X_Pi | 与A相同 | 固定ZX形式时，将第二块源/目标编码通道交换是否有效 |
| C：算符对照 | 与A相同 | X_Pj X_Vi | 与A相同 | 固定P→V支持时，将第二块源算符Z改为X是否有效 |

**最低必要组是A/B。**C仅在要区分通道变化和Pauli轴选择时加入，最多一个；不默认再运行B的XX版本。三组可以估计以A为参照的两个条件差异，不能估计“通道×算符”的完整交互效应，也不能由C失败推出所有其他算符都差。

各组均用完全相同的g_ji函数、接近特征、边集合、Γ界与角度归一化。每个替换图块内部仍相互对易：A仅用P上的Z/V上的X，B仅用V上的Z/P上的X，C只用X。因此不会因车辆槽位决定的门执行顺序而额外破坏置换等变性。

B不是直接速度—速度连接实验，应称为“交替P/V通道分配”。如果把全部P/V编码、门、局部操作和读出一起交换，只是重命名，不算新架构；本实验固定编码、局部操作、读出，仅替换第二图块，避免该无效对照。

C是有意的轴选择对照，不预设X更有物理意义。编码理论说明编码与门结构影响可表达函数；它并未证明某个轴天然更适合轨迹预测。[Schuld等，PRA 2021](https://arxiv.org/abs/2008.08605)

## 3. 必须固定的内容与读出可比性

| 项目 | 固定规则 |
|---|---|
| 原始输入 | 同一A01前端派生的合法估计历史、车辆集合、mask、SNR实现、划分和因果预处理；不以源状态替代state_hat |
| 节点编码 | P/V初始语义、角度中心、arctan尺度与重编码完全相同 |
| 经典边计算 | A02五维e⁺、ε=1m、s_v=15m/s、κ、分母7、边权参数域完全相同 |
| 本车混合 | 同一RXX及本车旋转位置、参数量与初始化 |
| 深度与参数 | 同L、同48（L3）或32（L2）核心参数；同24维投影和头；不加独立新角度或可训练门族 |
| 读出 | 严格使用同一24个通道、同一顺序和相同读出时刻；不随候选更换为专属测量 |
| 优化与评价 | 同数据顺序、噪声、配对种子、停止规则、图学习率、损失、训练预算和当前评价器 |

保持第一图块不变，使浅层9维及新增3维读出的定义和提取位置完全不变；深层12维仍测同一组算符。独立训练后第一层权重可以不同，因此不能声称各版本浅层数值永远相同。

这检验的是“当前编码和24维接口下，替换连接是否改善预测”，不是无条件比较所有拓扑的最优能力。如果候选信息落在当前读出不可见的方向，它可能不获益；允许据此否定本次固定接口替换的实用价值，但不能断言这种拓扑普遍无用。

不要为B/C增加专属VV测量、扩大读出或增加参数来追求分数。如果以后专门研究读出匹配，应独立立项；可以先在已冻结检查点上用两侧共有的局部Pauli读出做诊断，但不能将其结果混入本轮24维主比较。

### 参数与算力的核对

每块的跨车门仍为N(N−1)，N=8时56个；本车编码和混合门也不变。沿A02抽象计数，L3每时刻仍384个门，L2为256个；每场景T=20份整图态。A/B/C在同L下的参数、抽象门数、输入/输出维度全部相同。

不同Pauli旋转的后端实现、基变换、梯度算法、编译后门数和缓存可能不同，所以仍须测量每步前向/反向时间与峰值内存。相同抽象门数不等于相同GPU时间或硬件成本。主比较用合理训练和相同更新/早停规则，不为硬凑墙钟而让某组少训练；报告实际成本差异。

## 4. 不增加训练的路径诊断：区分速度通过哪里起作用

在合法的固定参数检查点上，把同一物理状态的使用位置在分析中拆成输入副本：节点编码S_E、原四维边S_e、门角度中的接近量a_g、读出权重a_R。所有副本的基点都取同一真实估计历史。

对v_B的局部总导数按链式法则拆为：

\[
D_{v_B}q=
(D_{S_E}q)(D_{v_B}S_E)
+(D_{S_e}q)(D_{v_B}S_e)
+(D_{a_g}q)(D_{v_B}a_g)
+(D_{a_R}q)(D_{v_B}a_R).
\]

依次只允许一个副本随v_B改变，其他副本停在同一基点；并验证四项之和等于完整输入梯度。第一项体现量子编码路径，第二项体现原相对速度/位置角度路径，后两项分开A02接近特征的两个用途。必要时对完整预测输出作同样分析，判断LLM是否对这些方向有响应。

这只是局部机制诊断：停止某条输入路径后构造的对象不一定对应可实现的交通场景，不能直接作为新的真实性能数据集。梯度大不等于有用，量子态响应也不等于未来标签可辨识；有限幅度干预具有交互项，不能把各项误差改善线性相加。配合自然开发片段上的ADE/FDE、预测变化方向和噪声敏感性解释。

基础校核还应包含：所有跨车角度为0时三组输出一致；车辆重排等变；无邻车与mask；小角度有限差分与解析导数；候选第二块算符对易性；同一初始参数下第一块浅层读出一致。不得把原T03的非零下界直接套到B/C。

## 5. 先导与完整LLM：次数及证据层次

### 5.1 配对先导

优先沿用A02 F04确认阶段已经选定的L、图学习率、同一训练manifest（最多2048个合法起点）、同四SNR日程与小头：两层因果Transformer、hidden128、4头、FFN256、dropout0，预测20步二维修正。

每个版本seeds2023/2024，各最多12epoch、耐心3，V_select早停，V_confirm在版本和权重固定后作一次评价。共同可训练参数使用相同初始数值，替换的仅是第二块算符；优化后允许各版本独立学习。若A现有F04确认运行与这些条件逐项一致，可以复用；否则必须补匹配A运行，不能混用小头与LLM、512与2048起点或不同深度的数据。

- A/B两组：共4次先导训练；已有合法A两次时，只新增B两次。
- 若加C：共6次先导训练；已有合法A两次时，共新增B/C四次。
- 不因C结果不好而继续换第四、第五种轴。

预声明晋级条件沿用A02的工程尺度：候选相对A的平均J改善至少2%，两个seed的ΔJ都为正，且ADE/FDE点估计均改善；数据、数值与成本检查通过。此处比原投入保护线更直接要求双指标方向，因为问题是是否值得为连接变化增加完整验证。它是投入规则而非统计定理。两候选均通过时取V_select J较低者，差不超过0.5%取实测成本较低者，再并列优先B；V_confirm只确认既定候选及方向，不按其分数重新排序候选。

如果先导未达标，本轮不晋级新增完整训练，不声称所有连接都无效。若共同停止规则导致某组仍明显未收敛，结论限于该日程，需重新预算才能研究充分训练后的能力。

V_confirm已参与A02开发的曝光记录继续保留，不能在本轮重新叫盲测；不读取最终测试来选连接。

### 5.2 最终ADE/FDE验证

最多晋级一个候选，与A02 A使用相同当前数据、100%合法训练集、同规格LLM和完整日程，seeds2026/2027/2028从头训练，复用同seed共同本车预热。若A02主训练已按完全相同合同完成，可以复用其三次；否则匹配基准也必须完成。

在A02原有9次核心训练之外，单一候选至少追加3次完整训练，变为12次核心训练。原F06-E探针、可选LLM时间对照、先导、新算符校核和成本测量均另计，不能说总共只增加三次。

先导通过后，测试前明确新增“候选对A”的ADE/FDE/J比较、与冻结GNN比较的主次和统计家族。A02原四项确认家族并未覆盖这个新模型；不能在旧区间阈值下多试模型，再把赢家当预先指定。若原最终测试已被用于结构选择，就不能再宣称这一测试是未使用确认数据。

候选赢A支持当前合同下连接/算符替换的完整系统增量；候选还赢G才支持其相对基线优势。若A本身没赢G，只赢A不足以支撑量子预测优势。三seed、小时间块的区间限制与A02一致，不能将重叠窗口当独立样本。

## 6. 与A02 F06-D/E的关系：哪些能复用，哪些不能冒充

| 现有实验 | 已经回答什么 | 本轮如何使用 |
|---|---|---|
| F06-D：旧Q对A02 | 接近边特征与读出共同修订后的整体效果 | 原样保留，不重做同一合法运行；不能用它隔离连接，因为两者拓扑相同 |
| F06-E：同电路21/24小头 | 固定候选表示下新增读出的预测价值 | 原样复用，说明读出增量；不能证明连接拓扑最优或接近边特征独立有效 |
| 新A/B | 固定接近特征与读出，仅替换第二块通道 | 回答连接问题；其条件是a和24维接口已经存在 |
| 可选新A/C | 固定连接支持，只替换第二块源Pauli轴 | 回答算符轴问题；不能估计通道×轴的全部交互 |
| 本稿路径导数 | 同检查点的编码/原边/接近角度/接近读出响应 | 无新训练的机制说明，不能代替最终ADE/FDE |

本轮通过“固定接近特征、固定读出”隔离连接效果，不必同时估计所有因素的独立贡献。若以后要研究接近特征与连接的交互，需要额外的交叉条件；特别是只把边角度中的a置零、却保留a加权读出，不叫“完全去掉接近特征”。额外零参数、补零输入也只能匹配名义参数量，不能声称有效容量和优化过程完全匹配。当前不扩展为全面因子搜索。

## 7. 若必须直接检验VV：一个备选及其陷阱

不建议首轮把所有Z_Pj X_Vi直接替换成Z_Vj X_Vi。三辆车时，Z_V1 X_V2与Z_V2 X_V3不对易；按槽编号串行执行可能破坏置换等变性。两条反向边Z_V1 X_V2和Z_V2 X_V1反而相互对易，不能用它们作不对易反例。更换顺序、Trotter步或联合Hamiltonian求指数，都已经引入新的设计和成本因素。

若另行选择直接VV问题，较容易保持对易性的备选是X_Vj X_Vi。它与所有原Z_Pk X_Vl及其他VV-XX项对易。可在第二块将每条边的角度预算分成

\[
e^{-ig_{ji}Z_{P_j}X_{V_i}/4}
e^{-ig_{ji}X_{V_j}X_{V_i}/4}.
\]

对应的调用数控制是两个原PV半角门，乘积精确等于原PV门。保持同一角度函数和参数量，不增加第二套边权。需要如实说明：该候选同时减弱PV并分配一半角预算给VV，检验的是资源再分配，不是在原PV强度不变下单纯添加VV。

VV反向两条门实际落在同一Pauli串，只保留g_ji+g_ij。在w_a=b=0等使g为反对称的子情形，该VV作用完全抵消；A02含对称接近量及偏置，通常并非反对称，不能说必然抵消。必须记录实际训练后的对称角度分量，避免解释一个实际没有作用的VV分支。

N=8、L=3时，第二块拆门后的未合并调用总数为440，原A02为384。VV两方向可合并为28个无向门；原PV半角重复控制则可合回原门，编译后实际成本不相等。不能通过禁止控制组门合并，把空转成本包装成公平硬件预算。必须同时报未合并调用、合并后门数和实测训练成本；如果要严格匹配有效门预算，需重新设计一个不同的比较，本稿不暗中追加。

因此首轮优先A/B，必要时加C；VV备选不与之同时展开。若用户更关心“直接VV”而不是“通道路由是否受限”，应在实施前明确选择VV这条问题及它较弱的归因边界，单独修订预算。

## 8. 实施前协议变更与结论边界

A02未授权上述新连接和先导；F06-D/E也明确不得触发结构搜索。若决定实施，需要新增有限连接研究条目，明确所选A/B（及是否C）、配对种子、成本上限、晋级与停止、最多一个完整候选、测试曝光与新增检验族，再同步模型规范与F03预算。当前不覆盖1.2文件，不修改代码，不运行训练。

图学习文献支持将结构和任务匹配作为研究对象，但特定MILP图判别结果不能证明当前车辆ZX结构或其替换一定更好。[Ye等，ICLR 2025](https://proceedings.iclr.cc/paper_files/paper/2025/file/ab59d149fc0c2c9039d3e3049f7914b1-Paper-Conference.pdf)

本稿能成立的判断是：隔离研究可行，且存在参数/门数匹配、置换性质清楚的第一轮替换。当前没有证据说明A02的速度路径已经足够，也没有证据说明直接VV或更密连接会降低ADE/FDE。

